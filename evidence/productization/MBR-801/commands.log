$ node --test tests/e2e/mbr801-run-harness.test.mjs
# PASS (9/9)
# ✔ resolvePlatform: explicit values win, otherwise falls back to process.platform
# ✔ runInstalledPathHarness: unsupported task returns open without touching disk
# ✔ runInstalledPathHarness: macOS run completes all ten scenarios with unique traces and a passed receipt
# ✔ runInstalledPathHarness: Windows run completes all ten scenarios with unique traces and a passed receipt
# ✔ acceptance: macOS and Windows receipts from the same commit both verify as passed, with globally unique traces
# ✔ a scenario that fails to produce a trace fails the receipt closed
# ✔ duplicate trace IDs across scenarios fail the receipt closed
# ✔ an unsigned or unverifiable build fails the receipt closed even if all scenarios pass
# ✔ an incomplete benchmark fails the receipt closed even if all scenarios pass
# tests 9, pass 9, fail 0

# Per MEMBRANE-BOOK-MODE-EXECUTION-RULES.md, the manifest command
# (`node scripts/qualification/run.mjs --task MBR-801`) is a real installed-path
# execution against a signed, installed macOS/Windows build with a live
# Membrane service, installed crypt CLI, and live event-log database. It is
# NOT executed during task implementation; it is deferred to the Book 2 final
# phased gate (gate phase 2.1), which runs it once per real platform:
#
#   node scripts/qualification/run.mjs --task MBR-801 --platform macos   --release-manifest <path> --event-db <path>
#   node scripts/qualification/run.mjs --task MBR-801 --platform windows --release-manifest <path> --event-db <path>
#
# This task's own deterministic tests (above) are the only commands executed
# here, per the task's hard rule limiting execution to `node --test <the
# specific test file written for this task>`.
