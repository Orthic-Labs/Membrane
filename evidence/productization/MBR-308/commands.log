# task=MBR-308; book=3 (Wave 4); status=implemented_pending_book_gate
# Per MEMBRANE-BOOK-MODE-EXECUTION-RULES.md ("Task implementation state" and
# "Local dispatch utility incompatibility"): no per-task test/qualification/PPM
# gate. The manifest `commands` list is deliberately NOT executed in this task
# change; it runs once, manually, at the Book 3 Wave 4 final phased gate
# against the integrated tree.
#
# Hard rule for this task change: never run cargo build / cargo test / cargo
# check / clippy / rustc / any Tauri or pnpm release build; the orchestrator
# runs heavy commands at gate time.

$ cargo fmt --manifest-path engine/Cargo.toml --all -- --check
# NOT RUN (deferred to Book 3 Wave 4 gate; hard rule forbids running cargo here)

$ cargo test --manifest-path engine/Cargo.toml --workspace
# NOT RUN (deferred to Book 3 Wave 4 gate; hard rule forbids running cargo here)

$ pnpm test
# NOT RUN (deferred to Book 3 Wave 4 gate)

# ---------------------------------------------------------------------------
# Scoped verification actually run in this task change, on the exact new/
# modified test files only, per the task's explicit allowance to run
# `node --test <the specific test file you wrote>` and
# `python3 -m pytest <your test file>` / `python3 -m unittest <your module>`:

$ node --test tests/sdk/http-transport-contract.test.mjs
✔ http-transport.md names the same path and headers as mcp_http.rs
✔ http-transport.md names every denial code the schema defines
✔ the Rust client's own compatibility tests exist and read the shared golden fixtures
ℹ tests 3
ℹ pass 3
ℹ fail 0

$ node --test tests/sdk/cross-language-compat.test.mjs
# pre-existing suite, re-run to confirm this task's changes did not regress it
✔ canonical v1 golden envelopes share TypeScript, Python & Rust constants
✔ TypeScript client accepts canonical success & types canonical error
✔ typed error sets match Python & Rust contracts
✔ clients are injected-transport only & reject version drift
ℹ tests 4
ℹ pass 4
ℹ fail 0

$ python3 tests/sdk/python_client_test.py
..........
----------------------------------------------------------------------
Ran 10 tests in 0.002s

OK

$ python3 -m unittest tests.sdk.python_client_test -v
# same 10 tests (5 pre-existing PythonClientTest + 5 new
# PythonClientGoldenFixtureTest), run via the module path form to confirm
# both invocation styles documented in docs/sdk/README.md work
# ... 10 passed, 0 failed

# engine/crates/membrane-client/tests/compat.rs (new Rust integration test)
# was NOT run: the hard rule forbids cargo in this task change. Its behavior
# was verified by careful manual type-checking against the crate's existing
# public API (MembraneClient, ProtocolResult, Transport, CONTEXT_OPERATION,
# SOURCE_READ_OPERATION — all already `pub` in engine/crates/membrane-client/
# src/lib.rs) and against the same golden fixtures loaded by the Python and
# TypeScript tests above, whose expected values (result.data, error code,
# retryable) were cross-checked by inspection of those fixture files. It is
# deferred to `cargo test --manifest-path engine/Cargo.toml --workspace` at
# the Book 3 Wave 4 gate along with every other Rust test.
