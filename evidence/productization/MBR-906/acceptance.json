{
  "schema": "orthic.membrane-evidence.acceptance.v1",
  "taskId": "MBR-906",
  "acceptance": "npx install works on supported platforms and rejects a checksum/signature mismatch.",
  "status": "pending_book_gate",
  "verifiedBy": "node --test tests/npm/*.test.mjs scripts/release/npm/*.test.mjs (23/23 pass, executed -- see commands.log). This includes the pre-existing tests/npm/bootstrap.test.mjs (5/5, from an earlier, incomplete pass at this task) unchanged, plus this task's four new files: tests/npm/install-cli.test.mjs (7/7), tests/npm/platform-packages.test.mjs (4/4), scripts/release/npm/resolve-platform-artifacts.test.mjs (7/7).",
  "observed": "'npx install works on supported platforms': npm/bin/membrane.mjs's runInstall resolves the optionalDependency platform package npm's own os/cpu matching selects for the host (selectPlatform), imports it once, and -- given a platform package that exports a correctly signed, matching-digest {artifact, metadata, dispatch} -- verifies and dispatches successfully; tests/npm/install-cli.test.mjs's 'runInstall verifies and dispatches the co-installed platform package' proves this end to end with an injected fixture (no real network or registry access, per this task's hard rules). The six real npm/platforms/<key>/package.json scaffolds (darwin-arm64, darwin-x64, linux-arm64, linux-x64, win32-arm64, win32-x64) give npm something concrete to resolve `npm i` against, wired into npm/package.json's optionalDependencies; tests/npm/platform-packages.test.mjs proves they stay in lockstep with npm/index.mjs's PLATFORM_PACKAGES registry. 'rejects a checksum/signature mismatch': proven at both the library layer (tests/npm/bootstrap.test.mjs, pre-existing) and the CLI layer this task adds (tests/npm/install-cli.test.mjs's 'checksum mismatch is rejected before dispatch' and 'signature mismatch is rejected before dispatch', both asserting ArtifactVerificationError and a `dispatched` flag that stays false). This task additionally ties the bootstrapper to MBR-903's real release pipeline: scripts/release/npm/resolve-platform-artifacts.mjs reads MBR-903's immutable evidence/releases/<releaseId>/release-generation.json and returns only artifacts MBR-903 itself marked 'verified', copied verbatim (name, sha256) -- never re-derived or invented -- and fails closed (ArtifactNotVerifiedError / NoPipelineTargetError) on every other real state, proven both against synthetic fixtures and directly against this actual checkout (which has none recorded yet).",
  "gap": "Actually running `npx @orthic/membrane install` against a real, published npm registry on a clean machine requires: (1) a real signed native artifact for at least one platform (none exists -- no cargo/Tauri/right-release build was run by this task or any task in its dependency chain today; MBR-903's own evidence/releases/**/release-generation.json has not been written for any releaseId in this checkout, confirmed directly by this task's tests), (2) a configured signing-key trust root for npm/bin/membrane.mjs's verifySignature (this package ships none by design -- SigningTrustNotConfiguredError is the deliberate fail-closed default; distributing/rotating that trust root is MBR-901/MBR-902/MBR-911 release-gate territory, outside this task's allowlist), and (3) an actual `npm publish` of the seven packages (root + six platform packages), which this task's hard rules explicitly forbid running or enabling unattended. Per MEMBRANE-BOOK-MODE-EXECUTION-RULES.md, this task ships the deterministic bootstrapper, per-platform package scaffolds, MBR-903-pipeline resolver, and CLI that make that later, credentialed run possible and verifiable -- it does not claim the run itself happened, and status is recorded as pending_book_gate, not pass. The manifest command `node scripts/release/verify-release-plan.mjs --task MBR-906` was not run: that script does not exist in this checkout yet (created by a later, gate-owning change) and per the book-mode execution rules, per-task manifest commands are reserved for the Book 3 final phased gate.",
  "at": "2026-08-08T18:10:00Z"
}
