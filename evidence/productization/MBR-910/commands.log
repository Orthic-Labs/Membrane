$ node --test tests/oci/generate-oci-release.test.mjs tests/oci/oci-contract.test.mjs
✔ readReleaseGeneration accepts a real MBR-903 record and rejects a tampered releaseId
✔ buildOciRelease binds identity to the real release-generation record and validates real evidence
✔ buildOciRelease fails closed when release-generation.json does not exist yet
✔ buildOciRelease fails closed when --base drifts from the Containerfile's FROM line
✔ buildOciRelease fails closed when a required evidence file is missing
✔ buildOciRelease fails closed on a non-digest-pinned --image
✔ writeOciRelease writes packaging/oci/release.v1.json, is idempotent, and refuses to overwrite a different ready release
✔ leaves an existing unavailable release.v1.json alone when generation fails
✔ OCI contract stays unavailable & non-publishing
✔ OCI contract rejects release-ready mutation
✔ ready OCI contract requires every hash-bound release proof
✔ Containerfile fixes non-root, digest base & a real headless healthcheck
✔ Containerfile exposes no network port by default (loopback-bound guarantee)
# tests 13, pass 13, fail 0.

# This is the only executable command run for this task, per its hard rule
# "You may run node --test <the specific test file you wrote>; nothing
# heavier." Both files are ones this task wrote (tests/oci/generate-oci-release.test.mjs,
# new) or modified (tests/oci/oci-contract.test.mjs, HEALTHCHECK regex +
# no-EXPOSE assertion). All filesystem fixtures in generate-oci-release.test.mjs
# are written under node:os tmpdir() (via mkdtempSync); none touch the real
# repository's packaging/oci/release.v1.json or evidence/releases/.
#
# Confirmed after the run:
#   $ git status --short -- packaging/oci scripts/release/oci tests/oci \
#       evidence/releases evidence/productization/MBR-910 docs/install-oci.md
#   M  packaging/oci/Containerfile
#   M  tests/oci/oci-contract.test.mjs
#   ?? scripts/release/oci/
#   ?? tests/oci/generate-oci-release.test.mjs
#   ?? docs/install-oci.md
#   ?? evidence/productization/MBR-910/
# -- i.e. only this task's own changes; nothing under evidence/releases/ or
# packaging/oci/release.v1.json (which stays untouched, still "unavailable").

# No cargo, pnpm, Tauri, right-release, docker, podman, buildah, cosign,
# syft, trivy, or any signing command was run. No mutating git command
# (add/commit/checkout/stash/reset/rebase/push) was run at any point.
# `git status` / `git log` / `git show` / `find` (read-only, informational)
# were used only to read the pre-existing scaffold (packaging/oci/Containerfile,
# packaging/oci/release.v1.json, scripts/release/verify-oci-release.mjs,
# tests/oci/oci-contract.test.mjs -- all committed at b7b01a8f) and MBR-903's
# landed output (commit 3811091e), and to confirm scope containment; they are
# not part of the task's release logic and are not "the manifest commands list."

# node scripts/release/verify-release-plan.mjs --task MBR-910 (the manifest's
# `commands` entry) -- NOT RUN. Per MEMBRANE-BOOK-MODE-EXECUTION-RULES.md and
# this task's explicit instructions, the manifest commands list is executed
# once at the final phased book gate, not per task; acceptance.json status
# stays pending_book_gate.

# scripts/release/oci/generate-oci-release.mjs's own CLI (`node
# scripts/release/oci/generate-oci-release.mjs --release-generation ... --write`)
# -- NOT RUN against real inputs. It requires a real evidence/releases/<releaseId>/
# release-generation.json (which requires signed macOS/Windows builds neither
# of which exist yet, per MBR-901/MBR-902's own evidence) plus a real Linux
# binary, SBOM, Ed25519 signature, cosign receipt, rootless-health receipt,
# and secret-scan report -- none of which this task is permitted or able to
# produce (no docker/podman/build/signing commands). Its behavior against
# synthetic-but-real fixture files is proven instead, by
# tests/oci/generate-oci-release.test.mjs above.

# scripts/release/verify-oci-release.mjs -- NOT MODIFIED. Read and imported
# only (generate-oci-release.mjs imports verifyOciRelease unchanged, and
# reuses it as the sole validator before ever writing packaging/oci/release.v1.json).
