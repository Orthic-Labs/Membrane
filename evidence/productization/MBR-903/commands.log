$ node --test scripts/release/pipeline/pipeline.test.mjs
✔ computeReleaseGeneration is deterministic for identical inputs
✔ computeReleaseGeneration changes when the source tree drifts
✔ computeReleaseGeneration is order-sensitive on targets but insensitive to object key order
✔ computeReleaseGeneration fails closed on invalid identity fields
✔ platform-targets names all four release/contracts/platforms.v1.json targets in order
✔ platform-targets is honest about the two targets RightKit cannot build today
✔ readSealedPlatformStage reports not-buildable targets without touching disk
✔ readSealedPlatformStage reports pending when nothing is sealed yet
✔ readSealedPlatformStage verifies a matching sealed mac artifact against its recomputed hash
✔ readSealedPlatformStage verifies a matching sealed windows artifact against RightKit's real manifest shape
✔ readSealedPlatformStage fails closed with ReleaseDriftError when a sealed artifact names a different commit (mac)
✔ releaseId matches RightKit's own convention: app-version-shortcommit
✔ buildMultiPlatformPlan never fabricates a verified target with nothing sealed
✔ buildMultiPlatformPlan is reproducible: identical inputs on two separate calls produce byte-identical plans
✔ buildMultiPlatformPlan surfaces cross-platform drift instead of silently combining mismatched commits
✔ buildMultiPlatformPlan fails closed on malformed identity
✔ writeImmutableReleaseGeneration is idempotent for an unchanged plan and rejects drift under the same releaseId
# tests 17, pass 17, fail 0.

# This is the only executable command run for this task. It is a plain Node
# structural/behavioral test over the new pure functions and fixture-backed
# I/O in scripts/release/pipeline/{identity,platform-targets,sealed-platforms,
# multi-platform-release}.mjs. All filesystem fixtures are written under
# node:os tmpdir() (via mkdtempSync) and removed in a `finally` block; no
# test wrote to or read the real repository's .right-release/ or
# evidence/releases/ directories. Confirmed after the run:
#   $ git status --short -- scripts/release/pipeline release/contracts \
#       evidence/releases evidence/productization/MBR-903 docs/release/pipeline.md
#   ?? release/contracts/release-generation.v1.schema.json
#   ?? scripts/release/pipeline/
# -- i.e. only this task's own new files, nothing under evidence/releases/
# or any temp/fixture leakage.

# No cargo, pnpm, Tauri, right-release, codesign, notarytool, stapler,
# spctl, signtool, or AzureSignTool command was run. No mutating git command
# (add/commit/checkout/stash/reset/rebase/push) was run at any point.
# `git status`/`git diff --stat`/`find` (read-only, informational) were used
# only to inspect the working tree while writing this task's own files and
# to confirm scope containment; they are not part of the task's release
# logic and are not "the manifest commands list."

# scripts/release/orchestrate-release.mjs, scripts/release/verify-release-evidence.mjs,
# scripts/release/macos/contract.mjs, scripts/release/windows/contract.mjs --
# NOT MODIFIED. Read and imported only (sealed-platforms.mjs imports
# sealedReleaseDir/readSealedReleaseManifest/verifySealedInstallerHash from
# macos/contract.mjs and validateSealedManifest from windows/contract.mjs;
# multi-platform-release.mjs imports the target list from
# release/contracts/platforms.v1.json and pipeline.v1.json, both pre-existing
# and unmodified).

# node scripts/release/verify-release-plan.mjs --task MBR-903 (the manifest's
# `commands` entry) -- NOT RUN. Per MEMBRANE-BOOK-MODE-EXECUTION-RULES.md and
# this task's explicit instructions, the manifest commands list is executed
# once at the final phased book gate, not per task; acceptance.json status
# stays pending_book_gate.

# pnpm release:doctor / pnpm release:build:mac / pnpm release:build:win --
# NOT RUN. These require macOS and Windows machines with the Rust/Tauri
# toolchain, Apple notarization credentials, and Azure Artifact Signing
# credentials; this task's hard rules forbid running cargo, pnpm, Tauri, or
# right-release from here. Running them, on each platform's own machine
# after pulling this commit, is exactly what
# scripts/release/pipeline/multi-platform-release.mjs is written to read
# the sealed output of -- see docs/release/pipeline.md.
