{
  "schema": "orthic.membrane-evidence.acceptance.v1",
  "taskId": "MBR-1002",
  "acceptance": "A new user reaches proven non-zero delivery without reading architecture docs.",
  "status": "pending_book_gate",
  "verifiedBy": "node --test tests/onboarding/quickstart.test.mjs tests/onboarding/getting-started-consistency.test.mjs",
  "observed": "Two things are proven now, without a build or a live service. First, the pre-existing offline fixture (examples/quickstart/run.mjs, from prior commit 0b8bdde5, left unmodified) mechanically demonstrates the acceptance criterion end to end: `node examples/quickstart/run.mjs` reads a receipt fixture and a service marker and emits a receipt-bound packet in milliseconds -- non-zero delivery, reached by running two documented commands, with no architecture doc required -- and `node examples/quickstart/run.mjs --degraded` proves the same path fails closed (packet: null) when the service marker is absent. Its three tests in tests/onboarding/quickstart.test.mjs (unchanged) still pass. Second, docs/getting-started.md's six-step live path (install, enroll, activate, first packet, receipt, forced degradation) documents the same journey against the real membrane_context MCP tool instead of a fixture; this task's new test, tests/onboarding/getting-started-consistency.test.mjs, mechanically checks every command, env var, required-field list, and prerequisite claim in that doc against the real source it depends on (mcp/install.mjs, mcp/client.mjs, mcp/server.mjs, mcp/project-registry.mjs, mcp/installation-binding.mjs, package.json, README.md, docs/support-matrix.json) so the doc cannot silently drift out of truth. All 12 of its assertions plus the 3 pre-existing fixture tests pass -- 15/15, 0 failures (see commands.log).",
  "notVerified": [
    "A real live-path round trip (an actual Crypt loopback service running, a real native client (`codex` or `claude`) enrolling this checkout via `mcp/install.mjs install`, and a genuine `membrane_context` tool call returning a non-fixture packet) was not executed. This requires a running service and a native client already authenticated -- outside this task's hard rules (no cargo/pnpm/Tauri builds) and reserved for the final phased book gate per book-mode execution rules. Partial due-diligence dry runs against mcp/install.mjs were performed to verify the documented commands are real (see commands.log's comment block and summary.json) but are not this task's acceptance evidence.",
    "The live path's non-dry-run enrollment and activation steps require this exact checkout to be nested inside the parent Orthic Labs workspace (tools/lib/memory/runtime.json must exist above the repository root) -- this is now disclosed as an explicit prerequisite in docs/getting-started.md rather than left to fail silently for a reader outside that workspace; it is not a defect this task can close, since fixing it is a distribution/packaging concern (tracked by other Book 3 tasks such as MBR-903/MBR-906/MBR-910), not an onboarding-doc concern."
  ],
  "deferredCommand": "cargo fmt --manifest-path engine/Cargo.toml --all -- --check && cargo test --manifest-path engine/Cargo.toml --workspace (this task's manifest commands; not run -- forbidden by this task's own hard rules and reserved for the final phased book gate per book-mode execution rules)",
  "at": "2026-08-08T17:34:39Z"
}
