{
  "version": "membrane.toolsets.v1",
  "registry": "closed",
  "note": "The public MCP registry is intentionally closed to pull and push. Toolset group names remain an accepted `_meta/membrane.toolsets.v1` request vocabulary for compatibility; no requested group expands the public registry. Retired membrane_* names are internal operations reachable only through the pull/push envelopes, never through tools/list discovery.",
  "groups": {
    "default": ["pull", "push"],
    "memory": ["pull", "push"],
    "blueprint": ["pull", "push"],
    "diagnostic": ["pull", "push"],
    "push": ["pull", "push"],
    "operator": ["pull", "push"]
  }
}
