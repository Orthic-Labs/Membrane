{
 "message":"feat(ledger): publish structural manifests, backlinks and durable exclusion policy\n\nShare one AST across section, block and link projection. Wire the three new\ncapabilities through the same owner. Keep source exclusions in the Membrane\ncatalog so an index rebuild cannot re-enroll erased documents. Preserve\nexplicit snapshot conversion provenance without claiming format qualification.",
 "files":[
  {"path":"engine/crates/membrane-runtime/src/ledger/mod.rs","sha":"6a3398fbefbc81dc1f7dec827bb02cf5d70e6460","replacements":[{"before":"pub mod db;","after":"pub(crate) mod diagnostics;\npub(crate) mod erasure;\npub mod db;"}]},
  {"path":"engine/crates/membrane-runtime/src/ledger/outline.rs","sha":"02b1c68c1f73333ee3bcc0b275c99537651243be","replacements":[
   {"before":"    let content_hash = hash(markdown);\n    let line_starts = line_starts(markdown);\n    let mut pending = Vec::new();\n    let arena = Arena::new();","after":"    let arena = Arena::new();"},
   {"before":"    let root = parse_document(&arena, markdown, &options);\n    let mut ordinals","after":"    let root = parse_document(&arena, markdown, &options);\n    build_outline_from_ast(source_ref, markdown, root, max_sections, continuation_cursor)\n}\n\npub(crate) fn build_outline_from_ast<'a>(\n    source_ref: &str, markdown: &str, root: &'a comrak::nodes::AstNode<'a>,\n    max_sections: usize, continuation_cursor: Option<&str>,\n) -> Result<DocOutlineV1, DocReadError> {\n    let content_hash = hash(markdown);\n    let line_starts = line_starts(markdown);\n    let mut pending = Vec::new();\n    let mut ordinals"}
  ]},
  {"path":"engine/crates/membrane-runtime/src/ledger/index.rs","sha":"b56f59592bd96e0ec7e487450533edc37609416c","replacements":[
   {"before":"use super::{outline::build_outline_page, LedgerDb};","after":"use super::{outline::build_outline_from_ast, LedgerDb};"},
   {"before":"    let arena = Arena::new();\n    let mut options = Options::default();\n    options.extension.front_matter_delimiter = Some(\"---\".to_owned());\n    options.extension.table = true;\n    options.extension.strikethrough = true;\n    options.extension.tasklist = true;\n    options.extension.autolink = true;\n    options.extension.footnotes = true;\n    options.render.sourcepos = true;\n    let root = parse_document(&arena, input.markdown, &options);\n","after":""},
   {"before":"    // Presentation pagination must never truncate the internal index.","after":"    let arena = Arena::new();\n    let mut options = Options::default();\n    options.extension.front_matter_delimiter = Some(\"---\".to_owned());\n    options.extension.table = true;\n    options.extension.strikethrough = true;\n    options.extension.tasklist = true;\n    options.extension.autolink = true;\n    options.extension.footnotes = true;\n    options.render.sourcepos = true;\n    let root = parse_document(&arena, input.markdown, &options);\n    // One AST supplies all section, block, and link projections.\n    // Presentation pagination must never truncate the internal index."},
   {"before":"    let outline = build_outline_page(","after":"    let outline = build_outline_from_ast("},
   {"before":"        input.markdown,\n        input.parser_version,\n        usize::MAX,","after":"        input.markdown,\n        root,\n        usize::MAX,"},
   {"before":"    let mut block_ordinal = outline.sections.len();","after":"    let mut block_ordinal = outline.sections.len();\n    let mut block_parents = BTreeMap::<usize, String>::new();"},
   {"before":"        let parent_id = anchor_to_node.get(&containing_section.anchor_id).cloned();","after":"        let parent_id = node.ancestors().skip(1)\n            .find_map(|ancestor| block_parents.get(&(ancestor as *const _ as usize)).cloned())\n            .or_else(|| anchor_to_node.get(&containing_section.anchor_id).cloned());"},
   {"before":"        block_ordinal += 1;","after":"        block_parents.insert(node as *const _ as usize, node_id.clone());\n        block_ordinal += 1;"},
   {"before":"    super::link_projection::replace_link_projection_tx(","after":"    super::link_projection::replace_link_projection_from_ast_tx("},
   {"before":"        input.source_revision,\n        input.generation,\n    )?;\n    Ok(())","after":"        input.source_revision,\n        input.generation,\n        root,\n    )?;\n    super::diagnostics::record_manifest_tx(tx, input.doc_id)?;\n    Ok(())"},
   {"before":"pub const PROJECTION_SCHEMA_VERSION: &str = \"ledger.projection.v3\";","after":"pub const PROJECTION_SCHEMA_VERSION: &str = \"ledger.projection.v4\";"}
  ]},
  {"path":"engine/crates/membrane-runtime/src/ledger/link_projection.rs","sha":"9923534e2b8241f3b8258cddfe8a05f9bf7154c4","replacements":[
   {"before":"    tx.execute(\n        \"DELETE FROM ledger_link_targets WHERE source_doc_id=?1\",\n        [doc_id],\n    )?;\n    let arena = Arena::new();","after":"    let arena = Arena::new();"},
   {"before":"    let root = parse_document(&arena, markdown, &options);\n    let starts = source_line_starts(markdown);","after":"    let root = parse_document(&arena, markdown, &options);\n    replace_link_projection_from_ast_tx(tx, doc_id, path, markdown, source_revision, generation, root)\n}\n\npub(crate) fn replace_link_projection_from_ast_tx<'a>(\n    tx: &Transaction<'_>, doc_id: &str, path: &str, markdown: &str,\n    source_revision: &str, generation: i64, root: &'a comrak::nodes::AstNode<'a>,\n) -> rusqlite::Result<()> {\n    tx.execute(\"DELETE FROM ledger_link_targets WHERE source_doc_id=?1\", [doc_id])?;\n    let starts = source_line_starts(markdown);"}
  ]},
  {"path":"engine/crates/membrane-runtime/src/ledger/doc_spine.rs","sha":"89cb8cb0c71009ff92d000a2700d74c263acff95","replacements":[
   {"before":"                registered += 1;\n                skipped += 1;","after":"                super::diagnostics::record_manifest_tx(&tx, id).map_err(|e|e.to_string())?;\n                registered += 1;\n                skipped += 1;"}
  ]},
  {"path":"engine/crates/membrane-runtime/src/ledger/query.rs","sha":"6b2ff6e510aff264a5c5d9b8439245d8d1cbdeb8","replacements":[
   {"before":"        if !policy.allows(&document.path, false, budget)? { continue; }","after":"        let erased: bool = db.lock().query_row(\n            \"SELECT EXISTS(SELECT 1 FROM ledger_erasure_fences WHERE repository_root=?1 AND path_digest=?2)\",\n            params![root,resolve::digest(document.path.as_bytes())],|r|r.get(0)).map_err(|e|e.to_string())?;\n        if erased || !policy.allows(&document.path, false, budget)? { continue; }"}
  ]},
  {"path":"engine/crates/membrane-runtime/src/ledger/service.rs","sha":"adf509f5ddc3925923220b6af7f1307732d94d71","replacements":[
   {"before":"    db: LedgerDb,\n    operation:","after":"    db: LedgerDb,\n    catalog: crate::catalog::ContextCatalog,\n    operation:"},
   {"before":"    fn new(db: LedgerDb) -> Result<Self, String> {\n        db.lock()","after":"    fn new(db: LedgerDb) -> Result<Self, String> {\n        let catalog = crate::catalog::ContextCatalog::open(crate::catalog::default_catalog_path().map_err(|e|e.to_string())?).map_err(|e|e.to_string())?;\n        Self::with_catalog(db,catalog)\n    }\n    fn with_catalog(db:LedgerDb,catalog:crate::catalog::ContextCatalog)->Result<Self,String> {\n        db.lock().execute_batch(super::diagnostics::SCHEMA).map_err(|e|e.to_string())?;\n        if index::recall_mode(&db)? == index::LedgerRecallMode::LedgerFts {\n            index::activate(&db,index::LedgerRecallMode::Shadow,None)?;\n        }\n        db.lock()"},
   {"before":"Ok(Self { db, operation: Mutex::new(()) })","after":"Ok(Self { db, catalog, operation: Mutex::new(()) })"},
   {"before":"Self::new(LedgerDb::open_in_memory()).unwrap()","after":"Self::with_catalog(LedgerDb::open_in_memory(),crate::catalog::ContextCatalog::open_in_memory()).unwrap()"},
   {"before":"        let result = work(&self.db);","after":"        super::erasure::synchronize(&self.catalog,&self.db,&caller.root,budget)?;\n        let result = work(&self.db);"},
   {"before":"            \"erase\" => self.run(&caller, \"checkpoint\", budget, |db| erase(db, &caller, arguments)),","after":"            \"erase\" => self.run(&caller, \"checkpoint\", budget, |db| erase(db, &self.catalog, &caller, arguments)),\n            \"backlinks\" | \"manifests\" | \"drift\" => self.run(&caller, \"context\", budget, |db| {\n                let doc = required_string(arguments,\"docId\")?;\n                match operation {\n                    \"backlinks\" => super::diagnostics::backlinks(db,&caller.root,&doc,arguments.get(\"nodeId\").and_then(Value::as_str),\n                        arguments.get(\"limit\").and_then(Value::as_u64).unwrap_or(64) as usize,budget),\n                    \"manifests\" => super::diagnostics::manifests(db,&caller.root,&doc,budget),\n                    _ => super::diagnostics::drift(db,&caller.root,&doc,&required_string(arguments,\"fromManifest\")?,&required_string(arguments,\"toManifest\")?,budget),\n                }\n            }),\n            \"ingest\" => self.run(&caller,\"checkpoint\",budget,|db| {\n                use super::document_conversion::{DocumentInputFormatV1 as Format,DocumentConversionGrantV1,DocumentConversionInputV1};\n                let path=required_string(arguments,\"path\")?;\n                permitted_path(db,&caller.root,&path,budget)?;\n                let format=match arguments.get(\"format\").and_then(Value::as_str) {\n                    Some(\"plain_text\")=>Format::PlainText,Some(\"html\")=>Format::Html,Some(\"json\")=>Format::Json,\n                    _=>return Err(\"ledger_format_qualification_required\".into()),\n                };\n                if path.to_ascii_lowercase().ends_with(\".md\") {return Err(\"ledger_markdown_uses_sync\".into());}\n                let raw=resolve::confined_bytes(Path::new(&caller.root),&path).map_err(|e|e.to_string())?;\n                budget.charge_bytes(raw.len())?;\n                let expected=required_string(arguments,\"expectedContentHash\")?;\n                let hash=resolve::digest(&raw);\n                if !resolve::hash_matches(&expected,&hash) {return Err(\"ledger_source_stale\".into());}\n                let grant=DocumentConversionGrantV1::new([format.clone()],resolve::MAX_SOURCE_BYTES);\n                let artifact=doc_spine::ingest_granted_document(db,&grant,doc_spine::GrantedDocumentIngestV1 {\n                    repository_root:caller.root.clone(),repository_id:caller.repository_id.clone(),revision:format!(\"snapshot:{hash}\"),\n                    path:path.clone(),title:path.clone(),document:DocumentConversionInputV1 {source_ref:format!(\"snapshot:sha256:{hash}\"),format,raw_input:raw},\n                })?;\n                permitted_path(db,&caller.root,&path,budget)?;\n                Ok(json!({\"schemaVersion\":1,\"artifact\":artifact,\"sourceKind\":\"imported_snapshot\",\"qualification\":\"pending\",\"externalFreshnessClaimed\":false}))\n            }),"},
   {"before":"            if let Ok(reference) = super::identifier::WorktreeDocRef::parse(&request.source_ref) {","after":"            if let Some(doc)=request.doc_id.as_deref().or_else(||request.source_ref.strip_prefix(\"ledger://doc/\")) {\n                let path:String=db.lock().query_row(\"SELECT path FROM ledger_doc_artifacts WHERE repository_root=?1 AND doc_id=?2\",\n                    params![caller.root,doc],|r|r.get(0)).map_err(|_|\"ledger_source_missing\")?;\n                permitted_path(db,&caller.root,&path,budget)?;\n            }\n            if let Ok(reference) = super::identifier::WorktreeDocRef::parse(&request.source_ref) {","count":2},
   {"before":"fn erase(db: &LedgerDb, caller: &Caller, arguments: &Value)","after":"fn erase(db: &LedgerDb, catalog: &crate::catalog::ContextCatalog, caller: &Caller, arguments: &Value)"},
   {"before":"    tx.execute(\"INSERT OR REPLACE INTO ledger_erasure_fences VALUES (?1,?2,?3)\",","after":"    let identities:i64=tx.query_row(\"SELECT COUNT(*) FROM ledger_doc_artifacts WHERE doc_id=?1\",[&doc_id],|r|r.get(0)).map_err(|e|e.to_string())?;\n    if identities!=1 {return Err(\"ledger_ambiguous_source_identity\".into());}\n    super::erasure::record(catalog,&caller.root,&resolve::digest(path.as_bytes()))?;\n    tx.execute(\"INSERT OR REPLACE INTO ledger_erasure_fences VALUES (?1,?2,?3)\","},
   {"before":"\"ledger_query_aliases\",\"ledger_document_conversions\",\"ledger_resolution_tickets\"]","after":"\"ledger_query_aliases\",\"ledger_document_conversions\",\"ledger_resolution_tickets\",\"ledger_document_manifests\"]"}
  ]}
 ]
}
