{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hub-local-ui",
  "description": "Allow on-demand Membrane Hub dashboard to use its local Tauri command & window bridge.",
  "windows": ["dashboard"],
  "permissions": ["core:default"]
}
