import test from "node:test";
import assert from "node:assert/strict";
import { execFileSync, spawnSync } from "node:child_process";
import { chmodSync, existsSync, mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { delimiter, dirname, join } from "node:path";
import { assetsJsonPath, checkBuilt, checkPackagedMac, finalizeMac, parseArgs as assetArgs, prepareBundle, sha256OfFile, sidecarSourcePath, strictIdentity, targetTriple } from "../scripts/release-assets.mjs";
import { assembleManifest, atomicWriteManifest, bundleRoot, checkCommitted, checkManifest, checkOrBootstrap, gatherSortedAssets, readReceipt, releaseManifestPath } from "../scripts/write-release-manifest.mjs";
import { engineReleaseIdentity } from "../scripts/release-identity.mjs";

// Mac is current release authority. Non-Mac hosts assert its native-host guard.
const HOST=`${process.platform}-${process.arch}`;
const NATIVE_PLATFORM=HOST==="darwin-arm64"?"mac":null;
const unsignedName=()=>`cortex-${targetTriple("mac")}.unsigned`;

const temp=(name)=>mkdtempSync(join(tmpdir(),`membrane-${name}-`));
const git=(dir,args)=>execFileSync("git",["-C",dir,...args],{encoding:"utf8"}).trim();
function init(dir){execFileSync("git",["-C",dir,"init","--initial-branch","main","--quiet"]);for(const [key,value] of [["user.email","test@example.com"],["user.name","test"]])execFileSync("git",["-C",dir,"config",key,value]);}
function commit(dir,message){execFileSync("git",["-C",dir,"add","."]);execFileSync("git",["-C",dir,"commit","-m",message,"--quiet"]);}
function repo(name,fn){const root=temp(`${name}-membrane`);init(root);mkdirSync(join(root,"engine"),{recursive:true});writeFileSync(join(root,"engine/source"),"x\n");const hub=join(root,"apps/membrane-hub"),target="aarch64-apple-darwin";for(const bin of ["cortex","membrane"]){const path=join(hub,"src-tauri/binaries",`${bin}-${target}`);mkdirSync(join(path,".."),{recursive:true});writeFileSync(path,`${bin}-${target}`);}writeFileSync(join(hub,"membrane-hub-release.json"),"{}\n");commit(root,"fixture");const head=git(root,["rev-parse","HEAD"]);try{return fn({root,hub,head});}finally{rmSync(root,{recursive:true,force:true});}}
function receipt(id){const target="aarch64-apple-darwin";return {schemaVersion:1,app:"membrane-hub",version:"0.1.17",platform:"mac",target,identity:id,files:{cli:{name:`cortex-${target}`,sha256:"a".repeat(64),signature:"developer-id",team:"ABCDE12345"},membrane:{name:`membrane-${target}`,sha256:"b".repeat(64),signature:"developer-id",team:"ABCDE12345"}}};}

test("identity is exactly six clean keys & actual CLIs enter guards",()=>{const id={commit:"0".repeat(40),dirty:false,fileCount:0,sourceTreePath:"engine",sourceTreeSha256:"a".repeat(64),releaseGeneration:`sha256:${"a".repeat(64)}`},asset=new URL("../scripts/release-assets.mjs",import.meta.url),writer=new URL("../scripts/write-release-manifest.mjs",import.meta.url);assert.deepEqual(strictIdentity(JSON.stringify(id)),id);assert.throws(()=>strictIdentity(JSON.stringify({...id,dirty:true})));assert.throws(()=>assetArgs(["node","x","prepare","--platform","mac","--skip-host-check"]));assert.notEqual(spawnSync(process.execPath,[asset.pathname,"wat"]).status,0);assert.notEqual(spawnSync(process.execPath,[writer.pathname,"wat"]).status,0);});
test("write-release-manifest check bootstraps a first build (no paired receipts yet) & still hard-fails malformed receipts",()=>repo("bootstrap",({root})=>{const id=engineReleaseIdentity(root),logs=[],log=(line)=>logs.push(line);
  let result=checkOrBootstrap({repoRoot:root,requireCommitted:true,log});
  assert.equal(result.bootstrap,true);assert.equal(result.macPresent,false);
  assert.match(logs.at(-1),/^bootstrap: first Mac build/);assert.match(logs.at(-1),/mac receipt absent/);
  logs.length=0;result=checkOrBootstrap({repoRoot:root,requireCommitted:false,log});assert.equal(result.bootstrap,true);
  const mac=receipt(id),macPath=join(bundleRoot(root,id.commit),"assets.json");mkdirSync(dirname(macPath),{recursive:true});writeFileSync(macPath,JSON.stringify(mac));
  logs.length=0;result=checkOrBootstrap({repoRoot:root,requireCommitted:true,log});
  assert.equal(result.bootstrap,true);assert.equal(result.macPresent,true);assert.doesNotMatch(logs.at(-1),/receipt absent/);
  writeFileSync(macPath,JSON.stringify({...mac,files:undefined}));
  assert.throws(()=>checkOrBootstrap({repoRoot:root,requireCommitted:true,log}),/invalid receipt/);
}));
test("write-release-manifest bootstraps when optional committed manifest is absent",()=>repo("manifest-absent",({root})=>{rmSync(releaseManifestPath(root));const logs=[];const result=checkOrBootstrap({repoRoot:root,requireCommitted:true,log:(line)=>logs.push(line)});assert.equal(result.bootstrap,true);assert.match(logs.at(-1),/mac receipt absent/);}));
test("prepare ignores generated ignored trees & check-built rejects source changes",(t)=>repo("built",({root,hub})=>{mkdirSync(join(root,"ignored"),{recursive:true});writeFileSync(join(root,".gitignore"),"ignored/\n");commit(root,"ignore generated tree");for(let index=0;index<4000;index+=1)writeFileSync(join(root,"ignored",`${index}.txt`),"generated");if(NATIVE_PLATFORM){const target=targetTriple("mac");const result=prepareBundle({repoRoot:root,hubDir:hub,platform:"mac"});assert.ok(existsSync(join(result.root,"unsigned",unsignedName())));checkBuilt({repoRoot:root,hubDir:hub,platform:"mac"});writeFileSync(sidecarSourcePath(hub,target,"cortex"),"changed");assert.throws(()=>checkBuilt({repoRoot:root,hubDir:hub,platform:"mac"}),/sha256/);}else{t.diagnostic(`host ${HOST} is not darwin-arm64; asserting Mac host guard`);assert.throws(()=>prepareBundle({repoRoot:root,hubDir:hub,platform:"mac"}),/requires host/);}}));
test("Mac final receipt is exact & packaged state is receipt-bound",(t)=>repo("mac",({root,hub})=>{const app=join(root,"app/Contents/MacOS");if(NATIVE_PLATFORM!=="mac"){t.diagnostic(`host ${HOST} is not darwin-arm64; asserting host() rejects the mac lane instead of running the codesign-backed path`);assert.throws(()=>prepareBundle({repoRoot:root,hubDir:hub,platform:"mac"}),/requires host/);assert.throws(()=>finalizeMac({repoRoot:root,platform:"mac",appMacosDir:app}),/requires host/);assert.throws(()=>checkPackagedMac({repoRoot:root,platform:"mac",appMacosDir:app}),/requires host/);return;}const prepared=prepareBundle({repoRoot:root,hubDir:hub,platform:"mac"}),id=engineReleaseIdentity(root),bin=join(root,"bin");mkdirSync(app,{recursive:true});mkdirSync(bin,{recursive:true});writeFileSync(join(app,"cortex"),"cortex");writeFileSync(join(app,"membrane"),`#!/bin/sh\nprintf '%s\\n' '${JSON.stringify({membrane_source_commit:id.commit,source_tree_sha256:id.sourceTreeSha256,release_generation:id.releaseGeneration,target:"aarch64-apple-darwin"})}'\n`);for(const name of ["cortex","membrane"])chmodSync(join(app,name),0o755);for(const command of ["codesign","lipo"]){writeFileSync(join(bin,command),command==="codesign"?"#!/bin/sh\nif [ \"$1\" = \"-dvvv\" ]; then echo TeamIdentifier=ABCDE12345 1>&2; fi\nexit 0\n":"#!/bin/sh\nexit 0\n");chmodSync(join(bin,command),0o755);}const old=process.env.PATH;process.env.PATH=`${bin}${delimiter}${old}`;try{const value=finalizeMac({repoRoot:root,hubDir:hub,platform:"mac",appMacosDir:app});assert.deepEqual(Object.keys(value).sort(),["app","files","identity","platform","schemaVersion","target","version"]);assert.equal(value.files.cli.sha256,sha256OfFile(join(app,"cortex")));checkPackagedMac({repoRoot:root,platform:"mac",appMacosDir:app});const mac=readReceipt(assetsJsonPath(prepared.root),"mac");assert.equal(mac.files.cli.sha256,value.files.cli.sha256);value.files.cli.team="";writeFileSync(assetsJsonPath(prepared.root),JSON.stringify(value));assert.throws(()=>readReceipt(assetsJsonPath(prepared.root),"mac"));value.files.cli.team="ABCDE12345";writeFileSync(join(app,"membrane"),"#!/bin/sh\nkill -TERM $$\n");chmodSync(join(app,"membrane"),0o755);value.files.membrane.sha256=sha256OfFile(join(app,"membrane"));writeFileSync(assetsJsonPath(prepared.root),JSON.stringify(value));assert.throws(()=>checkPackagedMac({repoRoot:root,platform:"mac",appMacosDir:app}),/build-info/);}finally{process.env.PATH=old;}}));
test("manifest assembly composes Mac receipt & rejects malformed input",()=>repo("manifest",({root})=>{const id=engineReleaseIdentity(root),mac=receipt(id),macPath=join(bundleRoot(root,id.commit),"assets.json");mkdirSync(dirname(macPath),{recursive:true});writeFileSync(macPath,JSON.stringify(mac));const macReceipt=readReceipt(macPath),manifest=assembleManifest({existing:{product_version:"0.1.9",source_repo:"membrane",schema_compat:"v1",model:"x"},macReceipt}).manifest;assert.equal(manifest.assets.length,2);assert.deepEqual(manifest.assets,gatherSortedAssets(macReceipt));assert.throws(()=>readReceipt(join(root,"missing.json")));assert.throws(()=>assembleManifest({existing:{},macReceipt}));assert.throws(()=>checkManifest({existing:{...manifest,assets:[...manifest.assets].reverse()},macReceipt}));}));
test("writer commits exact Membrane-repository HEAD evidence without a parent pin",()=>repo("writer",({root})=>{const path=releaseManifestPath(root),bytes=Buffer.from('{"ok":true}\n');atomicWriteManifest({manifestPath:path,manifest:{wrong:true}});assert.throws(()=>checkCommitted({repoRoot:root,manifestPath:path,manifestBytes:bytes}),/Membrane HEAD manifest/);writeFileSync(path,bytes);execFileSync("git",["-C",root,"add","apps/membrane-hub/membrane-hub-release.json"]);execFileSync("git",["-C",root,"commit","-m","manifest","--quiet"]);assert.equal(checkCommitted({repoRoot:root,manifestPath:path,manifestBytes:bytes}),true);}));
