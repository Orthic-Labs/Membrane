# Semantic audit & dogfood remediation

Active scope: validate PR #30, reproduce & repair confirmed findings on main, qualify installed Windows product & affected public boundaries, reconcile recoverable branches, commit & push. No product closure is claimed.

## Merge & evidence validation

- Main/origin baseline & PR #30 merge commit: `010bb010d0c454749dd129e6952731b6417f0d26`. PR source tip: `9420efe144b933fd0b54f56d4b26d15593bb3a3d`; ancestry succeeds & source-tip/merge tree diff is empty.
- All 42 committed-byte artifact hash comparisons pass, including Session A initial/final artifacts & Session B manifests. Checkout line endings are not substituted for committed bytes.
- Requested original baseline: `75c8427437a385ef78c5805624d139741fb5f1b1`. Both artifacts instead name `a4e0a9e02c64e5439cdec289a160b748f5e5686c`, claiming user supersession. Delta: 101 files, 17,162 insertions, 2,060 deletions. Original user correction/transcript & Session A pre-B freeze chronology remain unverified. Self-attestation is not independent provenance.
- Session A matrix has 353 rows; denominator alone does not prove per-atom terminal coverage. Semantic trace validation remains open.
- Session B reports zero executed product cases. Its frozen oracle is a test specification, not an independent completion PASS. Native test labels exceed actual assertions: F01 never interrupts/corrupts storage; H01 never prepares/resolves; I01 never revokes mid-flight; J01 omits lifecycle; L02 never compares adapters. Artifact identity & isolated tray host are not enforced by environment-variable presence. Preserve original artifacts; repaired harness must use separate files.
- Historical main CI run `34106940481` failed runtime-language manifest check. Exact replacement commit `1af581b67707bf860297028a67756f9a20ff17b1` passes public CI run `34161866201`; internal delivery remains driven by local validation & installed checks.

## Remediation ledger

Installed candidate `27dd9442f91c51a666e9dec3b5ff1b350c019619` replaced the prior candidate at `2026-09-07T22:54:07.2370066Z`, exit zero. Installer SHA-256: `6cc72558ba91075f114c75561a8bc209c394796a3c71eb8a9e0c1f0d7707b459`; installed release: `sha256:6f2e6b1fcece3e0f1ca84b444ee9059f768a09a1c76485626f7f801d3ac1e445`. Codex & Claude configuration resolves stable `current`; Claude connects, & CodeRight independently confirmed Codex binding without cwd/env overrides. Silent installation starts no resident process. Hub-off basic, manual graph refresh/query, durable memory, Ledger, Push cross-process restoration & explicit SDK checks pass; Hub-on memory/Ledger/Push/SDK checks pass. Evidence: `%TEMP%/membrane-installed-boundary-27dd9442/`. These probes do not establish Push task/session isolation or shared CodeRight residency.

Automatic watcher acceptance failed its 120-second startup limit: fixture remained queued behind CodeRight & Membrane reconciles. Membrane had 791 pending rows including Git-ignored installer staging copies. Journal processing now uses publication's Git eligibility before payload reads, converting excluded paths into normal deletions so prior indexed pollution is removed. Tests cover ignored reads, unchanged files becoming ignored, rename-in/out & absent deletion. Startup retains two slots: one FIFO slot for bulk progress, one smallest-persisted-file-count slot with FIFO ties; all subscriptions precede initial scheduling. Focused reconciliation/watcher checks passed (56 passed, one platform skip), exclusion-transition regression passed, & all 26 supervisor checks passed. Independent reviews passed both repair slices. Replacement installed automatic MOD/ADD/DELETE acceptance remains required.

Fresh client inspection after silent `1d3936d8` installation found Membrane absent from both Codex & Claude Code MCP configuration. NSIS deactivated old bindings during upgrade, then skipped activation in silent mode. Direct installed calls passing did not prove host integration. Source repair adds installed `activate --bindings-only` & requires it during every NSIS installation, without resident startup. Native invalid-tray integration proves bindings-only succeeds while ordinary Hub activation fails; 65 CLI unit tests & 14 NSIS contract checks pass, with independent source review PASS. Installed replacement remains required. Separate installed Hub-off memory recall, Ledger & cross-process Push restoration pass.

Current installed candidate is `1d3936d8c520e17cbad1a728057d89a2d7767d4c`, release generation `sha256:fdc85de13bea01cf6c56eab841f9ebeda45e0c5fb111dd5588498d86ec647538`. Unsigned installer SHA-256 `f7c9f74b5552041352a292e5202eb7dce59b9b69e4c26d6b4064860837ea488e`; silent installation exited zero at `2026-09-07T22:26:09.084616Z`. Stable `current` build identity & six bundled Blueprint hashes match. Hub-off basic subsystem probes & explicit SDK binding, memory put/get/list/use, diagnostics, wrong-binding refusal & route refusal pass. Initial diagnostics probe omitted required `operation`; corrected probe passes without product changes. Evidence: `%TEMP%/membrane-installed-boundary-1d3936d8/` & `%TEMP%/membrane-installed-boundary-1d3936d8-corrected/explicit-off/`.

Installed manual refresh updates tracked source correctly but final status falsely marks an applied untracked source as removed. Its journal & ledger are converged; status alone rescans tracked files. Source repair makes status use reconciliation's untracked-inclusive source universe. A regression reproduced failure before repair; 51 focused Blueprint tests pass afterward. Read-only source status against unchanged failing installed fixture reports fresh with no pending paths; this is source evidence, not replacement installed qualification.

Adrian's latest boundary requires CodeRight to adopt compatible installer-owned `current`, provision canonical Membrane when genuinely absent, & use canonical update/repair when incompatible. One resident controller must support independent Hub & CodeRight daemon holders; either enables full background services, final holder loss drains, & neither holder gates bounded explicit operations. These lifecycle/provisioning changes remain target architecture. Public bootstrap still targets 0.1.12; RightKit owns bootstrap generation/publication primitives, but stable current publication wiring & CodeRight production provisioning remain open. No public artifact was published.

Tray source now prioritizes installed current/version executable identity over development environment overrides, defaults to installed discovery without implicit checkout fallback, preserves fixed installed port, & replaces an earlier daemon path when installed resolution recovers. Managed Windows RightKit request `7b077036-f757-4bb5-9b56-68d550d43a87` passes all 29 tray tests. Root `pnpm test`, docs checks & regenerated runtime inventory pass; independent repair-slice source review passes. These repairs require replacement installation & installed acceptance; no atom closure is claimed.

Installed `1af581b6` has verified source identity, matching Blueprint payload hashes & successful unsigned installer exit. Hub-off fixture refresh/traversal/generation refusal, six-subsystem probes, Ledger sync/outline, Push cross-process exact restore, durable A1 memory put/get/recall, CodeRight graph search/source resolution & concurrent native graph build pass. Evidence is under `%TEMP%/membrane-installed-boundary-1af581b6`; source logs retain earlier negative fixtures. Initial A0 memory reads correctly refused ineligible data; distinct A1 input qualifies durable recall.

Remaining installed failures: Membrane's own graph query exceeds 30 seconds even after a successful 199.7-second bounded rebuild; journal replay includes excluded native snapshot paths. Automatic MODIFY passes in 12.8 seconds, but ADD fails after 45 seconds: watcher applies its journal while leaving source observation stale. DELETE is not reached. CodeRight independently passes actual Hub-on CLI search, but ordinary Hub-off startup still requires resident HTTP `/health`; existing SDK has no bounded-installed binding/transport. None of these failures is closed by earlier fixture passes.

Current source repair aligns native snapshot exclusions with scans, keeps untracked working-tree additions during bounded refresh, & publishes watcher source observation with its manifest only after exact source/ledger convergence & a second identical byte scan. Read-only queries cannot perform that publication. Source validation passes: 125 broader cases with one platform skip, 11 final focused cases including dirty-to-dirty publication refusal, & 282 root cases with one skip. Independent source recheck returns PASS. Replacement installation remains required; SDK binding repair remains open.

Additive SDK/CLI source now provides `InstalledExplicitClient`, distinct `ExplicitOwnerBindingV1`, & `MemoryBackendClient::from_explicit` over one closed installed `cli explicit-call` request. Existing memory/federation handlers retain ownership; diagnostics reuse native validation & provider lifecycle gates. Binding changes refuse before dispatch; cancellation, malformed replies & transport loss after uncertain effects become non-retryable `CommitUnknown`. Final validation: 40 client tests, 29 focused runtime tests, 282 root tests with one skip, docs/inventory checks & independent source PASS. Consumer integration & replacement installed qualification remain pending. Public Windows CI run `34164241514` rejected `bdb630c6` because inventory generation preceded tracking its new watcher test; current regenerated inventory includes that test plus every new SDK/owner source.

Candidate `8a5f9873` installed successfully with verified source/payload identity. Installed Hub-off fixture refresh, changed-source queries, traversal, document truth, snapshots, generation rejection, Pull, subsystem basics, Ledger sync/outline & Push cross-process exact recovery pass. Initial CodeRight catch-up exceeded its 30-second query deadline with eight events still pending. Automatic watcher MODIFY acceptance failed after 45 seconds: native owner stayed alive but source clocks did not advance while another repository caught up. ADD/DELETE were not reached. Deeper CLI memory write exposed a separate pre-connect timeout refusal on Windows; source repair permits fallback only before connection, with 69 native CLI tests passing (one ignored). Replacement installation of that CLI repair remains pending.

CodeRight's actual installed discovery rejected health's verbatim Windows DOS root against equivalent ordinary stable-root spelling. Client source now compares those spellings only for ordinary DOS components; reserved device names, ambiguous components & different roots remain rejected. Public binding types & installation/startup identity fences remain unchanged. Full client tests passed before additional namespace rejection regression; that focused regression also passes.

Watcher source repair retains a durable pre-base dependent closure & pending journal row, commits each cursor advance with its delta, yields outside write transactions & resumes before same-path coalescing. Overflow reconciliation & ordinary drains serialize in arrival order. Both freshness barriers reject partial repair. Final focused checks pass 85 cases with one platform skip; earlier incremental/equivalence checks pass 89; root tests pass 282 with one skip. A concurrent run exposed duplicate changed-file reads (repaired) & one two-second CLI timeout under load; unchanged Hub build/refresh/lease acceptance passes isolated & in the final sequential run. Independent source review passes. Replacement installer & installed automatic acceptance remain required.

Earlier candidate `101f341b` completed unsigned installation (exit 0), with stable build identity & Blueprint payload hashes verified. Hub-off refresh exposed persistent README convergence mismatch: full scans normalized generated pointers while incremental updates hashed raw bytes. Source repair binds exact bytes consistently & recovers authoritative differences missed by native snapshots. A copy of the failing installed fixture refreshed in 584 ms with `caught_up`, no event gap & no pending domains. Generated-output/reconciliation tests pass 18 cases; independent source review passes. Installed acceptance of this repair remains pending. CodeRight continues independently; older pause statements below record superseded sequencing.

| ID | Finding | State | Acceptance |
|---|---|---|---|
| SEM-001 | Installed Hub-off explicit execution unreachable | Installed `011ed798` passes Hub-off fixture & six-subsystem probes; CodeRight search/source resolution passes after synchronization. CodeRight resumed independently. Hub-on explicit deadline repair & automatic watcher acceptance remain active | Every explicit subsystem operation remains available; only automatic work requires Hub |
| SEM-002 | Unbound audit/decision providers claim complete-empty | Repaired in `c28e4f72`; focused checks & independent source review pass | Missing owner becomes typed omission & required lane remains insufficient |
| SEM-003 | Workspace child deadline resets & serial fanout | Source validation pending | One ingress deadline, bounded concurrency, healthy siblings retained |
| SEM-004 | Push recovery lacks task/session binding | Source-confirmed; open | Same-task restore succeeds; wrong task/session denied |
| SEM-005 | Diagnostics audit persistence silently fails | Source validation pending | Typed caller-visible persistence degradation |
| SEM-006 | Discovery readiness absent | Source validation pending | Stable schemas with truthful operational readiness |
| SEM-007 | Warm Pull composition repeatedly initializes owners | Unmeasured opportunity; open | Production counters & revocation-preserving reuse |
| SEM-008 | Generated budget claims exceed evidence | Source-confirmed; open | Inventory distinguished from qualified behavior |
| EVID-001 | Baseline substitution & independence chronology unproven | Open | Raw authoritative turns & freeze timeline |
| EVID-002 | Dogfood test assertions do not cover frozen oracle | Source-confirmed; open | Every required observable asserted through correct boundary |
| CI-001 | Runtime inventory drift blocks merged main | Inventory regenerated & local checks pass; internal delivery uses local RightKit validation | Manifest/graph checks & native checks pass; CI remains public-release validation |

## Process degradation

Installed stable Legion `contract seal --help` returns `status: incomplete`, `native contract implementation is not connected`; `run open --help` returns top-level usage. No contract receipt is claimed. Direct ambient source/evidence work continues; locked-domain effects remain unexecuted.

## Delivery state

Historical candidate `011ed798b18972836e91b8f9480aa1cde0876fa6`, unsigned Windows installer SHA-256 `cd7e9ce9ebc9766410b497465a57f88392cccf55d284c1b3fccbb078a24c8fa4`. Installation exited zero at `2026-09-07T19:38:02.4767894Z`; stable build-info & five bundled Blueprint file hashes match source. Evidence: `%TEMP%/membrane-installed-boundary-011ed798/`. Hub-off changed-source refresh, traversal, generation refusal, Cortex checkpoints, Ledger, Adapt, Push, diagnostics, evidence-bearing Pull & concurrent native build pass. CodeRight cold synchronization exceeded its 30-second deadline twice; installed one-shot synchronization then completed in 21.780 seconds, followed by successful normal MCP search & generation-pinned source resolution. Earlier timeout evidence is retained.

Hub activation restores all five stable client bindings & reports ready. Hub-on Cortex, Ledger, Adapt, Push & diagnostics probes pass; cold watcher enrollment initializes its graph successfully. Hub-on manual refresh failed at 2.047 seconds: `ExplicitBlueprintTransport` incorrectly capped an entire resident operation at a two-second readiness budget. Source repair forwards the caller deadline, subtracts fallback elapsed time on wire & transport, & preserves no-replay behavior after a resident timeout. Native validation, replacement installer & automatic modify/add/delete acceptance remain pending. CodeRight continues under Adrian's clarified instruction; historical pauses below do not supersede that instruction.

SEM-002 source repair verified & independently reviewed. Local RightKit now builds & tests internal Windows candidates; CI is not an internal installer gate. Unsigned installer 0.1.24 from `052973ccb2b4c5115fcfc6471056fff2cf3a201e` was installed successfully, SHA-256 `f67a79b4456ce9f3a48ec6dda30a0c7ffa2d1761379172d5394f989ebe3b1957`. Stable `current` executable reports that source identity. With Hub off, installed Blueprint fixture refresh/traversal/generation rejection, Cortex cross-process checkpoints, Ledger status, Adapt feedback, Push exact prepare & diagnostics cross-process checks pass. CodeRight independently confirms search & generation-pinned symbol resolution with fresh/caught-up receipts, then resumes its task. Evidence resides in `%TEMP%/membrane-installed-boundary-052973cc/`, including `coderight-independent-exact-{search,symbol,status}.json`.

Candidate `e18079e58c0cc12a8ab8f57d07b688dc85c2d96b` subsequently installed successfully; installer SHA-256 `9d7273a4739ebb6bfa8eb0c7758aaef8c5cd7a776164442d011ebbcd6e77d4d9`. Stable installed identity & Hub-off evidence reside in `%TEMP%/membrane-installed-boundary-e18079e5/`. Concurrent native build now publishes its graph while a peer request completes; ordinary Pull returns an evidence-bearing packet with an empty protected set. Blueprint changed-source refresh/query, generation refusal & the other subsystem probes pass. Activation reports Hub ready & installs stable commands for Codex, Claude, Cursor, Windsurf & Antigravity.

Hub-on installed verification exposed native watcher readiness timeouts & an enrollment revision missed during startup; CodeRight search returns `blueprint_stale`, so its later pause supersedes the earlier resume. Isolated installed native watching succeeds. Source remediation now preserves pending enrollment revisions, allows queued native callbacks before probe expiry & hands resident stale reads through bounded explicit freshness. Installed manual refresh with Hub on/off & automatic watcher modify/add/delete acceptance remain required before CodeRight resumes. No overall completion or branch reconciliation is claimed.

## Urgent internal installer scope correction

Adrian requires latest installed Blueprint before CodeRight resumes; unsigned internal installer explicitly authorized. This task owns local installation & uses existing RightKit packaging through `pnpm --dir apps/membrane-hub run release:build:win:unsigned`. Windows is provisioned; no Mac, signing setup or hosted installer is required. Default native/retained discovery includes Blueprint for clients without custom tools/list metadata. MCP resource transport returns valid contents or JSON-RPC errors. Local native tests & installed acceptance gate internal delivery; see `docs/reference/release/local-windows-development.md`.

User expanded lifecycle correction to all six Membrane subsystems: explicit operations work with Hub off; only automatic/resident processes require Hub. Canonical contract is `docs/architecture/execution-lifecycle-boundary.md`. Blueprint source tests cover initialization, traversal, edit/refresh, build, generation rejection & no watcher. Windows checkout CRLF broke hashed Python provider; LF byte preservation restores existing expected checksum without bypassing validation. Shared explicit execution routes Cortex, Pull, Ledger, Adapt & Push. Diagnostics cross-process state uses canonical event storage; provider children stop after explicit acquisition. Final installed acceptance records actual outcomes separately from source coverage.
