# Membrane delivery reconciliation

**Capture:** `1eb3fcbd` (`Close Adapt Cortex residuals`), 2026-09-08.  This is a source/evidence reconciliation only: source, builds, commits, installs, desktop/iOS work, & soak remain paused.

## Scope accounting

`docs/canon/README.md` records 340 committed atoms, 13 exploratory atoms, 58 competitive-current-best atoms, 282 competitive-pending atoms, & **zero lifecycle-closed atoms**.  Competitive pending is not an implementation backlog; this document schedules only lifecycle evidence, source defects, or already-authorized repair chains needed for committed scope.

Exact committed sets covered below:

| Canon | Exact committed IDs | Excluded exploratory IDs |
|---|---|---|
| Membrane | MEM-001–066 | — |
| Pull | PUL-001–033, PUL-035–037, PUL-039–042 | PUL-034 |
| Push | PSH-001–029 | — |
| Cortex | CTX-001–032, CTX-034–038, CTX-040–041 | CTX-033, CTX-039, CTX-042 |
| Blueprint | BPT-001–021, BPT-023–044, BPT-046–047, BPT-049–071 | BPT-048 |
| Ledger | LDG-001–022, LDG-024–031 | LDG-023 |
| Adapt | ADP-001–036, ADP-038, ADP-040–064, ADP-072–077 | ADP-065–071 |

`PUL-038`, `BPT-022`, `BPT-045`, `ADP-037`, & `ADP-039` have no canon row; they are not omitted requirements.

## Source-state inventory — not acceptance coverage

The following table records source state & ownership only. It is **not** a claim that one residual suite qualifies every atom in a listed range. The replacement acceptance map follows it.

| Atom IDs / requirement | Actual source state | Evidence & installed gap | Exact owner / packet | Concrete predecessor | Next executable action | Acceptance command / path |
|---|---|---|---|---|---|---|
| MEM-001–066 — Membrane controller, protocol, lifecycle, explicit operations | Native protocol/runtime, activation, Hub, tray, installer & client discovery exist; lifecycle source is split across `membrane-client/src/binding.rs`, `membrane/src/activation.rs`, Hub supervisor & tray supervisor. | No lifecycle closure; no shared Hub+CodeRight controller/lease receipt, installed generation digest, or installed no-interpreter proof. | Historical inventory: `audit/remediation/reconciliation/lifecycle-provisioning-inventory.md`; retained native-host packet. Current execution authority is this packet's `lifecycle-controller` & `Targeted lifecycle closure amendment`. | Historical pre-amendment SDK/holder observation; current route is bounded implementation plus its lane acceptance. | On resume, execute current lifecycle-controller allowlist; do not revive superseded host-packet instruction text. | `rightkit cargo test --manifest-path engine/Cargo.toml --locked --target x86_64-pc-windows-msvc -p membrane-runtime --test residency_holders`; installed matrix remains lane acceptance. |
| MEM-040; PSH-011–012, PSH-016–017 — authentic H4/H6/H8/H9/H10 shapes & strict H8 delivery | `membrane-protocol/src/host_observation.rs:543` defines H8; `push.rs:212` requires host-validated observation; native MCP forwards H8. | Historical source observation: prior manual H8 producer/transport route was unbound; wire-shape test alone was not producer proof. | Current governing CodeRight correction contract & retained external owner packets bind H8 producer/consumer work; Membrane has no separate manual-contract worker. | Historical missing-contract directive is superseded. | Execute only effective external producer/consumer packet route on resume; reject fabricated capacity. | Focused protocol/host producer test plus installed native-MCP request showing valid H8, typed absence, & one final host attachment; paths above. |
| PUL-001–003 — requirements catalog, capability publication, acknowledgement | `membrane-federation/src/requirements.rs`, runtime catalog/freshness/serve bytes & `pull_acknowledgement.rs` are source-complete in working tree. | Requirements 7/7 PASS, acknowledgement 3/3 PASS, post-merge 6/6 PASS. Catalog remains 21/22: parent repository grant identity mismatch; this is not installed proof. | `pull-final-requirements.dispatch.json`; `pull-final-publication-integration.dispatch.json`; Pull owner packet receipts. | User disposition after one permitted catalog repair/recheck. | Keep chain stopped; either accept typed parent-grant correction scope or explicitly replace source premise. | `rightkit cargo test --manifest-path engine/Cargo.toml -p membrane-runtime --test pull_catalog --locked`; exact failing catalog case first. |
| PUL-004–018 — acquisition/providers; PUL-020–023,025–030,033 — fusion/corrective/budget; PUL-035–036,040–042 — placement/native delivery | Existing runtime/federation implementation; fresh residual qualification source is frozen at `engine/crates/membrane-runtime/tests/pull_residual_qualification.rs`. | No PPL Rust qualification run; source alone cannot establish lifecycle. H8 matters only later for installed full-host proof, not this focused check. | `ppl-committed-residuals.dispatch.json` / receipt `69eb0e0e8bc6547baa0f4b7e1c8a54514415eb4a728761ca2a43ef43930ab6df`; inventory `reconciliation/ppl-committed-residuals-inventory.md`. | No H8 or residency prerequisite; preserve PUL-001–003 stop as a distinct catalog chain. | Run only frozen PPL test; return any semantic failure to its exact implementation owner. | `rightkit cargo test --manifest-path engine/Cargo.toml -p membrane-runtime --test pull_residual_qualification --locked`. |
| PUL-019,024,031–032 — coverage, minimum faithful evidence, receipt/outcome observations; PUL-037,039 — acknowledgement-bound suppression/prefix reuse | Requirements, journey map, catalog, delivery acknowledgement, `delivery_state.rs`, `cache_prefix.rs`, `pull_post_merge_acceptance.rs`, `adapt_observations.rs` & `serve.rs` contain final-path source work. | Requirements 7/7, acknowledgement 3/3 & post-merge 6/6 passed. Catalog 21/22 remains independently stopped. Existing results do not prove released-host delivery, H9/H10 producer, or authentic H8 full-host behavior. | `pull-final-requirements.dispatch.json`; `pull-final-publication-integration.dispatch.json`; owners `pull-publication-owner`, `pull-ack-bridge-owner`, `pull-suppression-owner`. | Pull catalog disposition only for catalog closure; full-host qualification follows public `native_route_response` wiring. Focused suppression checks are independent of H8/manual work. | Preserve exact acknowledgement suppression; re-run focused contracts, then run public admission→Push→fence→pending-publication→next-request acknowledgement acceptance. | `rightkit cargo test --manifest-path engine/Cargo.toml --locked --target x86_64-pc-windows-msvc -p membrane-federation --test requirements_contract`; `rightkit cargo test --manifest-path engine/Cargo.toml --locked --target x86_64-pc-windows-msvc -p membrane-runtime --test pull_acknowledgement --test pull_catalog --test pull_post_merge_acceptance`; missing acceptance: packet-owned public native-route/fence test. |
| PSH-001–029 — capture, reduction, recovery, governed delivery | Source exists; frozen recovery files remain under current integrator. Fresh residual test is frozen at `engine/crates/membrane-runtime/tests/push_residual_qualification.rs`. | Old canon/provenance claims are historical focused proof only. Fresh Push/Ledger pre-PPL receipt is 18/18 PASS, but no current PPL Rust run or installed actual-host H8 proof exists. | PPL packet/inventory; frozen recovery owner named there. | No H8/manual or shared-residency prerequisite for PPL. Any PPL failure returns to recovery owner. | Run PPL without modifying frozen recovery paths; separately validate real estimator/provenance via supported host after manual producer exists. | `rightkit cargo test --manifest-path engine/Cargo.toml -p membrane-runtime --test push_residual_qualification --locked`; installed H8 proof is a separate missing acceptance. |
| CTX-001–032,034–038,040–041 — durable Cortex truth, admission, recall, lifecycle | Cortex residual repair is committed in `1eb3fcbd`; runtime Adapt/Cortex observation source is present. | Multiwriter 2/2 PASS & remediation 19/19 PASS; Oracle PASS covered residual packet only. No installed same-store/holder-loss/rebind proof; no lifecycle closure for remaining Cortex atoms. | `adapt-cortex-committed-residuals.dispatch.json`; commit `1eb3fcbd`. | Preserve commit; exact `CTX-Q` cases/owners are now listed in Acceptance map; shared controller remains predecessor for holder continuity. | No new Cortex edit under pause. Qualify affected tests only after crate closure. | `rightkit cargo test --manifest-path D:/Claude/membrane/engine/Cargo.toml --locked --target x86_64-pc-windows-msvc -p membrane-runtime --test adapt_multiwriter_convergence`; missing acceptance: installed same-store/holder-loss/rebind case from `reconciliation/lifecycle-provisioning-inventory.md`. |
| BPT-001–021,023–044,046–047,049–071 — native graph/store, query, doc truth, Phase 2, export, findings, service | Native crate now carries graph/store/API/doc-truth/phase2/query/export/findings/service files in working tree. Graph repair froze at `14499bf6…`; findings repair froze at `7d4f…`; no repair is active while paused. | Initial native test stopped at `findings.rs` predicate compile error. Semantic review also blocked multiline ESM/TS closure, binding/delta coverage, bounds/cancellation & focused fixtures. No native installed Blueprint proof. | `blueprint-native-foundations*.dispatch.json`, `blueprint-native-findings-export.dispatch.json`, native closure brief `D:/Downloads/MEMBRANE-BLUEPRINT-NATIVE-RUST-CLOSURE.md`. | Resume graph/findings owner only, then focused qualification; engine routing remains separately stopped. | Preserve frozen working-tree source; first action after resume is exact owner review/qualification, not a new repair wave. | `rightkit cargo test --manifest-path engine/Cargo.toml -p membrane-blueprint --test graph --test findings --test export --locked`. |
| BPT query/engine advertised-operation subsets within prior row | Query semantics have two Oracle BLOCKs; engine routing for `Operation::Export` plus six findings operations is separately retry-stopped. Source must not route around those stops. | Unsupported-operation behavior is not parity evidence; source-complete export/findings cannot become reachable until owner resumes. | `blueprint-native-query.dispatch.json`; engine packet/owner; findings/export packet explicitly queues dependency. | User disposition for each stopped repair chain. | Do not retry until resumed; then one bounded owner repair & Oracle recheck. | Exact packet-owned query/engine test paths; no broad Blueprint audit. |
| LDG-001–013,015–022,024–027,030 — source/index/resolve; LDG-014,029,031 — diagnostics | Existing Ledger implementation; fresh residual test is frozen. Diagnostics files remain reserved to current integrator. | Old Ledger claims must not be read as fresh proof. Fresh 18/18 combined Push/Ledger receipt exists; PPL Rust qualification & installed read/resolve receipt do not. | PPL inventory; frozen diagnostics owner. | PPL test first; diagnostic defects only after test attribution. | Run frozen PPL test; preserve diagnostic ownership. | `rightkit cargo test --manifest-path engine/Cargo.toml -p membrane-runtime --test ledger_residual_qualification --locked`. |
| LDG-028 — public bounded PlainText/JSON/HTML/PDF/DOCX ingestion | New CLI, native MCP schema/bridge, service/doc-spine source & focused test are frozen; `document_conversion.rs` was intentionally unchanged historically. | JS MCP test PASS 1/1. Rust focused test, snapshot/live proof, installed CLI/MCP proof, converter drift/loss evidence remain absent. | Historical `ppl-committed-residuals.dispatch.json`; active `ledger-content-repair` lane. | Historical PPL evidence remains checkpoint-only. Active owner first preserves predecessor bytes, then repairs `ledger/document_conversion.rs` & expected-content fixture matrix for styled runs, tables, tabs, entity decoding, identifiers, & URLs. | Run converter repair plus expected-content Rust qualification; then exercise installed CLI & MCP with each allowed type plus denial/size/snapshot cases. | `rightkit cargo test --manifest-path engine/Cargo.toml -p membrane-runtime --test ledger_public_format_ingestion --locked`; `pnpm exec node --test mcp/server.test.mjs`; missing acceptance: expected-content fixtures & installed CLI/MCP matrix. |
| ADP-001–036,038,040–064,072–077 — governed proposal/adaptation | Exact residual slice ADP-019, ADP-022–025, ADP-041 is in commit `1eb3fcbd`; remaining committed rows retain existing source/canon mechanisms. | Residual tests passed, but canon still records zero lifecycle closures. No installed adaptation receipt with durable Cortex continuity. | `adapt-cortex-committed-residuals.dispatch.json`; commit `1eb3fcbd`. | Installed controller/store continuity. | Preserve commit; execute lifecycle acceptance against installed current after host lifecycle is available; H8 is not a source-test gate. | `rightkit cargo test --manifest-path D:/Claude/membrane/engine/Cargo.toml --locked --target x86_64-pc-windows-msvc -p membrane-runtime --test adapt_multiwriter_convergence`; `rightkit cargo test --manifest-path D:/Claude/membrane/engine/Cargo.toml --locked --target x86_64-pc-windows-msvc -p membrane-adapt --test remediation_lifecycle_closure`. |
| Cross-cutting installed native closure for every committed atom | Package/source edits exist across Hub, tray, runtime, native Blueprint & gate scripts. | No fresh immutable installer digest, no current installed package, no Node/Python-absent PATH receipt, no process-tree, Hub-off, CodeRight, upgrade/rollback, SBOM, or native-only seal evidence. Non-soak only. | `native-blueprint-package-qualification.dispatch.json`; `reconciliation/audit-closure-inventory.md`; `reconciliation/packaging-cleanup-inventory.md`. | All source commits; Blueprint engine/host lifecycle closure; installed artifact creation. | Build exact approved package, then run non-soak installed matrix; never infer from local source or old seal. | `pnpm exec node scripts/ci/check-runtime-language-manifest.mjs`; `pnpm exec node scripts/ci/check-invocation-graph.mjs`; `pnpm exec node scripts/ci/check-lifecycle-conformance.mjs`; `pnpm exec node scripts/qualification/issue-native-only-seal.mjs`; missing acceptance: installed current-root package/SBOM/process-tree/upgrade-rollback receipts. |

## Stopped chains & discrepancies

1. Pull catalog: second permitted repair/recheck still fails one parent grant-identity case (21/22).  Stop remains until scope disposition.
2. Blueprint query: two Oracle BLOCKs.  Blueprint engine routing is an independent stopped chain; `Export` plus six findings operations must later be wired by its owner.
3. SDK: `CanonicalBinding` remains forgeable through public fields in `engine/crates/membrane-client/src/residency.rs:43-56`; joint contract digest `8884…` exists outside this repository.  No retry authorized.
4. Native host embedding: final Oracle BLOCK found no installed `membrane.exe` headless-start/canonical wait/reconnect behavior in CodeRight companion.  Amendment packet identifies handoffs, but working-tree bytes are not a preserved patch; owner resumption must preserve them first.
5. H8: historical source-state note: schema/consumer existed before current governing producer/consumer correction. It remains an installed-proof requirement, not an instruction to create a separate manual contract.
6. Ledger: historical claims & 18/18 pre-PPL result are distinct from fresh PPL Rust qualification, public ingestion, & installed proof.
7. Lifecycle: all 340 committed atoms remain lifecycle-open by canon accounting; no inference from commit, source, or focused test is permitted.
8. Native host packet mismatch: `native-host-embedding-amendment.dispatch.json` embeds service in `membrane.exe`; reviewed bootstrap design uses `membrane.exe` bootstrap plus `membrane-daemon` controller. Its seven-file allowlist cannot implement that design.
9. Provisioning: `membrane-client` maps `NotFound` to `ProvisionPackaged`, but CodeRight host bootstrap has no canonical installer → rediscovery → identity/readiness effect. `NotFound` must not degrade to unavailable.
10. Platform transport: `native_diagnostics_pipe.rs` has no non-Windows resident implementation; no macOS installed headless package/peer-authenticated transport owner is identified. Windows-only proof cannot close a cross-platform claim.
11. CodeRight compaction integration needs a cross-product packet: `compactor_breaker.rs` remains failure classification only; `api_sessions/approvals.rs` is manual entry; `api_tests_part_31.rs` owns route coverage; `engine_session_controls.rs` retains live operation state & local commit ownership. This is separate from Membrane manual-task/H8 contracts.

## Acceptance map — replaces range-level qualification claims

Every row names real assertions, boundary, owner, & current evidence. A row marked **unmapped** is an executable planning defect, not evidence of absence of source.

| Capability IDs / exact obligation | Actual case / assertion required | Runtime or host boundary | Exact owner / files | Retained evidence | Missing evidence / next executable action |
|---|---|---|---|---|---|
| MEM-Q001 | One `membrane_context` request exercises MCP executor → federation → planner; assert one bounded planner, typed omission & receipt, without bypassing Pull/Push/Cortex/Blueprint/Ledger/Adapt gates. | `mcp_executor.rs` / federation planner; supported host. | Planner owner; existing MCP executor/federation/planner tests (`MEM-I001`). | No full canonical planner qualification. | Missing end-to-end source case in current federation/MCP test owner. |
| MEM-Q002–004 | Q002 versioned protocol request/response survives merge & Pull CLI; Q003 release identity/build-info verifier refuses unsealed identity; Q004 authorization denies cross-root/ungranted request before executor. | Protocol → federation/CLI; release CLI; MCP executor. | `membrane-protocol/src/types.rs`; `release_identity.rs`; `runtime/src/authorization.rs`, `mcp_executor.rs`. | MEM-004 historical 44-pass authorization corpus; Q002/Q003 remain PENDING. | Add Q002 merge+CLI round-trip & Q003 unsealed/mismatched-release cases; rerun existing Q004 corpus after runtime closure. |
| MEM-Q005,022–024 | Q005 authenticated revoke invalidates catalog candidate; Q022 working context retains task/session fence; Q023 scratchpad operations remain scoped; Q024 feedback/effectiveness rejects unresolved/reused receipt identity. | Runtime catalog/serve; session MCP/Hub; Pull/Cortex/Adapt feedback. | `catalog.rs`, federation `scope.rs`, `working_context.rs`, `scratchpad.rs`, feedback/runtime owners. | Source is partial; no released consumer proof. | Add revoke→serve, session-scope, scratchpad operation, & invalid receipt-identity cases in owners' existing modules. |
| MEM-Q006–009 | **Excluded desktop/iOS work.** Do not schedule desktop qualification. Non-UI host scope retains only no-spawn/explicit-operation semantics in Q008–009. | Tray/macOS desktop excluded; CLI/MCP/CodeRight explicit host remains in scope. | Tray/macOS owners excluded; runtime service/CLI/serve/MCP owner. | Historical desktop/no-spawn evidence is stale. | Missing non-UI case: explicit CLI/MCP/CodeRight operation works without UI spawn; desktop cases remain out of scope. |
| MEM-Q010–012 | Q010 service lifecycle keeps stable service identity; Q011 JSON-RPC framing rejects malformed/invalid modes; Q012 loopback HTTP rejects non-loopback, invalid request proof & rebinding while matching stdio result. | Runtime service, MCP JSON-RPC, loopback HTTP. | `serve.rs`, `service.rs`; `membrane-mcp/src/jsonrpc.rs`; `runtime/src/mcp_http.rs`. | Historical focused Q012 9-pass & source tests; no current release qualification. | Re-run named service/JSON-RPC/`mcp_http::tests` cases after runtime freeze; add installed loopback-to-stdio parity case. |
| MEM-Q013–015 | Q013 exact 17-tool default/optional groups + token/turn/success/latency cold-warm matrix; Q014 bounded resource list/read grant/no-body denial; Q015 prompt list/get with declared-operation authority only. | Native MCP client/model. | `membrane-mcp/src/tools.rs`, `resources.rs`, `prompts.rs`, `jsonrpc.rs`. | Historical Q014/Q015 resource/prompt corpus; tool descriptions still generic. | Add tool-surface/description utility & cold-warm host matrix; rerun resource/prompt denial cases. |
| MEM-Q016–021 | Q016 Hub/tray/CodeRight holder semantics; Q017 plane boundary denies forbidden stateful crossing; Q018 manifest mismatch/schema rejection; Q019 installation identity API; Q020 development/path isolation; Q021 signed candidate admission. | Installed `current`, explicit client, Hub/tray/CodeRight. | Runtime `service.rs`/`installation_manifest.rs`; `planes.rs`; client binding; activation/installer owners. | Q018 historical manifest tests only; all are lifecycle-open. | Controller/provision contract first; then cases for each state, manifest/identity/path, signed candidate. Desktop UI cases stay excluded. |
| MEM-Q025–028,031–032 | Hub capability/snapshot/overview/sources/fleet & thresholded notification assertion. | Hub/tray UI excluded; source producers still must preserve typed snapshots & no notification without threshold. | `hub_inputs.rs`, `notifications.rs`, Hub producers. | Historical/source-only evidence. | No desktop acceptance scheduled; add source-level snapshot schema/threshold cases only if source changes. |
| MEM-Q029–030 | Q029 emits exact/lower-bound diagnostic counts incl unavailable/not-evaluated; Q030 joins packet, host, event & outcome without invented association. | Runtime memory sentinel/delivery trace → Hub/serve. | `memory_sentinel_producer.rs`, `memory_sentinel_view.rs`, `hub_inputs.rs`; `delivery_trace_view.rs`, `serve.rs`. | Both sources explicitly partial. | Missing cases: every count dimension plus unavailable; valid/ambiguous/missing join cases. |
| MEM-Q033–039 | Q033 workspace root/worktree identity; Q034 epoch/fence purity; Q035 diagnostic availability/status; Q036 fence executor; Q037 provider status; Q038 baseline/delta; Q039 doctor/bundle redaction/read-only schema. | Runtime diagnostic MCP/CLI. | `live_diagnostics_service.rs`, `mcp_executor.rs`, `doctor.rs`, `diagnostic_bundle.rs`, CLI. | Historical Q033/034/039 focused cases; others pending. | Rerun retained cases; add Q035–038 provider/fence/baseline/delta cases through native MCP + CLI parity. |
| MEM-Q041–042 | Q041 ingress rejects incomplete caller transport; Q042 receipt has per-requirement envelope, omissions, accounting & recovery, preserving host/operator/evaluation joins. | Host observation ingress → runtime receipts. | `host_observation.rs`, `serve.rs`; receipt/runtime-receipt/protocol owners. | H9/H10 producers absent; envelope incomplete. | Missing typed H4/H6/H8/H9/H10 caller cases & receipt-envelope/fence cases. |
| MEM-Q043–044 | Q043 deadline/scheduler failure/cancellation latency receipts; Q044 maintenance executor/background/store operation with no unowned semantic input. | Federation/runtime background. | Federation scheduler; maintenance executor/background/store owners. | Source partial; installed percentiles absent. | Add deterministic deadline/cancel/fault tests & maintenance input/refusal cases. |
| MEM-Q045–051,053–054 | Q045 Claude, Q046 Codex, Q047 CodeRight, Q048 Cursor/Windsurf, Q049 generic MCP capability matrices; Q050 SDK compatibility; Q051 signed team-policy ingress; Q053 semantic review producer; Q054 CodeRight startup/no embedded fallback. | Installed client/SDK/host surfaces. | Exact client owners in `MEM-I045…MEM-I054`; CodeRight external owner for Q047/Q054. | No current installed client proof; Q051 ingress unwired. | Create per-host capability matrix only after opaque SDK/provision/transport contract; add Q051 signed ingress, Q053 producer/refusal & Q054 cold-start binding cases. |
| MEM-Q052 | Admission policy/permit gate accepts eligible review request, refuses denied/expired/over-budget request, & records no scheduling/single-flight/cancellation/retry claim (those are Q061–066). | `background_review.rs` → `bin/membrane-daemon.rs`. | `engine/crates/membrane-runtime/src/background_review.rs:315-367,1688-1735`; daemon owner. | Source has permit gating only. | Missing focused permit positive/denial/expiry/budget cases; no H5 scheduler assertion here. |
| MEM-Q055–060 | Tray-owned supervision/restart; drain leaves no daemon; signed activation; failed activation rollback/previous retention; governed uninstall; unauthorized downgrade refusal. | Installed tray/Hub/installer `current`. | Tray owner `apps/membrane-tray-windows/src/supervisor.rs`; installer owner `apps/membrane-hub/src-tauri/windows/installer.nsi`; activation owner `membrane/src/activation.rs`. | Installer source exists; no current installed acceptance. | Missing installed cases: process-tree restart/drain; signed update; failed-cutover rollback; uninstall; downgrade denial. |
| MEM-Q061–066 | Two job kinds under one single-flight scheduler; cancellation/shutdown; bounded retry terminal status; idle/activity budget; zero-token maintenance vs bounded semantic review; H5 schedule observations. | Daemon runtime/background review. | `membrane-runtime/src/bin/membrane-daemon.rs`, `background_review*.rs`, runtime service owner. | No release-bound evidence. | Add job-kind matrix using a deterministic provider, controlled cancellation/retry/budget fixtures & event receipt assertions. |
| PUL-001–018,020–023,025–030,033,035–036,040–042 | Provider class, concurrency/cancellation, normalization, corrective pass, deterministic fusion, budget/fence, multi-root identity, pre-emission refusal. | Runtime federation; later installed native MCP. | PPL Pull owner, `pull_residual_qualification.rs`; federation owner. | Source qualification fixture frozen; no Rust PPL run. | Run only after runtime/federation crate closure; then map each PUL-Q assertion not exercised by fixture. |
| PUL-019,024,031–032,037,039 | Per-dimension coverage; minimum evidence before depth; candidate journey & omission receipt; explicit delivery/outcome strength; exact acknowledged suppression; restart/compaction/response-loss/budget-drop restoration. | Public `native_route_response` → Push → fence → H9/H10 next request. | `pull-final-requirements.dispatch.json`; `pull-final-publication-integration.dispatch.json`; current host owner for `serve.rs`; publication/ack/suppression owners for their transferred files. | Requirements 7/7, acknowledgement 3/3, post-merge 6/6. | Public native-route/fence fixture is missing; catalog 21/22 is a separate stopped close. Full-host proof also needs real H8 producer. |
| PSH-001–002,005–010,018,020–021 | Normal CLI, HTTP, MCP & SDK parity; both streams/binary/CRLF; corrupt metadata; raw-first/concurrent publish; no source rerank; argv/path/env/cancel/one-spawn refusal. | Released CLI/tool, HTTP, native MCP, installed SDK. | Push recovery/adapter owners; `push_residual_qualification.rs`; public transport owners. | Fresh residual source; older focused tests are historical only. | Create explicit released-host transport matrix; PPL helper test cannot prove parity. |
| PSH-003–004,006–008,011–017,019,026–027 | Parser fixtures across Python/TS/TSX/Rust; protected fidelity; actual estimator; real host rendering; no-sink/refusal; final-wire emits selected content once, no unselected leakage, receipt/delivery join. | Reducer → Pull final wire → actual host. | Push/federation/publication owners; H8 producer owner for 011–012. | No fresh end-to-end final-wire evidence. | Preserve source; add exact final-wire assertions after H8 contract & public Pull route are frozen. |
| PSH-022–025,028–029 | Opaque-handle rejection; controlled expiry/restart/revocation; restore/list negotiation; quota/full disk/cancellation/simultaneous restore. | Released resolver CLI/HTTP/MCP/SDK. | Frozen recovery owner; `push_end_to_end.rs`, `push_hardening.rs`. | Historical focused recovery cases; no released parity evidence. | Map each PSH-Q022–029 case to controlled-clock/host test before package qualification. |
| CTX-Q001–003,005–016,018–025,028–032,034 | Each is canon-required legacy source claim: replay existing focused assertion through exact current Cortex store/runtime consumer, with source identity, authority, temporal/lifecycle & typed-refusal check retained. | Cortex store → runtime explicit/Pull consumer. | Exact source owner is corresponding `CTX-I` row in `docs/canon/cortex.md`; no Adapt worker owns these. | No current released receipt; `1eb3fcbd` does not cover them. | Missing test specification: one consumer-bound regression per `CTX-Q` in its `CTX-I` test module; plan test path from implementation row before edits. |
| CTX-Q018 | Checkpoint save/list/restart/load/done/list-empty; wrong scope/install, expiry/collision/size deny; checkpoint never enters recall. | Checkpoint → MCP executor/CLI. | `cortex/checkpoint.rs:192-270`; `mcp_executor.rs:739-787`; `cli.rs:4134-4193`; `cortex/tests/checkpoints.rs`, `checkpoint_cli_lineage.rs`, `native_authorization.rs`. | Source tests exist. | Missing installed-current save→restart→load sequence & typed denial matrix. |
| CTX-Q019 | Promotion acknowledges one durable pending proposal, only once, with restart/idempotence, scope denial & no recall. | Lifecycle → MCP executor/CLI. | `cortex_lifecycle.rs:986-1008,1563`; `mcp_executor.rs:1509-1511`; `cli.rs:4195-4212`. | Source lifecycle test exists. | Missing installed MCP/CLI restart/pending-only/no-recall cases. |
| CTX-Q020 | Propose/status preserves pending state across restart, reports no recall; zero-row/scope denials are typed. | MCP executor → lifecycle. | `mcp_executor.rs:1471-1481`; `cortex_lifecycle.rs:241-286`; `cortex_remaining_acceptance.rs:885,919`; `native_authorization.rs:686,765,879`. | Source acceptance/authorization cases exist. | Missing installed status/propose restart & zero-row denial matrix. |
| CTX-Q021 | Trust/review signed MCP decision, stable-current identity, kill/restart exact-once; forged/revoked/binding/expired refusal. | Lifecycle/journal → resident serve. | `cortex_lifecycle.rs:118-150,470-606,1209-1256`; `serve.rs:5926`; `native_authorization.rs:686,808,853,879`. | Source lifecycle/public executor cases exist. | Missing installed stable-current trust/review + restart/denial cases. |
| CTX-Q022 | Curate/list/restore across restart; duplicate/date/low-value/restricted corpus; deterministic replay. | Dream → store → CLI/serve. | `dream.rs:76-304,326,359,439`; `store.rs:3108-3427,15969,16037,16062,16119,16641`; `cli.rs:4466-4478`; `serve.rs:4893-4925,8838`. | Source tests exist. | Missing installed curate/list/restore restart corpus matrix. |
| CTX-Q004,017,035–038,040–041 | Q004 identity/provenance without guessed metadata; Q017 closed relation vocabulary/endpoints/provenance; Q035 durable utility admission; Q036–037 focused exact behavior; Q038 exact/lower-bound empty/capped/filtered/stale/unavailable causes across CLI/service/Pull; Q040 recipe digest/tolerance/fallback; Q041 suppression across restart/reindex/backup/restore with signed reversal. | Cortex store, runtime, CLI/service/Pull. | Cortex implementation owners; `cortex_lifecycle.rs` only for ADP residual integration. | No current acceptance receipt. | Add named cases for each enumerated Q to owning Cortex modules; holder-loss same-store case belongs jointly to lifecycle owner after controller contract. |
| ADP-019,022–025,041 | Competing multiwriter permutations/retry/restart/response-loss/crash conflict retention; compatible tuple, recurrence/reopen, attribution, applicability. | `membrane-adapt` + runtime admission boundary. | Commit `1eb3fcbd`; `adapt_multiwriter_convergence.rs`; `remediation_lifecycle_closure.rs`. | 2/2 & 19/19 focused PASS. | Re-run only after affected crate freeze; add installed observation/store continuity separately. |
| ADP-Q001–010 | Transcript/Taste/scope/authority: focused source cases must show bounded review, authority refusal, scoped preference, & no self-authorized durable truth. | Adapt daemon/control surface. | `ADP-I001…I010` owners in `docs/canon/adapt.md`; Adapt runtime modules. | All PENDING/PENDING/LOCAL; no RELEASED test. | Missing installed-current daemon/control case per Q, with canonical store, receipt & authority/refusal assertion. |
| ADP-Q011–016 | Rejected alternative & `none_recorded`; policy/version invalidation; CLI/HTTP/MCP/named-host standing-core & selection; loaded acknowledgement/outcome joins; user controls/delete/export/import. | Adapt daemon/MCP/host. | `ADP-I011…I016`; host integration owner for actual loaded/acknowledged path. | No released qualification. | Missing real daemon/MCP/named-host cases incl restart, exact join & deletion propagation. |
| ADP-Q017–021 | Detector catalog positives/hard negatives; replay/out-of-order episodes; proposal-only discovery; observed-vs-inferred outcomes. | Insights consumers. | `ADP-I017…I021`, `membrane-adapt` insight owners. | No released qualification. | Missing controlled event/replay cases & real observation-consumer assertion. |
| ADP-Q022–029 | Proposal kind/effect/target; causal attribution; applicability; recurrence; seals; atomic apply; grouping; canonical Cortex admission. | Remediation/admission daemon. | `ADP-I022…I029`; residual commit owns only Q022–025; Cortex admission owner Q029. | `1eb3fcbd` residual proof for Q022–025 only; all Q lifecycle PENDING. | Missing installed daemon cases for exact lineage, host outcome, restart/concurrency & canonical-store identity. |
| ADP-Q030–035 | Host ingress H4/client/model/cursor; trial input; cost; case lineage; live review; semantic provider/sink. | Named CodeRight host → Adapt daemon. | `ADP-I030…I035`; external CodeRight host-trial/H4 owner `01a07e9f-1f88-7861-90a8-fc8ef04cb4b4`. | No released qualification. | Missing H4 producer/incremental cursor/restart/gap, host trial, cost & provider failure/no-op cases. |
| ADP-Q036,038,040–042 | Activation/asset/loaded-representation/evaluator/outcome joins; detector coverage; coverage-to-outcome join; daemon multiwriter; read-only lineage. | Adapt daemon + runtime observation joins. | `ADP-I036/I038/I040/I041/I042`; Q041 residual commit has narrow focused proof. | Q041 2/2 focused PASS; no released daemon convergence. | Missing installed canonical-store restart/replay/join cases; Q041 requires retained conflicts beyond permutation. |
| ADP-Q043–064 | Exact efficiency/reporting detectors in `engine/crates/membrane-runtime/src/adapt_efficiency.rs`; per-detector positive/hard-negative & receipt-state assertions. | Runtime H4 observation → Adapt efficiency consumer. | `adapt_efficiency.rs` owner; `ADP-I043…I064`. | Source-unit assertions exist at `adapt_efficiency.rs:1047` atomic catalog, `:1058` duplicate-tool unavailable→ran, & `:1074` typed contract-gap; these are source-only proof. | Missing RELEASED external per-detector H4 qualification; CodeRight-only fields are required for Q043–052,055–056,060–061,063–064. |
| ADP-Q072–077 | Q072 clarification/resume/cancel/stale-target; Q073 one eligible target winner & merge/supersede/retry lock; Q074 MCP explanation/refusal; Q075 daemon status disconnected/stopped/missing-sink/stalled-cursor; Q076 immutable trial/no-improvement; Q077 rollout/rollback with explicit host authority. | Adapt daemon/MCP/named host. | `ADP-I072…I077`, Adapt owner + external host owner. | No source or released qualification identified. | Six explicit missing installed tests: `clarification`, `winner_concurrency`, `mcp_explanation`, `live_status`, `trial_controls`, `transition_rollback`. |

### Named Adapt/Cortex source seeds

| Q IDs | Existing named assertions to retain | Exact missing release-bound case |
|---|---|---|
| ADP-Q001–010 | `host_adapters::{named_hosts_filter_non_user_origins,opencode_adapter_emits_typed_user_event,copilot_parser_redacts_secrets_and_dedupes_long_reinjected_turns}`; `taste_contracts::{model_output_mistagged_as_user_turn_is_refused,security_weakening_beats_restrictive_surface_form,unknown_scope_dimension_fails_closed,episodic_fact_is_rejected_as_taste_class}`; `gates_and_boundaries::model_proposal_carries_no_authority_fields`. | Installed named-host provenance/span/act/scope/precedence matrix. |
| ADP-Q011–016 | `taste_contracts::{correction_counterfactual_is_source_bound_and_does_not_claim_failure,private_reasoning_is_never_retained_as_rejected_alternative,missing_rejected_option_is_explicit_none_recorded,lifecycle_transition_requires_receipt_and_valid_path}`; `proposal_state_machine::{sealed_plan_persists_propose_approve_commit_with_guards,expiry_is_persisted_and_stale_writers_fail_closed}`; `gates_and_boundaries::context_delivery_requires_explicit_planner_inclusion`; `remediation_outcomes_cost::exposure_adjusts_outcome_class`; `adapt_installed::installed_explicit_adapt_uses_canonical_store_with_hub_off`. | Real CLI/HTTP/MCP/host selection/delivery/acknowledgement/outcome join plus controls/delete/export. |
| ADP-Q017–021 | `insights_detector_benchmark::{corpus_loads_and_has_expected_shape,severity_matches_documented_contract_on_true_positives,non_gap_cases_match_ground_truth_exactly,known_gap_cases_reproduce_exactly_as_documented}`; `insights_pipeline::{guards_suppress_quoted_tool_hypothetical_spans,recurrence_is_deterministic_and_cross_session,single_episode_never_forms_an_issue,illegal_issue_transitions_are_refused}`. | Installed host catalog, replay/partial/out-of-order, cluster & observed-vs-inferred outcome matrix. |
| ADP-Q022–029 | `remediation_lifecycle_closure::{explicit_kind_is_independent_and_legacy_is_marked,bound_attribution_and_digest_gate_variant_consumption,insufficient_outcome_does_not_reopen_or_invent_count}`; `remediation_current_contract::current_contract_carries_versioned_effect_and_target`; `taste_contracts::{seal_detects_payload_mutation,envelope_mutations_never_touch_sealed_semantics}`; `canonical_and_ids::{preference_ids_resist_category_boundary_collision,ids_are_deterministic_across_calls}`; `adapt_proposal_service::{production_service_replays_exact_target_version_without_second_mutation,production_service_rejects_competing_variant_for_same_target_version}`. | Installed tuple/lineage/denominator/concurrency/canonical-store matrix. |
| ADP-Q030–035 | `adapt_resident::{live_status_distinguishes_missing_producer_from_empty_work,windows_resume_and_replay_without_duplicate_coverage,missing_result_is_unknown_and_honest_failure_report_is_not_a_completion,passing_retry_clears_failed_verification}`; `closed_loop_qualification::closed_loop_never_fabricates_missing_host_fields`. | CodeRight H4 cursor/trial/case/control/provider failure matrix. |
| ADP-Q036,038,040–042 | `procedural_effectiveness::{missing_joinable_evaluation_keeps_verdict_unavailable,complete_h4_h6_join_emits_verdict_and_preserves_evidence}`; `cli_surface_and_convergence::multiwriter_convergence_is_permutation_invariant`; runtime `adapt_multiwriter_convergence::{competing_requests_use_one_deterministic_proposal_and_retain_conflict,multiwriter_rejects_cross_repository_or_scope_rebinding}`. | Installed canonical-store restart/replay/retained-conflict & joined coverage cases. |
| ADP-Q072–077 | `clarification_state::{clarification_binds_one_answer_and_same_lineage_resume,stale_evidence_cannot_resume_or_rebind_answer,target_version_change_and_expiry_fail_closed,cancellation_and_unsupported_host_are_terminal,persisted_store_survives_restart_and_rejects_question_rewrite,stale_writers_fail_closed}`; `proposal_target_exclusion` target/version conflict cases; `adapt_resident::{scoped_inspection_cannot_mutate,operator_dispatch_rejects_offline_commands_and_unknown_fields}`; `governed_decisions` comparison/shadow/block/expiry cases. | Authenticated human transport; installed winner/merge/restart; real MCP; status; host trial; guard rollback & authority refusal. |
| CTX-Q018 | `cortex/tests/checkpoints::{checkpoint_rows_are_a0_session_records_and_never_ordinary_recall_candidates,closing_checkpoint_removes_resume_door_without_deleting_row,checkpoint_preserves_installation_lineage_and_optional_goal_task_snapshots,checkpoint_save_allows_only_exact_replay_and_preserves_collisions,checkpoint_payload_limit_accepts_exact_boundary_and_rejects_one_byte_over}`; `checkpoint_cli_lineage::checkpoint_cli_round_trip_preserves_lineage_resolves_docs_and_stays_session_only`; `native_authorization::{native_checkpoint_roundtrip_binds_installation_and_types_load_states,native_checkpoint_save_rate_is_shared_across_ids_and_retryable}`. | Installed save/list/restart/load/done/list, wrong install/scope, expiry/collision/size denial, zero recall. |
| CTX-Q019–021 | `cortex_checkpoint_promotion_acknowledges_one_durable_pending_proposal`; `cortex_remaining_acceptance::{ctx020_oversize_emission_rejected_before_any_write,ctx020_pending_proposal_readback_never_auto_admits}`; `native_authorization::{approved_proposal_reaches_cortex_admission_via_the_executor_review_path,rejected_and_pending_proposals_never_become_durable_truth,native_proposer_cannot_approve_by_adding_undeclared_reviewer_json,an_already_decided_proposal_cannot_be_re_decided,review_of_an_unknown_proposal_id_is_typed_and_creates_nothing}`; Cortex lifecycle trust/recovery/response-loss/expiry tests. | Installed promote/propose/status/review restart, zero-write rejection, forged/revoked/wrong-binding/expired denial & exact-once admission. |
| CTX-Q022 | `dream::{dream_merges_duplicate_memories_and_routes_secondary_to_quarantine_plan,dream_relative_dates_use_each_entry_created_at,dream_quarantines_low_effectiveness_entries}` plus store cases at `15969/16037/16062/16119/16641` & serve list/restore at `8838`. | Installed curate/restart/list/restore deterministic rerun with byte/scope preservation. |

### Complete committed Cortex Q register

Existing named tests below are focused source evidence only; every listed release-bound case remains PENDING. CTX-Q033, Q039, & Q042 are exploratory exclusions.

| Q / atom | Retained source assertion | Exact missing release-bound case / owner |
|---|---|---|
| Q001 / CTX-001 | `ctx001_canonical_open_reports_wal_and_converges_across_reopen` (`cortex_lifecycle.rs`). | Hub `hub_readonly_db.rs` & CLI direct-open use or are sanctioned by same migration/WAL/generation path; Cortex owner. |
| Q002 / CTX-002 | `ctx002_ordered_pregate_rejects_each_dimension_before_admission`. | MCP consumer exercises schema/scope/producer/DLP/epistemic/stable-ID denial plus accepted receipt; Cortex/runtime owner. |
| Q003 / CTX-003 | `memory_batch::{memories_batch_commits_every_item_and_returns_truthful_receipts,identical_retry_is_noop_and_conflicting_batch_id_is_atomic_409,invalid_middle_item_rejects_whole_batch_before_embedding_or_commit}`. | Released batch consumer; exact receipt/atomicity. |
| Q004 / CTX-004 | `ctx004_resolver_reports_explicit_provenance_availability_without_guessing`, sensitivity/derivation, migration-26 backfill cases. | Production write classification; current unavailable-legacy defaults cannot qualify. |
| Q005 / CTX-005 | `ctx006_exact_duplicate_with_new_evidence_updates_metadata_only` & batch retry no-op. | Released existing-identity/no-op consumer. |
| Q006 / CTX-006 | `ctx006_exact_duplicate_with_new_evidence_updates_metadata_only`; `ctx006_same_content_in_different_scope_is_not_a_duplicate`. | Released duplicate vs cross-scope consumer. |
| Q007 / CTX-007 | `ctx007_quarantined_conflict_invisible_across_recall_resolver_recipe`. | Released quarantine/ordinary-seam exclusion/governed restore. |
| Q008 / CTX-008 | `batch_lifecycle_supersession_transitions_old_row_in_same_transaction`; `lifecycle_supersede_hash_guard_and_receipt_are_content_free`. | Deterministic simultaneous-conflict/history matrix. |
| Q009 / CTX-009 | `ctx009_temporal_round_trip_keeps_observed_valid_recorded_expiry_distinct`. | Released temporal round-trip consumer. |
| Q010 / CTX-010 | Review-due/version/outcome trigger & external-signal idempotency cases. | Ledger/Blueprint intake production seam; Membrane/Cortex owner. |
| Q011 / CTX-011 | `ctx011_suppressed_and_quarantined_rows_excluded_from_recall_scored`; `fts5_bm25_ranks_keyword_column_above_body_only_match`. | CLI/service/Pull recall. |
| Q012 / CTX-012 | `ctx012_hybrid_recall_identical_eligibility_across_vector_dispatch_settings`. | Released hybrid consumer. |
| Q013 / CTX-013 | `ctx013_hybrid_recall_fuses_lanes_without_score_confusion` & membrane-core fusion units. | Released lane fusion/receipt consumer. |
| Q014 / CTX-014 | `ctx014_resolve_memory_pages_and_records_observed_use_only`. | Released page resolution/use-recording. |
| Q015 / CTX-015 | Verdict-ref rejection/ranking/advisory/non-replay cases. | Released candidate-bound verdict consumer. |
| Q016 / CTX-016 | `bounded_unknown_closure_links_each_unresolved_delivery_once` & saturated-cap rejection. | Released bounded closure telemetry. |
| Q017 / CTX-017 | Closed-vocabulary, resolved/dangling supersedes, relation-admission cases. | Durable supports/contradicts/derived_from ingest/traversal; only supersedes persists. |
| Q018 / CTX-018 | Checkpoint save/load/list collision, lineage & authorization cases listed above. | Installed checkpoint matrix; Cortex/runtime owner. |
| Q019 / CTX-019 | `cortex_checkpoint_promotion_acknowledges_one_durable_pending_proposal`. | Installed idempotent promotion/restart/scope denial. |
| Q020 / CTX-020 | Oversize pre-write denial & pending readback listed above. | Installed propose/status restart & DLP/producer/epistemic/scope zero-write matrix. |
| Q021 / CTX-021 | Approval+job recovery, expiry/corrupt approval denial & review cases listed above. | Installed trust/MCP review/kill-restart exact-once & forged/revoked/binding/expiry denial. |
| Q022 / CTX-022 | `lifecycle_curation_quarantines_expired_and_excludes_other_gated_states`. | Deterministic Stage-0 duplicate/date/low-value/restriction/quarantine matrix. |
| Q023 / CTX-023 | `deterministic_provider_produces_curation_proposals_from_the_event_window`; governed/idempotent drain. | First-party semantic provider/released producer; loopback endpoint is insufficient; external producer/deployment owner. |
| Q024 / CTX-024 | `bounded_episodic_candidate_matches_its_window_exactly`; provenance-fence & drain-idempotency cases. | Authoritative foreground event producer & released cursor-range consumer; external host/Membrane owner. |
| Q025 / CTX-025 | Hard erase links/FTS/projections, restore absence & rollback cases. | Released CLI erasure. |
| Q026 / CTX-026 | Backup/restore suppression/quarantine/tamper/recall-equivalence cases. | Released CLI backup/restore. |
| Q027 / CTX-027 | `export_md_writes_canonical_tree` (`cortex/tests/db_first.rs`). | Released export/audit-tree proving DB remains authority. |
| Q028 / CTX-028 | No exact committed test found. | Deterministic content-optional queue ordering, bounded fields, scope/lifecycle/conflict visibility & repeatability through released consumer; Cortex owner. |
| Q029 / CTX-029 | Erase/reindex absence, unchanged-row memoization & reindex-error cases. | Released reindex consumer. |
| Q030 / CTX-030 | `ctx030_explain_projection_is_read_only_with_lifecycle_and_provenance`. | Released explain projection. |
| Q031 / CTX-031 | No exact qualifying test found. | Trusted controlled H7/H9/H10 real-host telemetry joins, lineage, unavailable/failed outcomes, immutable experiment/promotion receipt; external host owner. |
| Q032 / CTX-032 | Atomic envelope measurements/family-producer-session dual-write/content-free telemetry cases. | Released telemetry consumer. |
| Q034 / CTX-034 | `ctx034_skill_index_search_bounded_read_handle_shape` & skill read traversal/privacy cases. | Released bounded skill-index consumer. |
| Q035 / CTX-035 | `cortex_admission_utility_precedes_mutation_and_preserves_independent_gates`. | Released utility-admission consumer. |
| Q036 / CTX-036 | Backup/restore suppression & wipe/restore recall-equivalence cases. | Released backup/restore. |
| Q037 / CTX-037 | `export_import_defaults_and_round_trip_content_scope` (`runtime/cli.rs`). | Released import qualification. |
| Q038 / CTX-038 | Exact/lower-bound list, review-due, recall envelope cases. | Byte-equivalent released CLI/service/Pull envelope. |
| Q040 / CTX-040 | `cortex_recall_recipe_is_versioned_bounded_deterministic_and_receipt_complete`. | Released recipe consumer. |
| Q041 / CTX-041 | Suppression reversible/version-fenced/reopen/reindex/backup/restore/signed-resume cases. | Released suppression lifecycle consumer. |
| BPT graph/store/identity: BPT-001–021,023–044 | Deterministic confined scan; Rust/Python/JS/TS/TSX parsing; provider precedence; stable IDs/generation; cancellation; v20 rollback/round-trip; watcher create/modify/delete/gap; explicit no-holder manual operations. | Native `membrane-blueprint` graph/store/watch/service. | Graph/store/watch owners: `graph.rs`, `index.rs`, `store.rs`, `watch.rs`, `freshness.rs`, `service.rs`; graph freeze `14499bf6…`. | Source freezes; old Node proofs are not native proof. | Qualify individual `graph`, `generation_store`, `watch`, `service`, `contracts`, `identity`, `schema_v20`, `migrations` tests after Blueprint crate freeze. |
| BPT query/Phase 2/export/findings: BPT-046–047,049–071 | Bounded query/cancellation; Phase 2 provenance; `Export`; seven advertised findings operations; baselines, SARIF/evidence pack; stale/generation/cap/cancellation negatives; MCP/federation consumer routing. | Native engine, one-shot, resident service, MCP/federation. | Query owner (retry-stopped); engine owner (retry-stopped); findings/export packet; `engine.rs`, `query.rs`, `export.rs`, `findings.rs`, `phase2.rs`. | Graph/findings source freezes only. `engine.rs` still returns unsupported for Export/findings & graph build lacks cooperative cancellation. | Resolve existing stopped chains by disposition; no route-around. Then run named tests plus consumer call paths. |
| BPT-Q001–007,010,012–018,020–021 | Root/worktree confinement & freshness (`root-registry`, `freshness-observation`); tracked source disposition; static graph/grammar/parser fixtures; compiler adapter typed degradation & ambiguity abstention; provider network/process/manifest fail-closed; rename/reanchor; confidence migration; atomic publication/store lease; config/provider/recovery equivalence. | Graph build → static provider → application/CLI/MCP. | `tests/root-registry.test.mjs`, `freshness-observation.test.mjs`, `source-disposition.test.mjs`, `graph-substrate.test.mjs`, `grammar-manifest.test.mjs`, `treesitter-provider.test.mjs`, `compiler-adapters.test.mjs`, `intelligence-build-integration.test.mjs`, `provider-registry-runtime.test.mjs`, `reconcile.test.mjs`, `portable-identity-reanchor.test.mjs`, `confidence-*`, `atomic-store-adoption`, `store-lease`, `store-migrations`, `dependency-dag`, `incremental-equivalence-config-provider`, `build-recovery-equivalence`; BPT-I owners. | Legacy JS cases only; no native lifecycle closure. | Port each cited assertion to native graph/store tests; source freezes permit this after Blueprint crate closure. |
| BPT-Q008–009,019,028–029,032,040,047 | Missing exact cases: declared HTTP/SQL provider positive+unsupported denial; stale sealed-generation refusal; bounded service search; anchor ambiguity; treeish/file/line impact; architecture projection citations/coverage/omissions; two-repository federation isolation. | Native application service/recall/freshness/federation. | BPT-I008/I009/I019/I028/I029/I032/I040/I047 owners; `query.rs`, `freshness.rs`, `service.rs`, federation consumer. | No named current runtime case found. | Add specified service-level tests; BPT-Q047 must preserve separate generation/evidence/omission slices. |
| BPT-Q023–027,030–031,033–037,041–044,046 | Exact evidence-path, candidate-order, cursor, neighborhood & graph-query cases; liveness/test-recommendation; snapshot deterministic/conflict/history; recall decision is reported not enforced; five-adapter parity/error taxonomy; service lifecycle; CLI doctor/repair. | Application service/one-shot/daemon/CLI/SDK/MCP. | `recall-circuit`, `recall-candidate-contract`, `traversal-cursor`, `neighborhood`, `graph-query`, `liveness`, `test-recommendation`, `snapshots`, `recall-contract`, `adapter-parity`, `service-lifecycle`, `doctor-repair` tests; corresponding BPT-I owners. | JS cases retained; BPT-Q035 current impact risk separation & Q037 live snapshot-vs-current non-overwrite are missing. | Port retained cases; add Q035 structural-risk-vs-cochange & Q037 history-projection isolation cases. |
| BPT-Q049–052,065–067 | Baseline capture/list/delta; SARIF uses same bound finding objects; explain exact fingerprint/source/hash; explicit bounded evidence-pack selection; BP001/2/3 plus omission states. | Resident findings service → engine → CLI/MCP. | JS `findings-service`, `sarif`, `findings-explain-pack`, `findings-detect`; native `tests/findings.rs`, `tests/export.rs`; engine/integrator owner. | Native direct tests cover some explain/pack/SARIF/detection; engine still rejects findings/export. | After stopped engine disposition, add engine-routed baseline, SARIF, source/hash, explicit selection, ambiguity/star-cycle/depth/default/type-only/namespace/out-of-scan/parse-failed cases. |
| BPT-Q053–057,060–063 | Framework/database/deployment/Terraform provider source/generation evidence & absence refusal; support-bundle/repair redaction; manifest admission; update/apply/rollback/checksum/signature/downgrade/replay; actual protocol redaction. | Providers/build, CLI update/repair/support bundle, MCP. | `support-bundle-redaction`, `doctor-repair`, `provider-registry-runtime`, `intelligence-build-integration`, `update-rollback`, `update-contract`; BPT-I053–057/I060–063. | Retained JS cases; provider fixtures & protocol-redaction case missing. | Add four provider fixtures plus actual MCP support-bundle redaction response case. |
| BPT-Q058–059,064,068–070 | Apply/rollback; explorer starts without API/token browser child; loopback/token-auth HTTP. | CLI update/explorer HTTP. | `update-rollback`, `update-contract`, `explorer`, `ui-security` tests; BPT-I058/I059/I064/I068–070. | Legacy JS proof only. | Native/package matrix must assert no token-bearing child, loopback-only & auth refusal. |
| BPT-Q071 | Frozen FFI/JNI/cgo/gRPC/PInvoke/WASM/COM corpus: deterministic source-addressed bridge/containment, no inferred CALLS, hard negatives, full/incremental equivalence. | Providers bridges → build → Recall. | BPT-I071, `providers/bridges/seams.mjs`, native bridge owner. | No committed native case. | Add bridge corpus & full/incremental equality test before released proof. |
| LDG-001–013,015–022,024–027,030; LDG-014,029,031 | Enrolled-root parse, stable identity, search→resolve, pagination/graph bounds, lifecycle/erasure, provider authority; diagnostics/backlinks/manifests/drift. | Runtime Ledger + native MCP. | PPL Ledger owner; frozen diagnostics owner. | 18/18 Push/Ledger pre-PPL result is limited fresh evidence. | Run `ledger_residual_qualification` after runtime crate freeze; map diagnostic `LDG-Q` cases independently. |
| LDG-028 | PlainText/JSON/HTML/PDF/DOCX: grant/size, raw/normalized hashes, converter config/loss/omission, unsupported media, repeated conversion, search→resolve, snapshot/live. | Public CLI + native MCP bridge. | `ledger-public-format-ingestion` owner; `tools.rs`, `mcp_executor.rs`, `ledger/cli.rs`, `ledger/service.rs`, `ledger/doc_spine.rs`. | JS MCP 1/1 PASS; reservation release already occurred. | Run focused Rust test after runtime crate freeze; add installed CLI/MCP fixture matrix. |

### Checkpoint freeze rule

PPL tests compile `membrane-runtime`; native effects/ACK/residency/host composition modify that same crate. Blueprint tests compile `membrane-blueprint`; graph/findings/engine changes share that crate. A filename-disjoint packet is not a check-disjoint lane. Owners may prepare read-only plans while source is paused; on resumption, integration owner freezes each actual crate/dependency closure before its checkpoint.

### Bootstrap, provisioning & transport contract correction

1. `membrane-client/src/binding.rs` already chooses `ProvisionPackaged` only for `NotFound`; `provisioning.rs` exists. Missing is CodeRight host effect: canonical installer invocation, rediscovery, binding/store identity verification & readiness before acquire. Verified compatible running authenticates/acquires; verified compatible stopped starts canonical stable `current`, then authenticates/acquires; `NotFound` provisions configured package then rediscovers; supported repairable incompatibility uses canonical update/repair then rediscovers; package-unavailable returns typed provisioning-unavailable; denied, corrupt, unverifiable, rotated, or timeout state refuses without credential/execution fallback.
2. `CanonicalBinding` public construction (`binding.rs:26`) prevents trustworthy SDK bootstrap. Its opaque replacement, child transport & post-effect continuity must freeze with client provisioning before host composition.
3. Current reviewed controller layout requires `engine/crates/membrane-runtime/src/bin/membrane-daemon.rs` plus `native_diagnostics_pipe.rs`, `serve.rs`, `residency.rs`, `membrane/src/serve.rs`, `membrane/src/dispatch.rs`, `membrane/src/modes.rs`, SDK client files, Hub/tray owners, & CodeRight bootstrap owner. Historical seven-file host packet is obsolete; this validated corrected owner packet is its replacement & preserves `serve.rs` transfer.
4. `native_diagnostics_pipe.rs` is no-op outside Windows. Existing macOS/headless path is stable-`current` `membrane-daemon` TCP loopback; it becomes cross-platform only after canonical SDK HMAC request/response proof is implemented. No Unix-socket transport is asserted.
5. Manual compaction task authority & authentic H8 producer/provenance are independent contracts. Each must be settled before it is named as a full-host acceptance prerequisite; neither blocks independent library/PPL checks.

### Targeted lifecycle closure amendment

This amendment replaces daemon-only parent-loss, independent tray token, construction-time deadline, & unspecified lease-retry instructions. It is an implementation contract for `lifecycle-controller`; source code & qualification stay paused. Packet ownership/input correction is prerequisite to this worker starting. Its deadline, lease, cache-pressure, & installed tests are worker acceptance after implementation, never evidence prerequisites to writing that repair.

| Finding & source | Finite transition | Exact owner & acceptance |
|---|---|---|
| Windows launcher termination: `process.rs:185-225,492-504`; `supervisor.rs:754-768` | `supervisor.rs` chooses shared-controller launch **before** spawn: `process.rs` explicitly escapes/no-assigns kill-on-close job & verifies no inherited kill-on-close parent job, or returns `shared_controller_job_escape_denied`. Non-shared paths retain existing containment. Daemon begins a 30-s startup-grace lease with automatic cleanup on launcher loss until first authenticated holder arrives; launch cannot be retrofitted after holder acquire. Thereafter closing tray wrapper closes pipes/observation but cannot kill shared daemon. `DaemonCommandV2::LauncherDetached` & stdin EOF are advisory only. `service.rs`/runtime `residency.rs` serialize detached state with holder mutations: zero holders drains; any valid holder keeps runtime, Blueprint watcher, & background scheduler live. Explicit tray stop first releases its own lease; command cannot drain a peer-held controller. `membrane-daemon.rs` unregisters disconnected control receiver, then runs background cadence independent of `signal_rx`; post-ready invalid/EOF cannot call global `lifecycle.fail` or drain peer-held runtime. Receiver-gone stdout becomes detached output state: bounded best-effort event emission never blocks or exits runtime; only explicit final-holder drain/fatal runtime ends it. | `process.rs`, `supervisor.rs`, `workspace.rs`, `main.rs`; `tray_daemon.rs`; runtime `service.rs`, `residency.rs`, `serve.rs`, `bin/membrane-daemon.rs`. Inline/module tests terminate actual Windows tray launcher after CodeRight holder acquisition, assert same daemon PID plus creation/startup generation stays alive (replacement PID fails), then filesystem change → Blueprint refresh/query & actual background output complete. Cover pre-holder grace cleanup, explicit tray stop with peer holder, no-holder detach drain, EOF/invalid receiver, receiver-gone output, & denied job escape. |
| Single protected credential: tray `workspace.rs:270-363`; daemon `serve.rs:959-1077,1393+`; all readers | `serve.rs` owns sole installed credential authority & cross-process creation/migration lock. It creates/publishes owner-protected 32-byte/64-lowercase-hex token, then all readers consume winning bytes. `workspace::api_token` becomes resolver only; no `OpenOptions` publisher, replacement, or independent rotation. Installed mode rejects env-token substitution & noncanonical file as `credential_migration_required`; canonical authority holds lock, rereads/fingerprints weak bytes, creates protected candidate, atomically compare-and-replaces only when file still has that fingerprint, then rereads winning protected token. A changed contender discards candidate & rereads winner; no create-new publication is attempted over existing name. Successful migration increments `startupGeneration` & records typed migration. Old-key caller receives fence/credential-rotated response then re-discovers; it never silently changes key. | Writers: `serve.rs`, `workspace.rs`, `supervisor.rs`, daemon launch schema. Readers/callers: tray `snapshot.rs`/`main.rs`, Hub `dashboard_connection.rs`, runtime `cli.rs` resident forwarding, `mcp_http.rs`, `hook_diagnostics.rs`, `background_review.rs`; generic non-resident bearer routes remain unchanged unless listed transport calls use HMAC. Tests: tray-first, daemon-first, concurrent cold start, weak/noncanonical migration contender race, old-key rotation/re-discovery, Windows DACL/Unix mode, & exact one winning credential/generation. |
| Long-lived client & synchronous transport: `membrane-client/residency.rs:83-208`; `memory_backend.rs:30-63` | Remove construction-time `CallOptions` from `InstalledResidentController`. Each direct public call makes fresh 30-s operation deadline. `with_call_options` remains request-scoped composed absolute deadline: every substep shares one deadline. Change `ResidentHolderTransport::call` to receive `&CallOptions`; tray snapshot, Hub dashboard, & CLI adapters enforce remaining budget separately at connect, write, & read, recomputing before each blocking operation. | `membrane-client/residency.rs`, `memory_backend.rs`, `lib.rs`; `snapshot.rs`, `dashboard_connection.rs`, `cli.rs`; tests reuse same controller across expiry periods then renew/status/release, prove composed budget is not reset, & stalled connect/write/read returns timeout without blocking beyond remaining budget. |
| Lease retry/order: protocol `operations.rs`; client/runtime `residency.rs` | V2 replaces client `expiresAtUnixMs` mutation with server-capped `requestedLeaseTtlMs` (`1..=60000`) plus server-returned expiry. A new `POST /resident-holder/acquire-permit` returns opaque server-MACed `AcquirePermitV2 { controller generation, holder binding, leaseIncarnation[128-bit], issued, expires≤10s }`; only an unexpired matching permit may create lease. Permit MAC uses per-start server-only key, so a delayed acquire cannot be renewed with fresh transport HMAC after all retained records expire. `LeaseHandleV2` fixes `(holder, credential, leaseIncarnation, operationSequence)`; each acquire/renew/release carries 128-bit `operationId`. First acceptance stores bounded per-incarnation `OperationOutcome { canonicalBodyDigest, operation, sequence, logicalDeadline, response }`, where `logicalDeadline = first authoritative receipt + min(requested TTL, 60 s)`. Fresh HMAC nonce/proof may retry only unchanged body & operation ID before that stored deadline; changed body/same ID is `operation_conflict`; after record/tombstone expiry result is `operation_outcome_unknown`, so client status/reconciles then gets new permit/new incarnation—never resurrecting old state. Runtime overwrites client wall clock, maps TTL onto process monotonic deadline, & exposes only nondecreasing authoritative wall observation; clock rewind cannot extend residence. Renew applies only next sequence for matching active incarnation & requires expiry strictly greater than current expiry; lower/equal sequence returns typed stale/conflict without mutation. Release closes matching current incarnation. Registry retains one tombstone **per closed incarnation** through max(its permit expiry, logical retry deadline), capped at 60 s; it never overwrites an unexpired prior incarnation when same holder rapidly reacquires/releases. Global 64 tombstone capacity rejects new acquire when full rather than evicting live tombstone; active acquire reserves its eventual tombstone slot, so release never fails for bookkeeping. Any delayed old acquire, renew, or release returns stale/expired permit or stale-incarnation & cannot affect reacquired lease. Controller generation mismatch forces rediscovery. | `operations.rs`, `lib.rs`, client/runtime `residency.rs`, `serve.rs`; CodeRight `memory-backend/binding.rs` is read-only consumer of returned SDK `LeaseHandleV2` & `native.rs` only injects transport. Protocol & registry tests cover lost response with fresh nonce/same logical operation, changed-body conflict, expired unknown/reconcile, expired delayed acquire permit, reordered renew, rapid A acquire→release→B acquire→release→delayed A while permit valid, tombstone capacity, clock rewind, & controller-generation change. |
| HMAC surface, confidentiality & pressure | **Canonical loopback-auth V1 bytes:** prefix `u32be(25) || ASCII("membrane.loopback.auth.v1") || u8(kind)`; every following field is `u32be(length) || octets`. Request kind `1` fields, in order: method ASCII, exact target ASCII, normalized Host ASCII, Content-Type ASCII or empty, raw-body SHA-256 (32 octets), installationId UTF-8, cortexStoreId UTF-8, releaseGeneration UTF-8, startupGeneration as `u64be`, stableInstallRoot UTF-8, nonce (32 octets), expiry as `u64be`. Response kind `2`: request nonce, status `u16be`, raw-response SHA-256, then same five identity fields. No NUL terminator exists. HMAC profile requires exactly one each of version, nonce, expiry, five identity, & proof headers; allows only Host, Content-Length, `Connection: close`, POST `Content-Type: application/json`, & those auth headers; rejects query/fragment, `Authorization`, duplicate/unknown semantic headers, chunked body, non-JSON body, or target/body/header mismatch. `memory_backend.rs` route constants & CodeRight `native.rs` adapter are sole SDK route authority: profile covers GET `/health`, `/metrics`, `/activity`; POST `/delete`, `/federate`, `/get`, `/list`, `/put`, `/recall`, `/remember`, `/remember_consolidated`, `/scopes`, `/search`, `/use`; plus holder-specific `POST /resident-holder/acquire-permit`, `POST /resident-holder`, & dashboard `GET /hub/snapshot`. No third route list is valid. Legacy unauthenticated liveness stays separate & cannot satisfy signed client response verification. Plaintext request contents can be read by stale listener; HMAC proves integrity/response authenticity only. Payload contains no reusable credential; confidentiality needs separate authenticated transport. Reserve 384 of 4096 unexpired nonce slots for renew/release; general traffic may occupy at most 3712. Full relevant partition returns typed retry-after without evicting live nonce; readiness reports `ReplayAdmissionSaturated` when protected reserve is exhausted. | Shared signer `membrane-client/residency.rs`/`lib.rs`, runtime `serve.rs`, `installation_manifest.rs`, protocol `operations.rs`; tray `snapshot.rs`, Hub `dashboard_connection.rs`, CLI forwarder & CodeRight `native.rs` adapter. Canonical vectors cover every field/encoding & each adapter-selected route; negatives cover query, header, target, body, response-proof, stale endpoint, & acquire permit. Saturate general partition at 3712 near renewal boundary, prove valid renew/release & bounded teardown use reserve, protected saturation reports typed degradation, then expiry recovers admission. |

### CodeRight compaction federation V1 — settled cross-product contract

This is a new compatible `/federate` extension, not an interpretation of existing H8 expiry or an H9 retention assertion. It governs one normal compaction attempt. It does not authorize a non-Membrane fallback on Anthropic, OmniRoute, or another required normal provider path.

| Wire object | JSON wire names & type | Authority / validation |
|---|---|---|
| Request | `compactionCommit: { "schemaVersion": 1 }` | Present only for this operation. Reject `loadedContextIdentities`, `refresh`, `pullPublication`, or any suppression/retention/eviction/digest field in same request. No V1 staged, retained, or removed member mapping exists. |
| Reply | `compactionCommit: { "schemaVersion": 1, "decisionId": string, "requestIdentity": ContextRequestIdentityV1, "binding": { "installationId": string, "cortexStoreId": string, "releaseGeneration": string, "startupGeneration": u64, "stableInstallRoot": string }, "repositoryId": string, "scopeId": string, "tokenGeneration": u64|null, "issuedAtUnixMs": u64, "expiresAtUnixMs": u64, "ttlMs": u32, "selectionReceipt": PacketReductionSelectionReceiptV1 }` | `ttlMs` is server-issued `1..=30000`; `expiresAtUnixMs` is a **new response field**, not request H8 `expiresAtUnixMs`. Reject mismatch of echoed request identity, full fence, scope tuple, or selection receipt before local commit. |

The response is selection only: Membrane chooses fresh packet content under authentic current H8. V1 guarantees no mechanical replay of old host blocks; it does not guarantee semantic non-reintroduction, so fresh selection may choose equivalent material. Non-reintroduction requires a future authenticated disposition signal; no local filter is authorized. `pull/federation.rs:260` derives `loaded_context` only from request `loadedContextIdentities`; its catalog/suppression path is guarded by `Some(loaded)` at lines 442–455, as are prior-packet lookup & pending H9 publication. The V1 rejection rule therefore proves no persisted/global H9 suppression alters fresh selection. A later H9 may report only actual post-commit host state.

`ContextFederationRequest.identity.generation` remains request/context generation; it must not carry binding or scope authority. Binding is exact five-part `NativeIdentityFence`; live scope derives from `RepositoryBindingV1` (`scope_id`, optional `token_generation`) reloaded on authorization. A successful request admits one in-flight, one-use local commit before expiry. Later revocation fences the next authorization/request; it does not rewrite an already-admitted decision.

| Commit rule | Required behavior |
|---|---|
| Lease start & restart | CodeRight records process-local `Instant` immediately before transport dispatch. Receipt delay consumes TTL. Pending decision belongs to live `SessionEngine` compaction operation; no durable queue. Restart loses that `Instant` & invalidates decision. |
| Pre-commit checks | While `&mut SessionEngine` owns compaction, reject unless wall clock is before `expiresAtUnixMs` **and** dispatch monotonic elapsed is below `ttlMs`; reject malformed/mismatched response. |
| Atomic storage | Existing archive seam becomes one synchronous SQLite transaction for archive, lineage, projection, event, & compaction commit. Final pre-`COMMIT` admission requires `now < expiresAtUnixMs` & monotonic elapsed `< ttlMs`, with no await gap. Admission authorizes synchronous commit; it does not promise durable completion before wall expiry. An injected post-check delay preserves that admitted decision; later revocation fences next request only. Current archive insert alone is insufficient. |
| Model-aware accounting | Required normal paths must obtain authentic current provider/model capacity through Membrane H8 before selection, final-image remeasure at authentic host render boundary, & account selected content through this receipt. Typed unavailable is valid only for genuinely absent/unsupported authenticated capacity, never for a configured Anthropic/OmniRoute normal path or as a bypass to ordinary compaction. |

Exact source owners: Membrane protocol `engine/crates/membrane-protocol/src/federation.rs` & `src/lib.rs`; native parser/selection `engine/crates/membrane-runtime/src/pull/federation.rs`; route `serve.rs:3026-3031,3331-3333`; current grant source `authorization.rs:147-164,226-231`; raw client compatibility `membrane-client/src/memory_backend.rs`. CodeRight transport/fence `coderight-memory-backend/src/context_federation.rs` & `native.rs:124-129,540-587`; operation state `crates/engine/src/engine_session_controls.rs:832-1029`; transaction seam `crates/engine/src/compaction_archive.rs` & `bins/coderight/src/api_compaction_archives.rs:30-110`; manual entry `bins/coderight/src/api_sessions/approvals.rs`; route coverage `bins/coderight/src/api_tests_part_31.rs`. `compactor_breaker.rs` & `compaction_coordinator.rs` receive no decision queue/state.

Compatibility: absent `compactionCommit` preserves current `/federate`; unknown schema or missing/malformed/expired reply is typed unavailable for this extension. That outcome cannot satisfy a configured required normal provider path.

`TokenEstimateV1` with `Complete` coverage means a numeric observation is present; it is **not** a proven mathematical upper bound. Current CodeRight H8 only subtracts caller-supplied window/reserved/observed fields, while current Membrane Push accepts only `o200k_base/1` & measures serialized packet content through `cortex_core::ContextTokenAccounting::count_exact`; final native egress measures only MCP result, not full provider-rendered image. Therefore each configured required model route needs one actual versioned estimator algorithm/recipe available at both host acquisition & Membrane selection, plus final-image remeasurement receipt. Equal `EstimatorBasisV1` labels alone do not prove same algorithm. `SafeBound` is a separate unimplemented profile. On overflow, bounded recovery re-enters Membrane with fresh H8 on same normal route; it never falls back to local ordinary compaction.

### Installed lifecycle, producer & scope decisions

#### Authentication, provisioning & resident lifetime

| Decision | Exact contract & owner |
|---|---|
| Transport authorization | `NativeIdentityFence` in CodeRight `coderight-memory-backend/src/native.rs:124-129` is installed identity tuple: `installationId`, `cortexStoreId`, `releaseGeneration`, `startupGeneration`, `stableInstallRoot`. Stable-`current` `membrane-daemon` (`apps/membrane-tray-windows/src/workspace.rs:45-52`) binds TCP `127.0.0.1`; no Unix-socket seam exists. Replace `snapshot.rs` bearer use with one SDK-owned signer in `membrane-client/src/residency.rs` re-exported by `lib.rs`: generated 32-random-byte/64-lowercase-hex `api-token` from `serve.rs:959+`, `token_from_file_or_create:988+` is required; arbitrary `MEMBRANE_API_TOKEN` values accepted by `validate_api_token:1449` do not meet this strength contract. Persist/retrieve generated credential only through existing token file protection: Windows owner-only DACL (`windows_create_owner_only_token_file:1028,1080,1129`); Unix `0600` non-symlink reader (`read_token_file:1393+`). `serve.rs` provides only thin runtime wrapper/verification around shared signer; `snapshot.rs` calls wrapper through existing `membrane-runtime` dependency, so tray `Cargo.toml` stays unchanged. CodeRight read-only consumers call SDK signer transport. Client owns `hmac`, workspace `sha2`, & `getrandom` nonce generation; it never transmits credential. Apply **only** canonical loopback-auth V1 encoding/routes from `Targeted lifecycle closure amendment`; no alternate domain or framing is operative. `serve.rs` verifies identity, expiry, & proof, then atomically reserves nonce before work. Replay cache holds at most 4096 unexpired nonces, each expiry capped at 30 s; if full it rejects admission rather than evicting a live nonce. Cache resets only with a new `startupGeneration`; rotation/reload uses a new protected generated credential, clears cache, & changes that generation. A fresh nonce can retransmit only unchanged active `LeaseHandleV2` logical operation before its stored deadline; it cannot change or extend that operation. `snapshot.rs`/runtime wrapper verify matching response proof & request nonce. `installation_manifest.rs` owns envelope/identity binding; `serve.rs` owns verification/replay; no CodeRight crypto authority. Windows `native_diagnostics_pipe.rs` retains DACL/remote-client rejection. Same-user secret readers are outside this transport-proof boundary. Reject forged endpoint, mismatched identity, stale generation, cross-root scope, foreign provenance, rotated/revoked grant. |
| Provision transaction | CodeRight owns one idempotent transaction: **discover → acquire installation lock → route by artifact class → rediscover `current` → health/fence validate → acquire holder → full-ready**. Production route verifies signed compatible artifact before canonical install/update; user-authorized internal route accepts only fixed internal candidate hash into isolated internal profile, labels receipt `unsigned-functional`, & cannot update production `current`, public release channel, or signed-activation evidence. Both validate `(CodeRight artifact version, Membrane artifact/release generation, SDK protocol version, store schema, installationId, cortexStoreId, startupGeneration, capability digest)`. Route exactly: verified compatible running → authenticate/acquire; verified compatible stopped/offline-known → start canonical stable `current`, then authenticate/acquire; `NotFound` → configured package provisioning, then rediscover; supported repairable incompatibility → canonical update/repair, then rediscover; package unavailable → typed provisioning-unavailable; denied, corrupt, unverifiable, rotated, timeout, interruption, or partial effect → refuse without credential/execution fallback. Owners: `membrane-client/src/provisioning.rs`, `binding.rs`, CodeRight `native.rs`, installer/tray/Hub supervisors. |
| Full-ready, not merely held | Ready means one authenticated fenced controller **plus** Blueprint source observation with current published generation/freshness, a live background review/maintenance scheduler, capability catalog, & a renewable holder. A zero-long-lived-holder explicit CLI/HTTP/MCP/SDK operation takes its own scoped lease through terminal response; controller cannot drain before that lease ends. Test concurrent first start/acquire/renew/release/final-drain, stale holder fencing, survivor continuity after one holder exits, full-ready work under survivor, no-holder explicit-operation completion, restart during acquire, & drain/acquire race. Existing local lease counts/races do not prove this installed matrix. |
| Causal watcher readiness | Current `watch.rs` moves `applied_clock` when callback merely schedules; `service.rs` immediately executes callbacks with `max_events=256`, has neither debounce nor retry. Replace this false readiness with one dirty generation, one scheduled rebuild, & a `256`-event bounded batch. Event during debounce/build supersedes in-flight generation; build must recheck input fingerprint before publish, discard stale output, then schedule latest dirty generation. Retry only failed latest batch, under explicit bounded retry policy; terminal failure remains stale/degraded, preserves journal, & never advances exposed freshness. Under quiescence each dirty generation converges to exactly one published matching generation or typed stale; under continuous edits it coalesces latest state without unbounded queue. Record `(sourceObservationId/fingerprint, dirty generation, scheduled callback generation, build input generation, published Blueprint generation, exposed freshness generation)`. Context admits only published generation covering observed source or returns typed stale/incomplete. Required sequence: edit → automatic build → second edit → publication → context request, plus manual refresh during build; neither may claim freshness from enqueue. Owners: Blueprint `watch.rs`/`freshness.rs`/`service.rs` & runtime consumer. |
| Rollback & signing | Binary cutover, store/data recovery, & signing are separate. `installer.nsi:340-392` previous-current swap is binary-only. Before upgrade, capture a recoverable store/activation snapshot; **after post-activation writes, never restore old snapshot**. Require forward-compatible new binary or write-preserving forward recovery; if downgrade cannot preserve writes, retain new runtime & emit controlled repair state. Separately prove unsigned internal candidate functionality, then RightKit-signed positive activation & tampered/unsigned activation denial using existing release-signing path. |

#### External producer ownership — no loopback substitutes

| Requirement | Exact production seam & proof boundary |
|---|---|
| CTX-Q023 first-party semantic producer | CTX-023 requires Stage-1 contradiction/near-duplicate/supersession **proposal-first** review, not merely event transport. Existing `DeterministicFirstPartySemanticReviewProvider` (`background_review.rs:867+`) calls `cortex_core::review::analyze_events_for_curation`; retain & qualify it against accepted contradiction/near-duplicate/supersession corpus before adding provider infrastructure. `background_review_input.rs:166-290` remains window/cursor producer; `serve.rs:3186-3225` invokes it after successful `AdaptObservationRequestV1::Analyze`; daemon owns installed route. Its explicit `AdaptBehavioralReview → SemanticProviderNotWired` refusal is distinct missing behavior: wire real semantic producer for that path, with provider identity, recipe, source-window digest, & proposal lineage. Fixture loopback cannot close either route. |
| CTX-Q024 authoritative foreground event/cursor | `serve.rs:3186-3225` turn boundary is foreground producer; `background_review_input.rs:95-103,198-291` owns cursor/snapshot atomicity. Required receipt binds real host turn, scope/session/task, from/to sequence, provenance, & installed consumer drain. Snapshot source is not authority to fabricate foreground events. |
| CTX-Q031 & Adapt trial/control | CodeRight external host owns trusted H7/H9/H10 telemetry, activated asset/version, final loaded representation, evaluator outcome including unavailable/failed, & immutable experiment/promotion receipt. Membrane `adapt_effectiveness.rs` consumes joins; `serve.rs:4845-4877` assigns policy/control cohort but does not supply real host trial evidence. `coderight/engine/crates/telemetry/src/evidence.rs` is H4/H6 record owner; `bins/coderight/src/api_runtime/resource_metrics.rs` is bounded exposure seam. No H8-only row qualifies CTX-Q031. |
| H4/cursor carriage | CodeRight telemetry evidence producer records host facts; runtime `background_review_input.rs` owns cursor exactly as above. CodeRight must route authenticated H4/cursor/trial/control fields through its native host transport; no user/model JSON may self-attest them. |
| H8 retained client/hook carriage | CodeRight `context_federation.rs:927-1044` creates current-request H8; `native.rs:425-430,549-560` sends it through canonical SDK HMAC/native transport, never a bearer header. Retained `mcp/client.mjs` & `mcp/host/context-adapter.cjs` must preserve this exact H8 envelope; CodeRight `hooks/src/registry.rs:149-286,320-385` currently runs env-command hooks with no Membrane H8 carriage. Add only this named request seam; no H8 is fabricated from generic hook input. |

#### Closure denominator & entry custody

The accounting denominator remains all **340 committed atoms**, each lifecycle-open. Closure eligibility is clause-level: every non-UI clause is eligible; visual desktop/iOS clauses are excluded. Partial atoms are explicitly `MEM-006`, `MEM-007`, `MEM-008`, `MEM-009`, `MEM-025`, `MEM-026`, `MEM-027`, & `MEM-028`: retain their non-UI supervision/daemon/transport/installer/restart clauses; exclude only their desktop/iOS visual assertions. No UI-only clause may be closed from headless evidence; no headless supervision may be omitted by calling Hub/tray “UI.” Retained non-UI clauses include `MEM-Q016–021`, `MEM-Q055–066`.

Before a stopped chain, worker, retry, or archive is resumed, its owner records one recoverable snapshot/commit containing tracked, untracked, deleted, binary, & configuration bytes; exact base revision; content hash/location; custody acknowledgement; allowed writer; packet scope; & explicit disposition. Freeze hashes are drift detectors, never recovery objects. On user resumption, Pull catalog 21/22 permits only parent-grant identity repair plus its frozen catalog test/recheck; Blueprint query permits only two named Oracle finding repairs in query/service path plus exact recheck; Blueprint Export/findings permits only engine/export/findings route repair plus existing finding/export checks; native-host mismatch permits only corrected daemon packet after preserved handoff. Each is one repair + one recheck; any new defect returns typed blocked disposition. Fresh work never resets a stopped chain.

## Source-complete task preservation

| Specialist task | Preservation object | Remaining owner |
|---|---|---|
| Adapt/Cortex residual | Canonical pushed commit `1eb3fcbd` | Integration owner: installed lifecycle qualification only. |
| PPL Pull test | **Unpreserved working-tree source.** `pull_residual_qualification.rs` SHA-256 `2f33fd…` is a drift detector under packet `69eb0e0e…`, not a content-addressed patch. | Integration owner must create canonical commit or external content-addressed patch before any archival; then run frozen check. |
| PPL Push test | **Unpreserved working-tree source.** `push_residual_qualification.rs` SHA-256 `27624…` is not a patch. | Frozen Push recovery owner for defects; integration owner preserves then qualifies. |
| PPL Ledger test | **Unpreserved working-tree source.** `ledger_residual_qualification.rs` SHA-256 `2f532…` is not a patch. | Frozen Ledger diagnostics owner for defects; integration owner preserves then qualifies. |
| LDG-028 ingestion | **Unpreserved working-tree source.** Recorded per-file digests (`tools.rs` `36f662…`, MCP bridge `439…` / `95c…`, executor `051c…`, CLI `6842…`, service `dad128…`, doc spine `6479…`) identify bytes but are not a recoverable patch. | `ledger-public-format-ingestion` owner preserves canonical commit or external patch, then PPL integration qualifies. |
| Native host embedding handoff | **Unpreserved working-tree source.** Receipt `cd1bdc04f01928d76bfd074482b6194df61c70b40ff551ea9bbdccaf4b6c7cb8` & frozen hashes identify handoff only. | Native-host owner preserves patch/commit before CodeRight companion owner resumes. |
| Blueprint findings/export | **Unpreserved working-tree source.** Packet/receipt records source contract; `14499bf6…` & `7d4f…` are freeze identifiers, not patch locations. | Graph/findings owners preserve patch/commit; engine owner remains stopped. |
| Native package qualification | **Unpreserved working-tree source.** Receipt `5460d67babf664407455ccee5cf14a1fe7043ab07d486ac94a50ef68d0a3aba5` proves packet provenance, not product bytes. | Package owner preserves exact source before package work; no installer/seal exists. |

Only `1eb3fcbd` is currently a canonical preservation object. Every other source-complete specialist result above is at risk until its named owner makes a canonical commit or creates an external content-addressed patch.

## Dependency DAG

```text
PUL catalog stop ─> catalog recheck
PPL source ─> freeze `membrane-runtime` closure ─> independent Pull/Push/Ledger focused checks ──┐
manual task authority ─┐                                                                       │
authentic H8 producer ─┴─> public Pull/Push final-wire tests ───────────────────────────────────┤
opaque SDK + provision/rediscovery ─> shared controller/leases ─> host composition ─> CodeRight │
Windows pipe + macOS transport/package ──────────────────────────────────────────────────────────┤
Blueprint graph/store/watch ─> query cancellation + findings/export ─> stopped engine routing ──┤
                                                                                                  v
                    each source owner preserves tracked + untracked bytes as commit/patch ─> coherent source checks
                                                                                                  v
                       exact installed native artifact ─> mapped CLI/HTTP/MCP/SDK/holder/recovery matrix
                                                                                                  v
                                            attributed repair/recheck ─> lifecycle closure (non-soak only)
```

## Critical-path measured durations

No duration is estimated or summed.  Observed elapsed times are retained only for their own gates.

| Observed job | Elapsed | Dependency use |
|---|---:|---|
| Push/Ledger pre-PPL qualification, 18/18 PASS | 376.852 s | Historical fresh source evidence; does not replace PPL. |
| Pull full check, failure | 56.330 s | Catalog stopped-chain evidence. |
| Pull targeted catalog check, failure | 7.444 s | Catalog stopped-chain evidence. |
| Pull post-merge acceptance, PASS | 1.350 s | PUL acknowledgement preservation. |
| Adapt remediation, 19/19 PASS | 25.444 s | Commit `1eb3fcbd` evidence. |
| Adapt multiwriter, 2/2 PASS | 3.050 s | Commit `1eb3fcbd` evidence. |
| PPL JavaScript MCP check, PASS | 8.711 s | LDG-028 bridge evidence only. |
| Blueprint initial focused check, compile stop | 5.732 s | Current graph/findings repair predecessor. |
| Historical Cortex + Membrane CLI release build | 170.508 s | RightKit receipt `deb6ab82-3d30-4fd0-bcd5-b0bec6f85597`, successful 2026-09-08 04:31–04:37 UTC; historical component build only. |
| Historical daemon build | 136.889 s | RightKit receipt `19bbcf58-aecd-40bf-8682-73f1263e9411`, successful 2026-09-08; historical component build only. |
| Historical tray build | 57.101 s | RightKit receipt `84ad37e0-e5d7-4f86-a4ca-e87192445b50`, successful 2026-09-08; historical component build only. |
| Historical Hub build | 87.930 s | RightKit receipt `c0b5238d-8943-48f9-9e58-d1ed5e498c02`, successful 2026-09-08; historical component build only. |
| Previous unsigned installed candidate: build exit → installation exit | timestamp delta 44.7759879 s | `audit/remediation/README.md`: build exited `2026-09-07T23:22:03.5863779Z`; install exited `2026-09-07T23:22:48.3611745Z`. This is not a standalone build or install duration because individual start times are absent. |
| H8/manual task contracts, SDK/provision, Windows+macOS transport, Blueprint repair/recheck, candidate build, mapped installed non-soak matrix | unmeasured | No historical receipt gives those stage durations; no ETA inferred. |

## Executable planning dispatch

Validated authority packet: [delivery-reconciliation-membrane.dispatch.json](D:/Claude/membrane/audit/remediation/delivery-reconciliation-membrane.dispatch.json). Sidecar receipt binds exact bytes at [delivery-reconciliation-membrane.dispatch.receipt.json](D:/Claude/membrane/audit/remediation/delivery-reconciliation-membrane.dispatch.receipt.json). It is a delta for remaining lifecycle/protocol/watcher/producer/package seams; retained packets remain authority for unrelated 340-atom work. CodeRight is read-only external consumer; no Membrane worker owns its files.

## Estimated critical path after resume

Measured history: release component builds were 170.508 s (Cortex/CLI), 136.889 s (daemon), 57.101 s (tray), & 87.930 s (Hub); exact combined build→install timestamp gap was 44.776 s. Forecast quantities are estimates: custody 12 min; longest Membrane source lane is lifecycle/controller 85 min; its focused check 25 min; final cross-lane check 20 min; native-host queue 20 min; signed release build 35 min; exact install 10 min; installed runtime dogfood 35 min. Package-harness authoring starts beside source lanes; federation, Blueprint, & semantic lanes each run their focused check as soon as their own bytes integrate, not after unrelated lanes. Candidate waits only for package harness plus named source-check gates; install waits for signed candidate; dogfood waits for exact install. Base agentic critical path: **4 h 02 min**. Each stopped repair allows one repair + one recheck; repair time is unmeasured & excluded from base forecast.

## External-review amendment — bound remaining deltas

| Finding | Settled correction & exact owner |
|---|---|
| 2 — V1 eviction | V1 guarantees only no mechanical old-host-block replay. Membrane may freshly reselect same semantic material; non-reintroduction requires a future authenticated disposition signal. No local filter is authorized. `pull/federation.rs` owns request semantics. |
| 4 — parent loss | `bin/membrane-daemon.rs` moves wholly to lifecycle-controller. It owns control-loop `ParentClosed`, stdin EOF, scheduler survival, & serialized handoff to semantic producer through `background_review.rs`; semantic-producer owns no daemon file. Surviving CodeRight holder suppresses parent-triggered drain, keeps scheduler active, then proves real Blueprint refresh + background job after launcher closure. |
| 5 — provisioning | Discovery/action is one table: verified compatible stopped → start canonical controller; absent → configured installer; incompatible repairable → update/repair; corrupt/denied → refuse; package unavailable → typed unavailable; no development fallback. |
| 6 — pre-credential server authentication | Existing loopback + echoed tuple is insufficient. Reuse only generated protected `api-token` (`configured_api_token:959+`, `token_from_file_or_create:988+`): 32 random bytes/64 lowercase hex, Windows owner-only DACL at `1028,1080,1129`, Unix `0600` non-symlink read at `read_token_file:1393+`; arbitrary configured token is not strength-qualified. Do **not** challenge then send bearer. One concrete SDK signer in `membrane-client/src/residency.rs`/`lib.rs` uses declared `hmac`, workspace `sha2`, & `getrandom`; `serve.rs` thinly wraps it, `snapshot.rs` calls through existing runtime dependency, & external CodeRight consumers call SDK transport. **Canonical loopback-auth V1** in `Targeted lifecycle closure amendment` is sole HMAC framing/route definition. `serve.rs` atomically reserves nonce before work in a 4096-entry, ≤30 s cache; a full unexpired cache rejects admission, never evicts a live nonce. Cache reset changes `startupGeneration`; restart/reload rotates credential & clears cache. Fresh nonce retry is permitted only for identical active logical operation before stored deadline; changed body conflicts & expired outcome requires reconciliation/new permit/incarnation. `installation_manifest.rs` owns envelope/identity binding; no CodeRight crypto authority. Stale endpoint/impostor receives no reusable bearer. Same-user actor able to read secret material remains outside this transport-proof boundary & must be stated in installed threat model. |
| 9 — Ledger fidelity | New `ledger-content-repair` owns `ledger/document_conversion.rs` + `tests/ledger_public_format_ingestion.rs`. Fixtures assert expected Markdown for contiguous styled runs, tables, tabs, single-pass entity decoding, identifiers, & URLs. Repeatability/provenance alone cannot close LDG-028. |
| 10 — cold start | Blueprint repair reconciles persisted graph against a fresh source observation before full-ready. Missing graph, controller-offline edit, graph/offline edit, & lost in-memory queue each converge automatically or return typed stale; manual refresh is not required. |
| 11 — expiry | One predicate: final pre-`COMMIT` authorization requires `now < expiresAtUnixMs` & monotonic elapsed `< ttlMs`. Commit need not finish before wall expiry. Injected post-check delay preserves admitted bounded decision; revocation after admission fences next request only. |

Provider serializer/continuation accounting, phase-specific post-commit provider failure, V7 predecessor baseline, ignored PTY corpus, authority-manifest completeness, & scale timeline remain owned by retained external packet set; this delta neither reassigned nor closes them.
