# Delivery reconciliation planning scope

Status: planning only. No source edit, test, build, install, release, Git write, worker launch, or reviewer launch is authorized by this artifact.

Governing shared correction contract: `D:/Claude/coderight/docs/pending/delivery-recovery-plan-2026-09-08.md` plus this reconciliation's current `Targeted lifecycle closure amendment`. They bind shared eviction, parent-loss, provisioning, server authority, expiry, custody, effective ownership, credential, deadline, lease, signer, & replay decisions.

On explicit resume, execute this existing bounded packet for lifecycle/protocol/watcher/semantic-producer/package/qualification work. All 340 committed atoms remain accounted; only non-UI clauses are closure-eligible. Use canonical SDK HMAC request/response proof on installed loopback transports; never restore bearer transport. Retain deterministic Cortex semantic analyzer when corpus-qualified, repair only explicitly refused Adapt semantic path, use truthful watcher publication, & separate fixed-hash unsigned internal proof from signed production activation.

Use repository-relative Membrane write allowlists only. CodeRight is an external read-only consumer. Preserve every stopped-chain handoff as canonical commit or external content-addressed patch before any writer. Current observed primary base is `1eb3fcbdf9aa3f86fd002c5013d07a9ecc33f605`; before any writer, capture current dirty candidate as content-addressed source pin, bind packet to it, revalidate receipt, then obtain required packet review.
