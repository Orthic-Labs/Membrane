[
  {
    "owner": "adapt-completion",
    "cluster": "cli-entrypoint",
    "verdict": "parity-proven",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-runtime/src/cli.rs (adapt CLI tail), engine/crates/membrane-adapt/src/cli_api.rs",
    "nativeTests": null,
    "gap": "Native CLI surface has passing tests asserting explicit-store requirement, canonical-pool-change refusal, and CLI-tail registration -- genuine behavioral assertions, not vacuous constructs."
  },
  {
    "owner": "adapt-completion",
    "cluster": "admission-authority",
    "verdict": "parity-proven",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-adapt/src/admission.rs, engine/crates/membrane-adapt/src/authority.rs (tests/gates_and_boundaries.rs)",
    "nativeTests": null,
    "gap": "gates_and_boundaries.rs asserts admission does not fabricate durable state and authority fields are excluded from proposals -- reads as a real assertion (checks struct fields absent / state absent), not vacuous."
  },
  {
    "owner": "adapt-completion",
    "cluster": "proposal-lifecycle-and-conflict",
    "verdict": "parity-proven",
    "fileCount": 7,
    "nativeDestination": "engine/crates/membrane-adapt/src/proposal.rs, proposal_state.rs (tests/proposal_state_machine.rs, tests/proposal_target_exclusion.rs)",
    "nativeTests": null,
    "gap": "Native tests assert replay convergence, typed-conflict surfacing for competing variants, and tamper rejection on reopen -- these are the same self-merge / conflict-review guarantees the legacy consolidate/adjudicate manifest tests targeted, and each assertion inspects a concrete returned value/error, not just construction."
  },
  {
    "owner": "adapt-completion",
    "cluster": "multiwriter-precedence",
    "verdict": "parity-proven",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-adapt/src/multiwriter.rs (inline #[cfg(test)] mod tests, tests/cli_surface_and_convergence.rs)",
    "nativeTests": null,
    "gap": "multiwriter.rs inline tests assert order-independent precedence resolution and conflict preservation on equal tiers with divergent payloads; convergence permutation-invariance is separately asserted in cli_surface_and_convergence.rs. Read source: both compare concrete winner()/conflict values, not vacuous."
  },
  {
    "owner": "adapt-completion",
    "cluster": "outcomes-effectiveness",
    "verdict": "parity-proven",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-adapt/src/outcomes.rs, procedural_effectiveness.rs (tests/remediation_outcomes_cost.rs, tests/procedural_effectiveness.rs)",
    "nativeTests": null,
    "gap": "Native outcome ledger tests assert same-signature-only reopen, exposure-based adjustment, and refusal to invent counts on insufficient evidence -- matches legacy learning_outcomes semantics with concrete equality assertions."
  },
  {
    "owner": "adapt-completion",
    "cluster": "taste-record-and-v2-pipeline",
    "verdict": "parity-proven",
    "fileCount": 11,
    "nativeDestination": "engine/crates/membrane-adapt/src/taste.rs, record.rs, scope.rs, seal.rs (tests/taste_contracts.rs, tests/taste_quality_benchmark.rs)",
    "nativeTests": null,
    "gap": "taste_contracts.rs asserts fail-closed behavior on unknown scope, seal tamper detection, and rejection of episodic facts misclassified as taste -- these are the precise correctness properties the legacy taste_v2/preference_record/rule_key tests targeted, each with a concrete assert on the returned Result/enum variant."
  },
  {
    "owner": "adapt-completion",
    "cluster": "insights-detectors-and-recurrence",
    "verdict": "parity-proven",
    "fileCount": 6,
    "nativeDestination": "engine/crates/membrane-adapt/src/insights/{detectors,guards,recurrence,sealed_issue}.rs (tests/insights_detector_benchmark.rs, tests/insights_pipeline.rs, tests/portable_insights_benchmark.rs, tests/real_heldout_corpus.rs)",
    "nativeTests": null,
    "gap": "This is the strongest cluster: native tests assert hard-negative suppression (quoted/hypothetical/negated spans) and exact ground-truth reproduction against a held-out corpus, directly matching the lane's detector-positive/hard-negative obligation."
  },
  {
    "owner": "adapt-completion",
    "cluster": "clarification-resume-cancel",
    "verdict": "parity-proven",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-adapt/src/clarification.rs (tests/clarification_state.rs)",
    "nativeTests": null,
    "gap": "clarification_state.rs directly asserts the resume/cancel/stale-target obligations named in this lane's charter: stale evidence cannot rebind, cancellation is terminal, and restart-persisted state rejects question rewrite -- concrete Result/state assertions, not vacuous constructs."
  },
  {
    "owner": "adapt-completion",
    "cluster": "manifest-family",
    "verdict": "insufficient",
    "fileCount": 9,
    "nativeDestination": "engine/crates/membrane-adapt/src/manifest.rs (no #[cfg(test)] module found in this file at inspection time)",
    "nativeTests": null,
    "gap": "Native manifest.rs exists but carries no discoverable unit tests, and no tests/*.rs file in membrane-adapt targets manifest v13 contract semantics, snapshot mining, or semantic validation by name. The legacy test_manifest_v13_contract.py is exactly the kind of versioned-contract test the NCL-02 bar requires a named equivalent for -- none found. Would need: a native test asserting the v13 manifest contract (field set / version gate) and one asserting mine_snapshot_manifest / semantic_validate_manifest fail-closed behavior, with I1 evidence hash proving they ran."
  },
  {
    "owner": "adapt-completion",
    "cluster": "session-transport-and-persistence",
    "verdict": "insufficient",
    "fileCount": 9,
    "nativeDestination": "unclear -- candidates in membrane-client (residency.rs, memory_backend.rs) or membrane-adapt, not confirmed",
    "nativeTests": null,
    "gap": "Could not identify a specific native module or test asserting session-selection, eval-isolation, or LLM-transport equivalence for adapt specifically (the membrane-client tests found relate to generic memory/residency, not adapt's own session/persistence/LLM-transport behavior). Would need a named native test file (or module) plus at least one passing assertion tying it to these five legacy behaviors before this cluster can move to parity-proven."
  },
  {
    "owner": "adapt-completion",
    "cluster": "workspace-cross-machine-and-doctor",
    "verdict": "insufficient",
    "fileCount": 14,
    "nativeDestination": "engine/crates/membrane-adapt/src/benchmark.rs / cli_api.rs (doctor_flags_corrupt_state, benchmark_digest_is_stable_and_scores_families exist, but token spend / transcript snapshot / cross-machine sync semantics not confirmed native-side)",
    "nativeTests": null,
    "gap": "Only the doctor/benchmark half of this cluster has a matching native assertion. token_spend.py, transcript_snapshots.py, transcript_sources.py, cross_machine.py, and run_journal.py have no identified native counterpart or test in the I1 passing list -- grepping for cross_machine, transcript_snapshot, token_spend, run_journal by name in the native crate and in i1-passing-tests.txt returned nothing. These legacy files should stay until a native destination and test are named for them specifically; do not let the doctor/benchmark match justify deleting the whole cluster."
  },
  {
    "owner": "adapt-completion",
    "cluster": "test-harness-and-smoke",
    "verdict": "insufficient",
    "fileCount": 7,
    "nativeDestination": "engine/crates/membrane-adapt (tests/closed_loop_qualification.rs, tests/governed_decisions.rs) and engine/crates/membrane-adapt/src/delivery.rs",
    "nativeTests": null,
    "gap": "The governed_decisions.rs tests cover the freeze/phase-pilot/corroboration *evaluation* semantics (comparison selection, hard-negative protection, terminal baseline) reasonably well, but 'delivery parity' and 'production delivery smoke' are end-to-end smoke assertions -- no native smoke-test equivalent was found asserting an actual delivered packet under production-like conditions (delivery.rs has no accompanying test file in the list gathered). Mark insufficient for test_production_delivery_smoke.py and test_delivery_parity.py specifically until a native smoke/parity test is named; the pilot/corroboration files are closer to proven via governed_decisions.rs."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/release/launchers",
    "verdict": "insufficient",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/scripts (top-level)",
    "verdict": "insufficient",
    "fileCount": 17,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/scripts/ci",
    "verdict": "insufficient",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/scripts/cli",
    "verdict": "insufficient",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/scripts/grammars",
    "verdict": "insufficient",
    "fileCount": 2,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/scripts/release",
    "verdict": "insufficient",
    "fileCount": 15,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/graph (core)",
    "verdict": "insufficient",
    "fileCount": 61,
    "nativeDestination": "engine/crates/membrane-blueprint/src/graph.rs (scan_repository/is_canonical_ignored_dir/is_canonical_ignored_file for ignored-prefixes.mjs canonical-list behavior; build_generation/ast_facts/walk_ast rust branch for language-extractors.mjs struct/impl extraction; generation_identity_bodies/is_registered_relationship_kind for generation-identity.mjs/relationship-kinds.mjs, already had native tests); engine/crates/membrane-blueprint/src/dependency_dag.rs (build_projection_dependency_dag/invalidated_projections/projection_fingerprint/ProjectionCache for dependency-dag.mjs's projection-dependency DAG and cache, newly ported); engine/crates/membrane-blueprint/src/bm25_index.rs (tokenize/Bm25CodeIndex::{replace,replace_document,remove_document,search}/build_bm25_code_index for bm25-code-index.mjs's identifier tokenizer, incremental BM25 index with name-containment admission gate, and generation-node symbol filter, newly ported); engine/crates/membrane-blueprint/src/merkle_ledger.rs (leaf_digest_for_file/dir_digest/update_leaf_chain/compute_full_ledger/diff_ledger_against_tree for merkle-ledger.mjs's bottom-up directory Merkle root, incremental leaf-chain update, and exact add/change/remove diff, newly ported; generation-identity.mjs was already fully covered pre-existing by engine/crates/membrane-blueprint/src/identity.rs's content_digest/compute_generation_id/compute_manifest_digest_value, confirmed by re-reading src/identity.rs before porting so no duplicate module was created; entry-points.mjs is now ported to engine/crates/membrane-blueprint/src/entry_points.rs::{build_entry_point_registry, build_entry_point_registry_from}, a new file operating on the same nodes/edges JSON shape carried by store::Generation, reproducing explicit entryPoint===true / EntryPoint-label tagging, the zero-inbound/has-outgoing structural-candidate rule restricted to kind=='symbol', includeStructuralCandidates suppression, and id-sorted output, registered via one append-only `pub mod entry_points;` line at the end of src/lib.rs; module-surface.mjs's export/import surface extraction was found ALREADY NATIVE and equivalent — graph.rs's private module_surface() function (tree-sitter-gated parse-error/has-error detection, then per-statement extraction of exports/starReexports/requests, with the identical closed vocabulary of open reasons: parse_error, export_assignment, commonjs_exports, ambient_module, destructured_export — nested_export and unfollowable_star_reexport are not separately reasoned but star-reexports to unresolvable targets are still handled as omissions downstream in findings.rs's effective_exports) already runs in the production build_generation path and feeds findings.rs; per lane instructions ('if present and equivalent, only add missing parity tests'), no duplicate module_surface.rs was created — parity_module_surface.rs exercises it end-to-end through the public build_generation API instead of duplicating its private logic in a new unused file). No native counterpart found for: architecture-model, atomic-store-adoption, bootstrap, confidence-migration, framework-intelligence, neighborhood, parse-cache, path-order, portable-identity, portable-manifest, precision-tiers, process-projection, provider-identity, publication-policy, reanchor, schema-extractors, scip-provider, seed-resolver, signature-projection, stable-read (native has a differently-scoped stable-read in freshness.rs/watch.rs, not confirmed equivalent), store-lease, store-sqlite, structural-intelligence, symbol-authority-order, test-recommendation, traversal-cursor, traversal-policy, traverse-store, treesitter-provider, watchman. Lane GC9 additionally ported evidence-authority.mjs (plus the small provenance.mjs helpers it needs) into the new evidence_authority.rs -- SEMANTIC_AUTHORITY_ORDER/RESOLUTION_SPECIFICITY_ORDER, sourceCoherenceRank, semanticAuthorityForFact/semanticAuthorityRankForFact, resolutionSpecificityRank, and the full non-compensatory evaluateEvidence pipeline (admissibility -> source coherence -> authority -> specificity -> inferential confidence, with LIVE_VERIFICATION treated as a cross-check and every frontier/unresolved reason and the admitted/categorical_precedence success path reproduced) -- registered via one append-only 'pub mod evidence_authority;' line at the end of src/lib.rs; and liveness.mjs into the new liveness.rs -- LIVENESS_STATES, the citation-dedup helper, buildLivenessProjection's maxNodes/maxEdges/maxHops bounding, the trustworthiness gate (complete && sourceState==='clean' && admitted explicit entrypoints present) with its three reasons, the untrustworthy-path UNKNOWN projection, and the trustworthy-path BFS reachability/path reconstruction -- reusing crate::entry_points::build_entry_point_registry rather than reimplementing entry-point selection, registered via one append-only 'pub mod liveness;' line at the end of src/lib.rs. doc-truth-projection.mjs was found ALREADY NATIVE and equivalent at the pre-existing src/doc_truth.rs (project_document_truth/DocumentClaim/TruthEdge/GROUNDING_STATES matching DETERMINISTIC_CLASSES/publicConfidence/citationFor/groundingState/projectDocumentTruth field-for-field), confirmed by re-reading doc_truth.rs before porting so no duplicate module was created -- only missing parity tests were added. parity_evidence_authority.rs (18/18: freshness-precedes-authority, authority-precedes-inferential-confidence, categorical tie -> authority_tie_conflict, heuristic-only confidence tiebreak, unknown-relation never coherent, inadmissible/wrong-relation exclusion, legacy categorical mapping, equivalent-evidence retention, plus every evidence-authority-boundary.test.mjs case: source-identity mismatch dominance, explicit-stale dominance, unknown/object-identity non-coercion, UNRESOLVED-marker/unknown-provenance rejection, missing-target/missing-relation rejection, non-numeric/out-of-range inferential-confidence rejection, null-confidence-vs-zero, legacy-confidence normalization without input mutation, and LIVE_VERIFICATION cross-check-only/resolution_conflict semantics), parity_doc_truth_projection.rs (4/4: direct/contradicted/ambiguous/unsupported/stale grounding states, deterministic-null-confidence preservation, heuristic inferential-confidence retention, and projection schema shape), and parity_liveness.rs (3/3: entry-registry explicit-vs-structural-candidate separation, LIVE/UNREACHED/UNKNOWN-only emission with correct reachabilityPath, and the three UNKNOWN-collapse triggers -- stale source, no explicit entrypoint evidence, incomplete/bounded generation) all pass locally under rightkit cargo test, alongside the crate's full existing suite (unaffected). This is additive: like the rest of this cluster, caller cutover is still not observed (blueprint/src/graph/evidence-authority.mjs, doc-truth-projection.mjs, liveness.mjs, and provenance.mjs remain live per the callerCutover note below), so verdict stays insufficient regardless of this new native coverage. static-provider.mjs and delta-store.mjs are now PARTIALLY ported (see Lane GC6 note below): static-provider.mjs itself has no config-digest logic (that lives in blueprint/src/providers/build.mjs's buildConfigDigest, a different legacy file, owned by the graph (core)/misc boundary per GC2's original note) and is now ported to the new src/static_provider.rs; delta-store.mjs's core applyFileDelta is now ported natively (lane GC10: src/delta_store.rs apply_file_delta + src/store_delta.rs; tests/parity_delta_store.rs 16/16 green) but its pure sub-logic (normalizeDigest, the domains_pending watch-state helpers, replaceBySource) is now ported to the new src/delta_store.rs. ast-structural-search and generic-ast-walker (generic table-driven strategy only) are now ported -- see ast_structural_search.rs/ast_walker.rs above.; engine/crates/membrane-blueprint/src/ast_structural_search.rs (search_ast_structure/SearchPattern for ast-structural-search.mjs's kind/name/pathPrefix/relation/declaringType/label-filtered bounded structural search over an already-built GraphGeneration's nodes/edges, with deterministic sort, limit clamp, and truncation/omission accounting, newly ported); engine/crates/membrane-blueprint/src/ast_walker.rs (define_language_table/walk_table for generic-ast-walker.mjs's table-driven generic 'patterns' strategy over a tree-sitter node -- functions/classes/declarations/imports/comments pattern matching, evidence with content hash, confidence-from-parse-error, and uniqueQualifiedName's nested same-name disambiguation, newly ported; the file's hand-written jsLike/python/rust per-grammar strategies were left unported since graph.rs's own ast_facts/walk_ast already serves that per-language extraction role in production and was out of scope for lane GC7, which does not own graph.rs); engine/crates/membrane-blueprint/src/conformance_verifier.rs (verify_semantic_conformance/assert_semantic_conformance for conformance-verifier.mjs's deterministic node/edge assertion engine -- dot-path traversal incl. array indices, $exists/$includes/$containsAll/$matches operators, _absent/exactly/atLeast count rules, and the exact failure-message format -- newly ported, operating generically over serde_json::Value facts rather than one generation shape); engine/crates/membrane-blueprint/src/freshness_receipt.rs (build_freshness_receipt for freshness-receipt.mjs's buildFreshnessReceipt receipt-assembly/suppression-mode derivation rules -- staleSources filtered to indexed paths, suppression required/mode(none|changed_paths|whole_generation) -- newly ported; the module's pure freshness/generation-coherence axes (generationFreshnessBasis/evaluateFreshness/assertGenerationCoherence) were already present and equivalent pre-existing in src/freshness.rs (evaluate_freshness/assert_generation_coherence/GenerationFreshnessBasis/CurrentSourceState), confirmed by re-reading freshness.rs before porting so only missing parity tests were added for that half rather than a duplicate module; git-shell enumeration (observeCurrentVcsState/changedPathsSinceGeneration) has no native port, matching freshness.rs's own deliberate no-process/shell-dependency design -- callers supply already-observed inputs) Lane WIRE1 (2026-09-10): newly ported engine/crates/membrane-blueprint/src/process_projection.rs (build_process_projection, port of graph/process-projection.mjs), src/signature_projection.rs (project_symbol_signatures, port of graph/signature-projection.mjs), and src/orientation_projection.rs (build_cold_start_orientation, port of graph/orientation.mjs composing entry_points/contract_registry/signature_projection/conventions); all three plus the pre-existing liveness.rs/contract_registry.rs/architecture_model.rs are now wired through new src/architecture_views.rs::dispatch_view, reached from query.rs Operation::Architecture when request.input.view is one of flows/liveness/processes/contracts/signatures/orientation/projection/changes, caching processes/contracts/signatures/orientation through dependency_dag::ProjectionCache keyed by repo root. flows and changes remain typed architecture_view_not_wired (no native graphFlowInventory port exists; changes needs a live store Connection this layer does not carry). See engine/crates/membrane-blueprint/tests/parity_architecture_views.rs.",
    "nativeTests": null,
    "gap": "Of 61 legacy files, a small slice (ignored-prefixes canonical-list subset, generation-identity, relationship-kinds, the rust/python/js/ts/tsx branch of language extraction, dependency-dag, bm25-code-index, merkle-ledger, and now entry-points plus the already-native module-surface) has a confirmed native counterpart. merkle-ledger.mjs's bottom-up Merkle ledger (leaf digest = normalized file content digest; directory digest = xxh128 over the JSON-encoded sorted [name, digest] child-entry array, matching the JS authority's `JSON.stringify` of a sorted array-of-pairs, which needs no key-sort/stableStringify handling since it is an array not an object; deepest-first/reverse-lexicographic directory rebuild ordering; incremental updateLeafChain touching only the changed leaf's ancestor chain; full computeFullLedger rebuild; and diffLedgerAgainstTree's exact added/changed/removed sets against a fresh scan) is now ported behaviour-for-behaviour into merkle_ledger.rs, reusing the crate's existing identity::content_digest (xxh3-128, the same algorithm family as the JS authority's hash-wasm xxh128, an equivalence already relied on elsewhere in identity.rs) rather than adding a hashing dependency. The legacy suite (blueprint/tests/merkle-ledger.test.mjs) asserts relational parity only (root changes iff a leaf changes; delta-built root equals a clean full-build root; diff is exact) rather than pinned literal digest values, so parity_merkle_ledger.rs ports that same relational shape verbatim with the identical treeA/treeB fixtures and assertions, rather than inventing digest fixtures the legacy suite never had — 2/2 passing locally under rightkit cargo test, alongside the crate's full existing suite (unaffected). dependency-dag.mjs's projection-dependency DAG (fixed parent-to-projection edges over source/provider/config/schema/generation, invalidatedProjections closure, sha256 projection fingerprinting over only declared parents, and the bounded ProjectionCache with hit/miss/invalidated reporting and oldest-entry eviction) is ported behaviour-for-behaviour into dependency_dag.rs, with parity_dependency_dag.rs covering the DAG shape, invalidation closure determinism, fingerprint scoping (including the unknown-projection error), cache hit/miss/invalidated transitions, and eviction — 6/6 passing locally under rightkit cargo test. entry-points.mjs's explicit-vs-structural-candidate entry-point registry (source-backed entryPoint===true / EntryPoint|entry_point label tagging always wins; a kind=='symbol' node with an outgoing edge and zero observed inbound edges is a structural candidate only when includeStructuralCandidates is true; non-symbol nodes never qualify as candidates; rows sorted by id) is ported verbatim into the new entry_points.rs, with parity_entry_points.rs covering explicit-marker precedence, case-insensitive label tagging, the zero-inbound/outgoing structural-candidate rule, exclusion of nodes with any inbound edge, exclusion of non-symbol kinds, suppression under includeStructuralCandidates=false, id-sort ordering, evidence pass-through, and the empty-generation case — 9/9 passing locally under rightkit cargo test. module-surface.mjs's tree-sitter export/import extractor was independently found already present and behaviorally equivalent in graph.rs's private module_surface() (same closed set of open reasons, same soundness rule that any unrecognized/dynamic construct opens rather than silently closes the surface), already wired into the production build_generation -> findings.rs path, so per lane instructions only missing parity tests were added: parity_module_surface.rs proves named/default exports close the surface, `export {x as y} from` records both the export and the reexport request, commonjs `module.exports`/`export =`/`declare module`/destructured-export constructs each open the surface with the correct reason, unsupported extensions report no language, and a genuinely unparsable file reports parse_error rather than a partial guess — 9/9 passing locally under rightkit cargo test. The remaining ~45 files (architecture-model, structural-intelligence, store-sqlite, watchman, scip-provider, treesitter-provider, etc.) have no identified native module at all, these are named above as the true remaining port, not faked. Lane GC6 additionally ported the real config-digest producer -- found, after grepping static-provider.mjs itself and finding no digest logic there, to actually live in blueprint/src/providers/build.mjs's buildConfigDigest/BUILD_CONFIG_FILES/isBuildConfigFile (the configDigest field dependency-dag.mjs threads through was historically unwired until build.mjs started computing it from tsconfig.json/jsconfig.json/package.json/pnpm-workspace.yaml content hashes, sorted by path and sha256-hashed; see the BPT-020 follow-up comment above buildConfigDigest) -- into the new src/static_provider.rs (build_config_digest/is_build_config_file/BUILD_CONFIG_FILES, byte-for-byte row/separator/sort/hash parity with the legacy function), plus delta-store.mjs's pure, DB-independent sub-logic into the new src/delta_store.rs (normalize_digest for the xxh128: prefixing helper; PendingDomains reproducing the watch_state domains_pending CSV read/mark/clear semantics, including the legacy's own non-dedup-on-read quirk; replace_by_source reproducing replaceDocumentArtifacts's splice-in-place-else-append list logic). delta-store.mjs's applyFileDelta (409 lines orchestrating writes across files/symbols/edges/fact_owner/node_provider/dependency_index/file_state/watch_state/event_journal/generation tables plus generation-identity reseal and merkle-ledger leaf-chain updates and doc-artifact filesystem writes) remains unported: this crate's native engine (store.rs/engine.rs) has no equivalent incremental-apply entry point or shared schema to port that transactional core onto, so it is named here as remaining scope rather than silently dropped. parity_static_provider.rs (5/5: config-file basename admission incl. path-separator normalization, null digest with no config files present, a tsconfig.json paths/baseUrl fixture matching the shape blueprint/tests/incremental-equivalence-config-provider.test.mjs uses, order-independence, and edit-sensitivity) and parity_delta_store.rs (5/5: normalize_digest idempotent prefixing, domains_pending read/mark/clear round-trip incl. the non-dedup-on-read case, and replace_by_source's splice/append/delete-by-empty-replacement cases) pass locally under rightkit cargo test, alongside 10 new inline unit tests in the two new modules and the crate's full existing suite (unaffected). This is additive: like the rest of this cluster, caller cutover is still not observed (blueprint/src/graph/static-provider.mjs, blueprint/src/graph/delta-store.mjs, and blueprint/src/providers/build.mjs remain live per the callerCutover note below), so verdict stays insufficient regardless of this new native coverage. Independent of test coverage: NOT CUT OVER: scripts/run-m0-baseline-current.mjs still imports blueprint/scripts/release/npm-cli.mjs and runs \"pnpm --dir blueprint test:all\"; scripts/qualification/cases/bpt-windows.mjs still states DELIVERED status is void for the native path per REC-02 and still loads legacy blueprint/ files directly; mcp/capability-inventory.mjs still lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, blueprint/src/graph/resolution/index.mjs as live shipped artifacts. Per NCL-02 a cluster with no caller cutover is rejected regardless of native module existence or new test evidence, so verdict stays insufficient even where ported tests now pass. Lane GC7 additionally ported ast-structural-search.mjs and the generic table-driven strategy of generic-ast-walker.mjs (the only strategy exercised by that file's own legacy test suite; the hand-written jsLike/python/rust strategies in the same legacy file were left unported as out of scope, since graph.rs's existing ast_facts/walk_ast already performs equivalent per-language extraction in production and graph.rs is owned by another lane). search_ast_structure operates on the crate's real GraphGeneration/GraphNode/GraphEdge shapes (model.rs), pulling labels/qualifiedName out of node.evidence[0] the way the production graph builder already populates them; walk_table reimplements a minimal, independent tree-sitter node walk (via the tree-sitter crate already in Cargo.toml) rather than reusing any private graph.rs helper. 10/10 (ast_structural_search) and 9/9 (ast_walker) parity tests pass locally under rightkit cargo test; ast_walker's tests parse real tree-sitter-javascript source (the legacy suite used hand-built mock tree nodes, not available here). This is additive: like the rest of this cluster, caller cutover is still not observed (blueprint/ remains the live shipped path per the callerCutover note below), so verdict stays insufficient regardless of this new native coverage. Lane GC5 additionally ported conformance-verifier.mjs's assertion engine into conformance_verifier.rs (parity_conformance_verifier.rs: operator matrix incl. $exists/$includes/$containsAll/$matches, _absent/exactly/atLeast count rules, null-vs-missing-path Object.is semantics, and the legacy 'failure names the exact violated assertion' case, plus the exact-import-call fixture adapted to real build_generation output -- 4/4 passing) and freshness-receipt.mjs's receipt-assembly rules into freshness_receipt.rs (parity_freshness_receipt.rs: the pure freshness/generation-coherence axis ported against the pre-existing equivalent code in freshness.rs, plus build_freshness_receipt's suppression-mode derivation across fresh/changed_since_generation/unknown/unavailable -- 14/14 passing) -- 18/18 new tests passing locally under rightkit cargo test, alongside the crate's existing suite (unaffected by this lane's changes; one unrelated pre-existing failure, nested_same_name_generic_functions_retain_distinct_qualified_ids in a concurrent lane's parity_ast_walker.rs, was observed and is not this lane's file). This is additive: like the rest of this cluster, caller cutover is still not observed, so verdict stays insufficient regardless of this new native coverage. Lane GC8 additionally ported contract-registry.mjs's contract extraction/exact-key bridge stitching/trace assembly into the new contract_registry.rs (buildContractRegistry's HttpRoute/EventTopic/ToolContract/UiRoute kind+address+schema digest keying and PRODUCES/CONSUMES/USES/ROUTES_TO role derivation; bridgeContractRegistries's exact-contractKey-only join that never bridges within the same repoId; stitchContractTraces's consumer-then-provider step assembly -- newly ported, registered via one append-only `pub mod contract_registry;` line at the end of src/lib.rs) and conventions.mjs's descriptive convention mining into the new conventions.rs (namingStyle's kebab/snake/camel/Pascal classification, the test-path-exclusion rule for the production file-naming population, the larger-population test_placement heuristic with the smaller population retained as counterexample, and the always-false policyAuthority/WeakEvidence framing on both the top-level and per-row shape -- newly ported, registered via one append-only `pub mod conventions;` line at the end of src/lib.rs). parity_contract_registry.rs (5/5: exact-key bridging incl. a near-miss-address negative control, same-repo exclusion, HttpRoute method+path address derivation, unrecognized-label nodes never extracted, PRODUCES/ROUTES_TO granting the provider role) and parity_conventions.rs (4/4: WeakEvidence/policyAuthority=false framing with counterexamples retained, test-placement population-size preference, module_layout with no counterexamples, below-minimum-examples yields no row) all pass locally under rightkit cargo test, alongside 5 inline unit tests in the new modules and the crate's full existing suite (unaffected). This is additive: like the rest of this cluster, caller cutover is still not observed (blueprint/src/graph/contract-registry.mjs and conventions.mjs remain live per the callerCutover note below), so verdict stays insufficient regardless of this new native coverage. Lane GC9 additionally ported evidence-authority.mjs and liveness.mjs into evidence_authority.rs/liveness.rs (liveness.rs reuses the already-ported entry_points.rs rather than reimplementing entry-point selection), and confirmed doc-truth-projection.mjs already has an equivalent, pre-existing native port in doc_truth.rs (only missing parity tests added there). 25 new parity tests pass locally under rightkit cargo test (18 evidence-authority, 4 doc-truth-projection, 3 liveness), alongside the crate's full existing suite (unaffected). Independent of test coverage: caller cutover is still not observed for these three files either (see callerCutover below), so verdict stays insufficient even where ported tests now pass. Lane GC11 additionally ported architecture-model.mjs, framework-intelligence.mjs, and git-source-observation.mjs: architecture_model.rs (buildArchitectureModel's assignLayers-based layer clustering ported inline from analytics/index.mjs since buildArchitectureModel depends on it, plus coupling/hotspot ranking, and buildDisposableArchitectureProjection's path-prefix component grouping, cross-component flow extraction, and component/flow-ceiling omissions), framework_intelligence.rs (augmentFrameworkIntelligence's config/ORM/DI/RPC/UI regex-driven binding detection and domain-node/edge synthesis, with same-file exact-name symbol resolution standing in for the legacy structural-intelligence.mjs resolver, the only resolution behavior the legacy fixtures exercise), and git_source_observation.rs (gitBaseCommit/gitSourceObservation's bounded `git rev-parse HEAD` + `git status --porcelain=v1 -z` pair, reproducing the legacy 5000ms timeout manually via a worker thread + mpsc recv_timeout since no process-timeout crate dependency was added; no existing native git observation helper was found in this crate or in membrane-runtime for this exact pair) -- all newly ported, registered via three append-only pub mod architecture_model; / pub mod framework_intelligence; / pub mod git_source_observation; lines at the end of src/lib.rs. parity_architecture_model.rs (5/5: analytics-ported layer determinism/tie-breaking fixture from graph-analytics.test.mjs, layer/coupling/hotspot grouping, path-prefix component grouping with cross-component flow extraction, component-ceiling omission, and repeat-call determinism), parity_framework_intelligence.rs (2/2: the real DI/ORM/config/RPC/UI fixture from framework-process-contracts.test.mjs, plus an unresolved-DI-target frontier case), and parity_git_source_observation.rs (5/5: pre-commit None state, lowercase-hex head format, clean-worktree digest stability across repeat calls, untracked-file dirty transition with a changed digest and unmoved head, and a nonexistent-root None case, against a real temporary git repository per blueprint/tests/freshness-receipt.test.mjs's withGitRepo pattern) all pass locally under rightkit cargo test, alongside the crate's full existing suite (unaffected). Documented assumption: the legacy git-source-observation.mjs hashes porcelain status bytes with hash-wasm's xxh3-128; this port uses the xxhash-rust xxh3_128 implementation already used elsewhere in this crate (freshness.rs, identity.rs, phase2.rs) for the same algorithm family, but byte-for-byte cross-runtime digest equality with the JS hash-wasm output was not independently verified in this lane (no Node runtime harness was wired into the Rust test), so parity tests assert digest stability/sensitivity behavior rather than a literal JS-vs-Rust digest match -- flagged as a blocker in the lane GC11 receipt. This is additive: like the rest of this cluster, caller cutover is still not observed (blueprint/src/graph/architecture-model.mjs, framework-intelligence.mjs, and git-source-observation.mjs remain live per the callerCutover note below), so verdict stays insufficient regardless of this new native coverage. [lane GC14 2026-09-10]: recall-circuit.mjs comparePaths ranking is now wired into the native query pipeline -- engine/crates/membrane-blueprint/src/query.rs Operation::Path enumerates bounded from->to alternative paths (enumerate_paths) and ranks them with the already-ported recall_circuit::compare_paths (BPT-026 non-compensatory ordering) via rank_paths, replacing the prior first-found-BFS pick. Regression: engine/crates/membrane-blueprint/tests/parity_path_ranking.rs (3/3, including a BFS-first-vs-ranked-best divergence fixture). Scope note: only Operation::Path was wired this pass -- Recall/Expand/Impact still build their frontier via plain adjacency traversal in query.rs and do not yet construct AtomicPath candidates through recall_circuit, so this cluster remains insufficient for those operations pending a further port. [lane GC15 2026-09-10]: Operation::Recall is now store-driven end-to-end, closing the gap GC14 left open for it (Expand/Impact intentionally untouched -- legacy service.mjs's expand/impact handlers never called executeRecallCircuit, only recall did, so their shared plain-BFS adjacency traversal in query.rs is unchanged). recall_circuit.rs gained the DB-driving pieces its own module doc comment recorded as missing: resolve_seeds_native (in-memory port of seed-resolver.mjs's resolveSeeds lane order -- explicit node_id, source_address path match, qualified_symbol/exact_term/bounded_lexical task-term matches -- re-expressed as scans over GraphGeneration.nodes since query.rs has no live SQLite handle at this layer, only an already-loaded generation), select_traversal_policy_native (full port of traversal-policy.mjs's 7 POLICY_DEFINITIONS families with task-text auto-selection and legacy's same requested<=fallback clamp semantics), traversal_neighbors_native (bounded multi-seed multi-hop frontier walk mirroring traversalNeighbors), and execute_recall_circuit_with_totals (the existing GC13 BFS/path/compare_paths core, now taking separate pre-cap frontier totals so node/edge-ceiling omissions are computed against the true unbounded frontier size exactly like legacy, not the already-capped graph). query.rs's new recall_op wires these together and replaces Operation::Recall's prior shared-BFS arm; it preserves every existing V1 response field (requestedSeed/resolution/root/direction/nodes/edges/depths/omissions/candidateSet with sourceRef/sourceHash-bearing candidates) and adds two new fields, never removing any (recallCircuit: id/policy/paths/state/omissions/seeds; orientation: action/reasonCode/reason/evidence, a reduced native port of recallOrientation's decision order). Stale-source suppression (staleRow/staleSourcePolicy/suppressRows) is applied to both candidateSet.candidates and recallCircuit.paths, but is driven by two optional request inputs (staleSourcePaths, staleWholeGeneration) rather than a freshness receipt: no field on BlueprintRequest/RequestContext carries one at this layer (freshness/receipts are owned by a different subsystem per docs/agent-rules.md's provider-authority/freshness-distinctness invariant), so a freshness-owning caller supplies the policy instead of this layer inventing one. Regression: engine/crates/membrane-blueprint/tests/parity_recall_operation.rs (4/4 -- candidateSet+circuit-paths production, stale-source suppression removing a path/candidate from both views with a typed omission, a BPT-026 ranking-overrides-BFS-order case proving the store-driven circuit is actually exercised rather than falling back to BFS, and an unresolved-seed abstain case), plus the existing engine/crates/membrane-blueprint/tests/query.rs suite (9/9, including the pre-existing BPT-026 ranking negative control) and the crate's full existing suite pass unaffected except one pre-existing, unrelated flake (parity_application_ops.rs's changes_via_treeish_with_bad_treeish_is_typed_error_and_leaves_no_worktree, a bounded-git-diff-timeout test in an untracked file from a concurrent lane, not touched by this lane). Per-symbol authority ranking within a seed-resolution lane (legacy's symbolAuthorityOrder SQL expression) has no native counterpart at this layer and is replaced with a stable id sort -- a recorded scoping simplification, not silently dropped ranking semantics. Independent of test coverage: caller cutover is still not observed for this cluster (see callerCutover below), so verdict stays insufficient even though Operation::Recall itself is now store-driven and parity-tested.",
    "portedTestNames": [
      "canonical_ignored_dirs_and_files_are_excluded_from_scan",
      "agent_dot_prefixed_directories_are_canonically_ignored",
      "rust_struct_and_impl_method_are_extracted",
      "projection_dag_explicitly_binds_declared_parents",
      "invalidation_closure_is_projection_specific_and_deterministic",
      "projection_cache_invalidates_when_declared_parent_changes",
      "fingerprint_ignores_undeclared_parent_dimensions",
      "fingerprint_reports_unknown_projection",
      "cache_evicts_oldest_entry_once_over_capacity",
      "over_specified_query_still_finds_stored_identifier",
      "document_not_named_by_query_is_never_a_hit",
      "over_specified_query_finds_shorter_identifier_at_two_subtokens",
      "query_naming_nothing_returns_nothing",
      "admission_does_not_depend_on_what_else_is_indexed",
      "shared_path_segment_alone_does_not_qualify",
      "prose_and_single_subtoken_queries_keep_plain_or",
      "exact_name_matches_outrank_partial_ones",
      "tokenizer_splits_camel_snake_and_paths",
      "tokenizer_splits_acronym_runs_from_following_word",
      "acronym_prefixed_symbol_reachable_by_hidden_word",
      "non_ascii_identifiers_survive_tokenization",
      "single_character_identifiers_are_indexed_and_findable",
      "one_character_admission_does_not_depend_on_what_else_is_indexed",
      "incremental_mutation_is_equivalent_to_cold_rebuild",
      "removing_every_document_leaves_index_equivalent_to_empty",
      "index_built_only_from_symbol_like_generation_nodes",
      "empty_or_operator_only_query_returns_nothing",
      "merkle_root_changes_only_when_a_leaf_changes_and_diff_is_exact",
      "delta_built_merkle_root_equals_clean_full_build_root",
      "explicit_marker_wins_regardless_of_edges",
      "entrypoint_label_is_case_insensitive_and_explicit",
      "zero_inbound_outgoing_symbol_is_structural_candidate",
      "node_with_inbound_edge_is_not_a_candidate",
      "non_symbol_kind_never_becomes_a_structural_candidate",
      "structural_candidates_are_suppressed_when_disabled",
      "rows_are_sorted_by_id",
      "evidence_is_carried_through_verbatim",
      "empty_generation_yields_empty_registry",
      "named_and_default_exports_are_closed_and_enumerated",
      "re_export_clause_records_export_and_request",
      "commonjs_module_exports_opens_the_surface",
      "export_assignment_opens_the_surface",
      "ambient_module_declaration_opens_the_surface",
      "destructured_export_opens_the_surface",
      "unsupported_extension_reports_no_language_and_no_findings_material",
      "parse_error_opens_the_surface_rather_than_reporting_partial_exports",
      "imports_are_recorded_as_outbound_requests",
      "filters_by_kind_case_insensitively_including_labels",
      "name_match_is_substring_unless_exact",
      "path_prefix_normalizes_backslashes",
      "declaring_type_matches_prefix_of_qualified_name",
      "label_filter_requires_exact_label_membership",
      "sorts_by_path_then_qualified_name_then_id",
      "limit_truncates_and_records_omission",
      "non_positive_or_missing_limit_falls_back_to_default_and_caps_at_max",
      "edges_are_filtered_by_relation_and_bound_to_selected_nodes",
      "generation_id_and_schema_are_echoed",
      "define_language_table_sorts_extensions_and_defaults_profile",
      "walker_emits_function_nodes_with_evidence_and_confidence",
      "walker_emits_imports_with_unresolved_target_derived_from_specifier",
      "walker_reports_partial_parse_for_error_trees",
      "walker_reports_failed_parse_when_no_root_node",
      "data_profile_table_with_no_function_or_import_patterns_never_fabricates_them",
      "table_declarations_emit_bounded_data_facts_with_structural_evidence",
      "nested_same_name_generic_functions_retain_distinct_qualified_ids",
      "walker_emits_comment_nodes_bound_to_file",
      "semantic_conformance_fixture_passes_exact_import_call",
      "semantic_conformance_failure_names_the_exact_violated_assertion",
      "matches_expected_operators_and_count_rules",
      "null_expected_requires_a_present_json_null_not_a_missing_field",
      "evaluate_freshness_fresh_when_current_matches_indexed_exactly",
      "evaluate_freshness_changed_since_generation_on_revision_drift_is_a_truthful_success",
      "evaluate_freshness_changed_since_generation_on_same_revision_but_dirty_overlay_drift",
      "evaluate_freshness_unknown_when_generation_carries_no_indexed_basis",
      "evaluate_freshness_unavailable_when_current_state_observation_failed",
      "assert_generation_coherence_no_op_when_nothing_pinned_or_pin_matches",
      "assert_generation_coherence_changed_since_generation_is_not_by_itself_a_mismatch",
      "assert_generation_coherence_fails_closed_on_mismatch_even_when_freshness_reports_fresh",
      "assert_generation_coherence_fails_closed_at_unknown_and_unavailable_too",
      "build_freshness_receipt_fresh_requires_no_suppression_and_no_changed_enumeration",
      "build_freshness_receipt_changed_since_generation_reports_suppression_changed_paths_when_complete",
      "build_freshness_receipt_changed_since_generation_falls_back_to_whole_generation_suppression_when_enumeration_incomplete",
      "build_freshness_receipt_unknown_generation_never_enumerates_changed_paths",
      "build_freshness_receipt_unavailable_repo_never_enumerates_changed_paths",
      "contract_registry_and_bridge_stitching_join_only_exact_contract_keys",
      "bridge_stitching_never_joins_same_repo_provider_and_consumer",
      "http_route_kind_and_address_derive_from_method_and_path",
      "node_without_a_recognized_contract_label_is_never_extracted",
      "produces_and_routes_to_edges_grant_the_provider_role",
      "conventions_are_weak_descriptive_evidence_and_retain_counterexamples",
      "test_placement_prefers_the_larger_population_and_records_the_smaller_as_counterexample",
      "module_layout_lists_common_top_level_directories_without_counterexamples",
      "below_minimum_examples_yields_no_evidence_row",
      "only_recognized_config_basenames_are_admitted",
      "digest_is_null_without_any_config_file",
      "adding_tsconfig_changes_the_digest_and_only_config_files_count",
      "digest_is_stable_under_input_reordering_and_path_separator_style",
      "editing_the_tsconfig_content_changes_the_digest",
      "normalize_digest_matches_legacy_prefixing",
      "pending_domains_matches_legacy_watch_state_semantics",
      "replace_by_source_matches_legacy_splice_semantics",
      "replace_by_source_is_a_pure_append_for_a_brand_new_source",
      "replace_by_source_with_empty_replacements_deletes_the_source",
      "freshness_precedes_authority_current_structural_beats_stale_compiler",
      "authority_precedes_inferential_confidence",
      "equal_categorical_authority_with_conflicting_targets_is_frontier",
      "inferential_confidence_is_final_tiebreak_only_within_heuristic_evidence",
      "unknown_never_collapses_to_current",
      "inadmissible_and_wrong_relation_candidates_cannot_participate",
      "legacy_facts_are_mapped_categorically_without_consulting_scalar_confidence",
      "same_target_from_equivalent_evidence_is_admitted_and_retained",
      "optimistic_current_flags_cannot_override_mismatched_source_identity",
      "explicit_stale_observations_dominate_current_flags_and_matching_identity",
      "unknown_or_object_identities_never_become_current_through_string_coercion",
      "unresolved_markers_and_unknown_provenance_cannot_acquire_compiler_authority",
      "required_relationship_and_real_target_cannot_be_replaced_by_edge_id",
      "inference_confidence_rejects_coercible_nonnumbers",
      "null_inferential_confidence_remains_unknown_not_numeric_zero",
      "admission_normalizes_tagged_legacy_confidence_without_mutating_input",
      "lsp_verification_cannot_originate_or_outrank_canonical_graph_fact",
      "coherent_lsp_disagreement_returns_resolution_conflict_without_rewriting_truth",
      "doc_truth_preserves_declaration_and_observed_evidence_with_deterministic_null_confidence",
      "doc_truth_emits_contradicted_ambiguous_unsupported_and_stale",
      "heuristic_doc_relation_may_retain_inferential_confidence",
      "empty_supersedes_and_counts_shape_match_schema",
      "entry_registry_separates_explicit_evidence_from_zero_inbound_structural_candidates",
      "liveness_emits_only_live_unreached_unknown_and_never_calls_zero_inbound_dead",
      "liveness_fails_to_unknown_when_source_or_entrypoint_basis_is_not_trustworthy"
    ],
    "portedTestFile": "engine/crates/membrane-blueprint/tests/parity_graph_core.rs; engine/crates/membrane-blueprint/tests/parity_dependency_dag.rs; engine/crates/membrane-blueprint/tests/parity_bm25_index.rs; engine/crates/membrane-blueprint/tests/parity_merkle_ledger.rs; engine/crates/membrane-blueprint/tests/parity_entry_points.rs; engine/crates/membrane-blueprint/tests/parity_module_surface.rs; engine/crates/membrane-blueprint/tests/parity_ast_structural_search.rs; engine/crates/membrane-blueprint/tests/parity_ast_walker.rs; engine/crates/membrane-blueprint/tests/parity_conformance_verifier.rs; engine/crates/membrane-blueprint/tests/parity_freshness_receipt.rs; engine/crates/membrane-blueprint/tests/parity_contract_registry.rs; engine/crates/membrane-blueprint/tests/parity_conventions.rs; engine/crates/membrane-blueprint/tests/parity_static_provider.rs; engine/crates/membrane-blueprint/tests/parity_delta_store.rs; engine/crates/membrane-blueprint/tests/parity_evidence_authority.rs; engine/crates/membrane-blueprint/tests/parity_doc_truth_projection.rs; engine/crates/membrane-blueprint/tests/parity_liveness.rs",
    "callerCutover": "NOT CUT OVER: scripts/run-m0-baseline-current.mjs still imports blueprint/scripts/release/npm-cli.mjs and runs \"pnpm --dir blueprint test:all\"; scripts/qualification/cases/bpt-windows.mjs still states DELIVERED status is void for the native path per REC-02 and still loads legacy blueprint/ files directly; mcp/capability-inventory.mjs still lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, blueprint/src/graph/resolution/index.mjs as live shipped artifacts. Per NCL-02 a cluster with no caller cutover is rejected regardless of native module existence or new test evidence, so verdict stays insufficient even where ported tests now pass.",
    "status": "INSUFFICIENT"
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/graph/language-tables",
    "verdict": "insufficient",
    "fileCount": 37,
    "nativeDestination": "engine/crates/membrane-blueprint/src/graph.rs (parser_language/ast_facts/walk_ast/fallback_declaration_name) implements 26 of 35 legacy languages after batches 1-3: rust, python, javascript, typescript, tsx, go, java, c, cpp, c_sharp, ruby, php, bash (batch 1) plus elixir, kotlin, lua, scala, swift, zig (batch 2, landed by a prior lane with matching #[test] fixtures already green) plus css, dart, html, json, objc, ocaml, toml, yaml (batch 3, lane GRAM2, this pass) — via tree-sitter-{rust 0.24, python 0.25, javascript 0.25, typescript 0.23, go 0.25, java 0.23, c 0.24, cpp 0.23, c-sharp 0.23, ruby 0.23, php 0.24, bash 0.25, kotlin-ng 1, swift 0.7, scala 0.24, dart 0.0.4, lua 0.4, json 0.24, yaml 0.7, toml-ng 0.7, html 0.23, css 0.23, objc 3, ocaml 0.24, elixir 0.3, zig 1} crates, all compatible with tree-sitter 0.26. GRAM2 found batch-3's 8 grammar crates already correctly pinned and 0.26-compatible; the batch-3 red was not a grammar-version mismatch but a graph.rs extraction gap: walk_ast's declaration matcher and its `node.child_by_field_name(\"name\")` lookup assumed every declaration-shaped node exposes a named `name` field the way rust/go/java do, but css (rule_set/keyframes_statement), html (element), json (pair), toml (pair), yaml (block_mapping_pair), objc (class_interface/class_implementation/method_declaration/method_definition), ocaml (value_definition) and dart (function_signature, confirmed to already carry a name field) do not label their declaration name that way in these grammar versions — confirmed node-for-node against each fixture's actual parse tree, not assumed from the language-table declarations. Fixed by adding element/pair/block_mapping_pair to the declaration matcher and a new fallback_declaration_name(language, kind, node, source) consulted when the direct field lookup misses, recovering each grammar's real name location (css rule_set -> `selectors` child text; css keyframes_statement -> `keyframes_name` child; html element -> `start_tag`'s `tag_name` child; json pair -> `string`'s `string_content` child; toml pair -> `bare_key` child; yaml block_mapping_pair -> first `flow_node` child text; objc class_interface/class_implementation/method_declaration/method_definition -> first direct `identifier` child; ocaml value_definition -> first `value_name` descendant). walk_ast now threads the parser's own language id through to this helper so json's and toml's identically-named `pair` node kind cannot be conflated. The remaining 9 language-table files (elisp, elm, embedded_template, ql, rescript, scala [see note], solidity, systemrdl, tlaplus, vue) plus _factory.mjs still have no native grammar/table; scala is listed under batch 2 above and is NOT in the remaining set — this list is the true 9 unported. Crates.io check for those 9 (2026-09-10, via GET https://crates.io/api/v1/crates/tree-sitter-<lang> and the dependency endpoint for tree-sitter's version requirement on the crate's latest release): tree-sitter-elisp 1.7.2 exists but pins tree-sitter ^0.25.10 (not 0.26-compatible); tree-sitter-elm 5.9.4 exists and pins tree-sitter ^0.26.10 (0.26-COMPATIBLE, ready to add); tree-sitter-embedded-template 0.25.0 exists but pins ^0.25.8 (not compatible); tree-sitter-ql 0.23.1 exists but pins ^0.24 (not compatible); tree-sitter-rescript does not exist on crates.io at all; tree-sitter-solidity 1.2.13 exists but pins ^0.25 (not compatible); tree-sitter-systemrdl does not exist on crates.io at all; tree-sitter-tlaplus 1.5.0 exists but pins ^0.23 (not compatible); tree-sitter-vue 0.0.3 exists but pins ~0.20.3 (not compatible).",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_lang_{go,java,c,cpp,c_sharp,ruby,php,bash,elixir,kotlin,lua,scala,swift,zig,css,dart,html,json,objc,ocaml,toml,yaml,elm,elisp,embedded_template,ql,rescript,solidity,systemrdl,tlaplus,vue}.rs — 2 tests per language for symbol-bearing languages (functions/classes-or-methods extracted by name; CONTAINS edges link file to symbols), 1 test each for the two markup-only languages with no declaration-shaped node kinds in their grammars (embedded_template, vue — clean/non-partial parse asserted instead of fabricated symbols), 62 tests total, all passing locally via rightkit cargo test -p membrane-blueprint.",
    "gap": "All 35 languages now closed (lane GRAM3, this pass, closes the final 9). 26 in batches 1-3 as before (see prior gap text preserved below). Batch 4 / lane GRAM3 this pass closes the remaining 9: elm is native via the tree-sitter-elm 5.9.4 crates.io crate (0.26-compatible) added directly to membrane-blueprint's Cargo.toml, same pattern as batches 1-3, with fallback_declaration_name for its nameless value_declaration (descends into function_declaration_left's lower_case_identifier child) since type_alias_declaration/type_declaration already carry a name field. The other 8 (elisp, embedded_template, ql, rescript, solidity, systemrdl, tlaplus, vue) had no 0.26-compatible crate on crates.io (confirmed again 2026-09-10, unchanged from GRAM2's finding) so a new crate, engine/crates/membrane-grammars-vendored, vendors each grammar's C source under grammars/<lang>/ at the exact commit/tag the legacy JS Blueprint's tree-sitter-wasms@0.1.13 bundle pinned (see engine/crates/membrane-grammars-vendored/VENDORED.md for the full repo/commit/ABI table and provenance notes, including the one upstream regex incompatibility found and fixed in rescript's grammar.js and the systemrdl commit resolved via npm's gitHead since that repo carries no git tags). 4 of the 8 (embedded_template, solidity, tlaplus, vue) shipped a committed src/parser.c at ABI 14, used unmodified; the other 4 (elisp, ql, systemrdl, rescript) had no committed parser.c and were regenerated with tree-sitter-cli 0.26.13 (fetched as the standalone GitHub release binary since npm/npx were not usable in the lane's shell) via `tree-sitter generate` run inside each clone — none needed an ABI-only regen (no grammar in the set was below ABI 13, tree-sitter 0.26's floor). build.rs compiles each grammar's parser.c (+ scanner.c where present) with the `cc` crate (already a transitive workspace dependency at 1.4.4, so no new external dependency was added), exposing `pub fn LANGUAGE` per language module via `tree_sitter_language::LanguageFn::from_raw` over each grammar's `extern \"C\" fn tree_sitter_<lang>()`, verified by the crate's own `all_vendored_grammars_load` test. graph.rs wires all 9 through parser_language/walk_ast/fallback_declaration_name exactly like the batch 1-3 languages: elisp (function_definition, macro_definition — direct name field), ql (module, class — fallback via modulename/classname children, no name field in this grammar), rescript (type_binding, module_binding — direct name field), solidity (function_definition, contract_declaration — direct name field), systemrdl (component_named_def — fallback via the grammar's `id` field, not `name`), tlaplus (module, operator_definition — direct name field). embedded_template and vue are markup-only grammars whose legacy JS table used the generic 'element' fallback that neither grammar's own node kinds actually emit (ERB has no element-shaped declaration at all; Vue's SFC `element`/`component` nodes carry no name-bearing field either) — both are wired for parsing (tree-sitter loads, CONTAINS-eligible AST walk runs) but, matching what the legacy generic table could actually extract from these two grammars, produce zero fabricated symbol names; their tests assert a clean non-partial parse instead. Two of the 8 vendored repos (rescript, tlaplus) also ship an external scanner (scanner.c), compiled alongside parser.c per build.rs's has_scanner flag. NOT CUT OVER: scripts/run-m0-baseline-current.mjs still imports blueprint/scripts/release/npm-cli.mjs and runs \"pnpm --dir blueprint test:all\"; scripts/qualification/cases/bpt-windows.mjs still states DELIVERED status is void for the native path per REC-02 and still loads legacy blueprint/ files directly; mcp/capability-inventory.mjs still lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, blueprint/src/graph/resolution/index.mjs as live shipped artifacts. Per NCL-02 a cluster with no caller cutover is rejected regardless of native module existence or new test evidence, so verdict stays insufficient even though all 35 languages now have passing native ports — the JS Blueprint deletion and caller cutover are separate, still-open work this lane did not touch (out of GRAM3's file scope).",
    "portedTestNames": [
      "rust_struct_and_impl_method_are_extracted",
      "go_functions_and_types_are_extracted",
      "go_contains_edges_link_file_to_symbols",
      "java_class_and_methods_are_extracted",
      "java_contains_edges_link_file_to_symbols",
      "c_functions_and_structs_are_extracted",
      "c_contains_edges_link_file_to_symbols",
      "cpp_functions_and_classes_are_extracted",
      "cpp_contains_edges_link_file_to_symbols",
      "c_sharp_class_and_methods_are_extracted",
      "c_sharp_contains_edges_link_file_to_symbols",
      "ruby_class_and_methods_are_extracted",
      "ruby_contains_edges_link_file_to_symbols",
      "php_functions_and_classes_are_extracted",
      "php_contains_edges_link_file_to_symbols",
      "bash_functions_are_extracted",
      "bash_contains_edges_link_file_to_symbols",
      "elixir_functions_are_extracted",
      "elixir_contains_edges_link_file_to_symbols",
      "kotlin_functions_and_classes_are_extracted",
      "kotlin_contains_edges_link_file_to_symbols",
      "lua_functions_are_extracted",
      "lua_contains_edges_link_file_to_symbols",
      "scala_classes_and_methods_are_extracted",
      "scala_contains_edges_link_file_to_symbols",
      "swift_functions_and_classes_are_extracted",
      "swift_contains_edges_link_file_to_symbols",
      "zig_functions_are_extracted",
      "zig_contains_edges_link_file_to_symbols",
      "css_parses_without_error",
      "css_contains_edges_link_file_to_symbols",
      "dart_functions_are_extracted",
      "dart_contains_edges_link_file_to_symbols",
      "html_parses_without_error",
      "html_contains_edges_link_file_to_symbols",
      "json_parses_without_error",
      "json_contains_edges_link_file_to_symbols",
      "objc_class_and_methods_are_extracted",
      "objc_contains_edges_link_file_to_symbols",
      "ocaml_values_are_extracted",
      "ocaml_contains_edges_link_file_to_symbols",
      "toml_parses_without_error",
      "toml_contains_edges_link_file_to_symbols",
      "yaml_parses_without_error",
      "yaml_contains_edges_link_file_to_symbols",
      "elm_values_are_extracted",
      "elm_contains_edges_link_file_to_symbols",
      "elisp_symbols_are_extracted",
      "elisp_contains_edges_link_file_to_symbols",
      "embedded_template_parses_without_errors",
      "ql_declarations_are_extracted",
      "ql_contains_edges_link_file_to_symbols",
      "rescript_declarations_are_extracted",
      "rescript_contains_edges_link_file_to_symbols",
      "solidity_declarations_are_extracted",
      "solidity_contains_edges_link_file_to_symbols",
      "systemrdl_declarations_are_extracted",
      "systemrdl_contains_edges_link_file_to_symbols",
      "tlaplus_declarations_are_extracted",
      "tlaplus_contains_edges_link_file_to_symbols",
      "vue_parses_and_contains_elements"
    ],
    "portedTestFile": "engine/crates/membrane-blueprint/tests/parity_graph_core.rs (rust); tests/graph.rs already covered python (native_generation_is_stable_and_has_registered_edges) and javascript/typescript/tsx (module_surface_* tests) prior to this pass; engine/crates/membrane-blueprint/tests/parity_lang_go.rs, parity_lang_java.rs, parity_lang_c.rs, parity_lang_cpp.rs, parity_lang_c_sharp.rs, parity_lang_ruby.rs, parity_lang_php.rs, parity_lang_bash.rs (batch 1); parity_lang_elixir.rs, parity_lang_kotlin.rs, parity_lang_lua.rs, parity_lang_scala.rs, parity_lang_swift.rs, parity_lang_zig.rs (batch 2, prior lane, confirmed green this pass); parity_lang_css.rs, parity_lang_dart.rs, parity_lang_html.rs, parity_lang_json.rs, parity_lang_objc.rs, parity_lang_ocaml.rs, parity_lang_toml.rs, parity_lang_yaml.rs (batch 3, lane GRAM2); parity_lang_elm.rs, parity_lang_elisp.rs, parity_lang_embedded_template.rs, parity_lang_ql.rs, parity_lang_rescript.rs, parity_lang_solidity.rs, parity_lang_systemrdl.rs, parity_lang_tlaplus.rs, parity_lang_vue.rs (batch 4, lane GRAM3, this pass — new membrane-grammars-vendored crate for 8 of the 9, tree-sitter-elm crate dependency for the 9th)",
    "callerCutover": "NOT CUT OVER: scripts/run-m0-baseline-current.mjs still imports blueprint/scripts/release/npm-cli.mjs and runs \"pnpm --dir blueprint test:all\"; scripts/qualification/cases/bpt-windows.mjs still states DELIVERED status is void for the native path per REC-02 and still loads legacy blueprint/ files directly; mcp/capability-inventory.mjs still lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, blueprint/src/graph/resolution/index.mjs as live shipped artifacts. Per NCL-02 a cluster with no caller cutover is rejected regardless of native module existence or new test evidence, so verdict stays insufficient even where ported tests now pass.",
    "status": "INSUFFICIENT"
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/graph/resolution",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib (misc)",
    "verdict": "insufficient",
    "fileCount": 20,
    "nativeDestination": "Of 18 legacy files, 6 have a confirmed native counterpart, ALL already covered by pre-existing native tests (not touched by this pass, other lanes own those files): evidence-pack.mjs -> export.rs/findings.rs build_evidence_pack (tests/export.rs), sarif.mjs -> export.rs findings_to_sarif (tests/export.rs), session-token.mjs -> security.rs (tests/security.rs), snapshot.mjs -> graph.rs/freshness.rs/watch.rs/service.rs (tests/watch.rs, tests/service.rs, tests/contracts.rs), incremental-phase2.mjs -> phase2.rs (tests/phase2.rs), telemetry.mjs -> index.rs (indirectly; covered via tests/query.rs and tests/graph.rs exercising the index it backs, no telemetry-named test found). No native counterpart found for the remaining 12: admission.mjs, comment-claims.mjs, generated-docs.mjs, http-server.mjs, orientation-evidence.mjs, path-confinement.mjs, phase2-completion.mjs, receipt-store.mjs, redaction.mjs, run-ledger.mjs, runtime-capabilities.mjs, token-budget.mjs. | LANE LIB1 (2026-09-10) ported path-confinement.mjs -> engine/crates/membrane-blueprint/src/lib_path_confinement.rs (resolve_physical_path, is_confined_path), 7 passing parity tests in tests/parity_lib_path_confinement.rs. Remaining 11 of the 12 no-native files (admission, comment-claims, generated-docs, http-server, orientation-evidence, phase2-completion, receipt-store, redaction, run-ledger, runtime-capabilities, token-budget) are still unported.",
    "nativeTests": null,
    "gap": "No new test was ported for this cluster: every legacy file with a confirmed native counterpart (6 of 18) already had non-vacuous native test coverage before this pass, owned by other lanes and left untouched per lane rules. The other 12 files have no native implementation at all, this is the true remaining port (admission, comment-claims, generated-docs, http-server, orientation-evidence, path-confinement, phase2-completion, receipt-store, redaction, run-ledger, runtime-capabilities, token-budget). NOT CUT OVER: scripts/run-m0-baseline-current.mjs still imports blueprint/scripts/release/npm-cli.mjs and runs \"pnpm --dir blueprint test:all\"; scripts/qualification/cases/bpt-windows.mjs still states DELIVERED status is void for the native path per REC-02 and still loads legacy blueprint/ files directly; mcp/capability-inventory.mjs still lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, blueprint/src/graph/resolution/index.mjs as live shipped artifacts. Per NCL-02 a cluster with no caller cutover is rejected regardless of native module existence or new test evidence, so verdict stays insufficient even where ported tests now pass. UPDATE (LANE LIB1, 2026-09-10): path-confinement.mjs now has a native Rust port with passing parity tests (see nativeDestination). Verdict stays insufficient overall because (a) the other 11 no-native-equivalent misc files remain unported, and (b) caller cutover for the blueprint/src/lib tree as a whole is still NOT observed -- scripts/run-m0-baseline-current.mjs still runs pnpm --dir blueprint test:all against the legacy JS, so the new Rust module has no live caller yet. UPDATE (LANE LIB4, 2026-09-10): all 11 remaining no-native-equivalent misc files now have native Rust ports with passing parity tests -- admission -> lib_admission.rs, comment-claims -> lib_comment_claims.rs, generated-docs -> lib_generated_docs.rs, http-server -> lib_http_server.rs (pure route/auth-gate functions only; membrane-runtime's http_server.rs is a differently-scoped axum/hyper HMAC loopback server for the Hub health endpoint with no route/auth overlap, confirmed by direct read before porting), orientation-evidence -> lib_orientation_evidence.rs, phase2-completion -> lib_phase2_completion.rs (pure generation-fencing decision logic only; the SQLite/delta-store transaction orchestration depends on sibling modules outside this lane's scope and on store.rs/graph.rs schema, which lane rules forbid touching), receipt-store -> lib_receipt_store.rs, redaction -> lib_redaction.rs, run-ledger -> lib_run_ledger.rs, runtime-capabilities -> lib_runtime_capabilities.rs (only the portable semver-floor logic; the Node-runtime-version check itself does not apply to a native binary), and the chars-per-token budget module -> lib_token_budget.rs. All ported via new files only, one append-only `pub mod` line each at the end of src/lib.rs, no new crate dependencies (HMAC-SHA256, SHA-1, and a v4-shaped random id were hand-rolled against existing sha2/getrandom deps where the legacy module needed node:crypto primitives not otherwise available). -p membrane-blueprint full suite (unit + all parity_* integration tests, ~140+ lib tests plus one parity file per module) passes locally under rightkit cargo test on x86_64-pc-windows-msvc. Verdict STILL stays insufficient: caller cutover for the blueprint/src/lib tree as a whole remains NOT observed -- scripts/run-m0-baseline-current.mjs still runs pnpm --dir blueprint test:all against the legacy JS, so none of these new Rust modules (path-confinement included) have a live production caller yet. Native existence and new parity-test evidence do not themselves satisfy NCL-02's cutover requirement. Lane CRYPTO 2026-09-10: within this cluster, five files (comment-claims.mjs, init/state-integrity.mjs, receipt-store.mjs, run-ledger.mjs, update/manifest.mjs) that LIB3/LIB4 had ported with hand-rolled crypto primitives (believing no crypto crates were available) were revisited after ring/hmac were added as real dependencies to engine/crates/membrane-blueprint/Cargo.toml. update/manifest.mjs verify_signed_manifest_structural now performs real Ed25519 verification via ring::signature (previously stubbed to SignatureCheck::CryptoUnavailable, never returning Ok); the hand-rolled HMAC-SHA256 in init/state-integrity.mjs, receipt-store.mjs, and run-ledger.mjs was replaced with ring::hmac, and the hand-rolled SHA-1 in comment-claims.mjs was replaced with ring::digest::SHA1_FOR_LEGACY_USE_ONLY. All five hand-rolled HMAC/SHA-1 constructions were cross-checked against the ring-based replacement on RFC 4231 / Node-crypto known-answer vectors and agreed byte-for-byte (no correctness bug found in the prior hand-rolled code); the Ed25519 stub was a true capability gap (never verified anything), now closed. Known-answer tests added to the parity test files for all five (plus an in-module crypto_tests mod for lib_receipt_store.rs, whose HMAC signing is behind private helpers). This does not change the cluster caller-cutover verdict above.",
    "portedTestNames": [],
    "portedTestFile": null,
    "callerCutover": "NOT CUT OVER: scripts/run-m0-baseline-current.mjs still imports blueprint/scripts/release/npm-cli.mjs and runs \"pnpm --dir blueprint test:all\"; scripts/qualification/cases/bpt-windows.mjs still states DELIVERED status is void for the native path per REC-02 and still loads legacy blueprint/ files directly; mcp/capability-inventory.mjs still lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, blueprint/src/graph/resolution/index.mjs as live shipped artifacts. Per NCL-02 a cluster with no caller cutover is rejected regardless of native module existence or new test evidence, so verdict stays insufficient even where ported tests now pass.",
    "status": "INSUFFICIENT"
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/application",
    "verdict": "insufficient",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_lib_application_errors_normalize.rs, parity_lib_application_root_registry.rs (service.mjs has no test: blocked)",
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). LANE LIB2 (2026-09-10) per-file disposition for this cluster's 4 files (see audit/qualification/windows-r5/ncl-02/lib-inventory.json for full evidence): errors.mjs -- native-equivalent confirmed via a newly-ported standalone module engine/crates/membrane-blueprint/src/lib_application_errors.rs (no prior native equivalent existed), proven by tests/parity_lib_application_errors_normalize.rs (4 passing cases). normalize.mjs -- same disposition, lib_application_normalize.rs, same test file (4 more passing cases). root-registry.mjs -- native-equivalent confirmed via a newly-ported lib_application_root_registry.rs built on the pre-existing crate::identity::repository_identity, proven by tests/parity_lib_application_root_registry.rs (7 passing cases ported from blueprint/tests/root-registry.test.mjs). service.mjs -- BLOCKED, not ported: an 860-line orchestrator over ~30 other legacy modules mostly outside this lane's scope; engine.rs's NativeBlueprintOperation dispatch is a genuine partial behavioral match for the search/resolve/recall/expand/impact/path/architecture/findings/build/refresh/status surface, but the resident WAL-pinned freshness session, build-singleflight join, snapshot/changes lanes, and architecture flow-cursor pagination are not confirmed native and were not ported within budget. This replaces the prior generic 'no file-level mapping confirmed' placeholder for 3 of these 4 files with proven-and-tested native equivalents; the 4th (service.mjs) remains genuinely unresolved and is recorded as such, not glossed over."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/explorer",
    "verdict": "insufficient",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_lib_explorer.rs (index.mjs has no test: blocked)",
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). LANE LIB2 (2026-09-10) per-file disposition for this cluster's 3 files: layout.mjs -- native-equivalent confirmed via newly-ported engine/crates/membrane-blueprint/src/lib_explorer_layout.rs (spherical_layout, project_orb), proven by tests/parity_lib_explorer.rs (4 passing cases, including the ported blueprint/tests/explorer-layout.test.mjs scenario). static.mjs -- same disposition, lib_explorer_static.rs (byte-identical HTML/CSS/JS payloads, identical routing/headers), same test file. index.mjs -- BLOCKED, not ported: its two dependencies (http-server.mjs's actual socket-binding/bearer-token server, and the blocked application/service.mjs) are both out of this lane's reach; the pure layout/static-asset logic it composes is what got ported above. This replaces the prior generic placeholder for 2 of these 3 files with proven-and-tested native equivalents; index.mjs's server-binding half remains genuinely unresolved. LANE LIB5 (2026-09-10) UPDATE on application/service.mjs and explorer/index.mjs: ran a full operation-by-operation audit against the native dispatch surface, recorded in audit/qualification/windows-r5/ncl-02/application-parity.json. Of service.mjs's 16 operations, 10 are now confirmed equivalent (engine.rs's NativeBlueprintOperation dispatch into query.rs, not merely 'partial correspondence' as LIB2's initial pass recorded), 1 (documentTruth) was ported this lane into new file engine/crates/membrane-blueprint/src/lib_application_document_truth.rs (wired via one new Operation::DocumentTruth arm in engine.rs; 5 passing unit tests), and 4 (snapshot_get, snapshot_list, changes, federate) remain confirmed MISSING with named shipped callers and precise evidence for why each is blocked (snapshot/changes: no `named_snapshot` SQLite table exists anywhere in store.rs; federate: Operation::Federate is declared but has no engine.rs dispatch arm and depends on the same missing snapshot infra). index.mjs's disposition is unchanged by this update (still blocked on http-server.mjs's live socket binding). The blueprint/ tree's caller-cutover status recorded at the top of this cluster entry is unchanged by this lane's work. LANE STORE1 (2026-09-10) UPDATE on application/service.mjs's snapshot_get/snapshot_list/changes/federate (distinct from blueprint/src/lib/federation/index.mjs, which LIB2 already ported into engine/crates/membrane-federation/src/lib_federation_index.rs): all 4 operations are now ported into engine/crates/membrane-blueprint (new files lib_application_snapshots.rs, lib_application_federate.rs; named_snapshot SQLite table already existed in migrations.rs schema v17 with no read/write path -- one added), wired via engine.rs Operation::SnapshotGet/SnapshotList/Changes/Federate dispatch arms, proven by tests/parity_application_ops.rs (10 passing cases). See application-parity.json for full per-operation evidence and this lane's scoped deviations (generation_leaf-vs-files-table leaf digest source, semanticDelta not ported, federate's contractBridges/traces not wired to membrane-federation's already-ported stitching). The blueprint/ tree's caller-cutover status recorded at the top of this cluster entry is unchanged by this lane's work. LANE STORE2 (2026-09-10) UPDATE closing STORE1's 4 named gaps on the same 4 operations: (1) semanticDelta -- lib_application_snapshots.rs now ports semantic_graph_from_generation/semantic_delta (node/edge fingerprint diff, mirroring snapshots.mjs semanticGraphFromGeneration/semanticDelta) for the snapshot and generation reference kinds; treeish still reports semanticDelta: null (treeishSemanticGraph's blob materialisation remains unported, named in omissions). (2) generation_leaf -- engine.rs build_and_publish now calls merkle_ledger::compute_full_ledger from the files table on every publish, so generation_leaf is populated for the full-build path (it was already populated for the incremental watch path via merkle_ledger::update_leaf_chain); identity_from_store reads leaf digests from generation_leaf WHERE kind='file' now, not files. (3) sourceObservation.head/dirty -- build_and_publish now calls current_git_identity(root) once and persists {head,dirty} inside Generation::source_observation; identity_from_store reads it back instead of recomputing live. (4) federate contractBridges/traces -- lib_application_federate.rs now loads each federated repository's own persisted generation and calls the crate's own contract_registry::build_contract_registry/stitch_contract_traces directly (the same native port membrane-federation's lib_federation_index.rs already calls; membrane-blueprint cannot depend on membrane-federation, which depends on it). All 4 operations remain wired through the same engine.rs dispatch arms STORE1 added; no new Operation variants or dispatch arms were needed. Proven by tests/parity_application_ops.rs (14 passing cases, up from 10 -- 4 new tests: generation_leaf_table_is_populated_on_publish_and_sourced_by_snapshots, snapshot_source_observation_is_persisted_not_recomputed_live, changes_via_current_generation_reports_empty_semantic_delta_with_no_changes, federate_stitches_contract_bridges_from_each_repos_own_generation, plus updated assertions on the pre-existing changes_since_snapshot_reports_added_modified_deleted_leaves case). See application-parity.json rows 11-14 for full per-operation evidence. The blueprint/ tree's caller-cutover status recorded at the top of this cluster entry is unchanged by this lane's work."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/federation",
    "verdict": "parity-proven",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": "engine/crates/membrane-federation/tests/parity_lib_federation_index.rs",
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). LANE LIB2 (2026-09-10) disposition for this cluster's 1 file (index.mjs): ported faithfully as engine/crates/membrane-federation/src/lib_federation_index.rs (define_federation_group, compose_federated_slices, is_repo_allowed, route_federated_query) -- no native equivalent existed. Built on the pre-existing membrane_blueprint::contract_registry::stitch_contract_traces (itself the confirmed native port of graph/contract-registry.mjs's stitchContractTraces, which the legacy federation module also calls), so exact-key-only contract bridging semantics are inherited unchanged. Proven by engine/crates/membrane-federation/tests/parity_lib_federation_index.rs, porting all 3 tests from blueprint/tests/federation-groups-contracts.test.mjs verbatim; all 3 pass. This resolves the entire cluster (fileCount 1) from the prior generic placeholder to a proven-and-tested native equivalent."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/findings",
    "verdict": "parity-proven",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_lib_findings_detect.rs, parity_lib_findings_specifier.rs",
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). LANE LIB2 (2026-09-10) per-file disposition for this cluster's 4 files: all four CONFIRMED native-equivalent against the pre-existing engine/crates/membrane-blueprint/src/findings.rs (BP001/BP002/BP003 detection, the findings.* method dispatch, and baseline capture/list/delta), which this lane verified line-by-line against detect.mjs, registry.mjs, and service.mjs and proved with a new test file, tests/parity_lib_findings_detect.rs (4 passing cases ported from blueprint/tests/findings-detect.test.mjs: clean-repo zero findings, BP001, BP002, registry rule ids). One documented, uncorrected gap: findings.rs's own inline specifier-candidate resolver does not strip an explicit .js/.jsx/.mjs/.cjs extension the way the true specifier-resolution owner does (see next line), so a .js-suffixed specifier against a .ts file can spuriously fire BP002 natively where the legacy detector resolves cleanly -- recorded, not silently patched (would be a behavior change to findings.rs outside this lane's porting/parity-proving charter). specifier.mjs (this cluster's remaining file per lib-inventory.json's finer split) -- also CONFIRMED, but not via findings.rs: it is a zero-logic re-export of blueprint/src/graph/resolution/index.mjs, which had no native port before this lane and is now newly ported as engine/crates/membrane-blueprint/src/lib_findings_specifier.rs, proven by tests/parity_lib_findings_specifier.rs (5 passing cases ported from blueprint/tests/resolution-owner.test.mjs). This replaces the prior generic 'no file-level mapping confirmed' placeholder for the whole cluster with proven-and-tested native equivalents for all 4 files; the fileCount:4 here reflects detect/registry/service/specifier as originally scoped."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/hooks",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). UPDATE (LANE LIB3, 2026-09-10): the sole file in this cluster (blueprint-graph-index.mjs) was ported to engine/crates/membrane-blueprint/src/lib_hooks_graph_index.rs with a parity test at tests/parity_lib_hooks_graph_index.rs (both compile and pass under -p membrane-blueprint). Verdict stays insufficient per NCL-02: caller cutover for the blueprint/ tree as a whole is still not observed (see UPDATE above from LANE LIB1), so a passing native test does not establish parity while a live caller still reaches the legacy path. See lib-inventory.json for the per-file nativeEquivalent record and portLocation."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/init",
    "verdict": "insufficient",
    "fileCount": 5,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). UPDATE (LANE LIB3, 2026-09-10): all 5 files in this cluster were ported to engine/crates/membrane-blueprint/src/lib_init_*.rs (detect_hosts, host_configs, plan, state_integrity fully; apply.mjs scoped to its deterministic core -- see lib-inventory.json notes for the scope limitation on the OS-transaction orchestration). Each has a parity test under tests/parity_lib_init_*.rs; all compile and pass under -p membrane-blueprint. Verdict stays insufficient per NCL-02 (caller cutover for the blueprint/ tree is still not observed; see UPDATE above from LANE LIB1). See lib-inventory.json for per-file nativeEquivalent records and portLocations."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/languages",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). UPDATE (LANE LIB3, 2026-09-10): the sole file in this cluster (custom-config.mjs) was ported to engine/crates/membrane-blueprint/src/lib_languages_custom_config.rs with a parity test at tests/parity_lib_languages_custom_config.rs (both compile and pass under -p membrane-blueprint). Built-in language id/extension sets are caller-supplied parameters since no native language-capability-registry export exists to source them from. Verdict stays insufficient per NCL-02 (caller cutover for the blueprint/ tree is still not observed; see UPDATE above from LANE LIB1). See lib-inventory.json for the per-file nativeEquivalent record and portLocation."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/operations",
    "verdict": "insufficient",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). UPDATE (LANE LIB3, 2026-09-10): all 3 files in this cluster were ported to engine/crates/membrane-blueprint/src/lib_operations_*.rs (repair.mjs and support-bundle.mjs fully; doctor.mjs's deterministic reason-code core and MCP liveness probe -- membrane-runtime/src/doctor.rs was read in full and confirmed NOT a match, see lib-inventory.json). Each has a parity test under tests/parity_lib_operations_*.rs; all compile and pass under -p membrane-blueprint. Verdict stays insufficient per NCL-02 (caller cutover for the blueprint/ tree is still not observed; see UPDATE above from LANE LIB1). See lib-inventory.json for per-file nativeEquivalent records and portLocations."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/rules",
    "verdict": "insufficient",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). UPDATE (LANE LIB3, 2026-09-10): all 4 files in this cluster were ported to engine/crates/membrane-blueprint/src/lib_rules_*.rs (parser, exceptions, baseline, evaluate -- evaluate.mjs reuses the existing FACT_PROVENANCE_RULE_RESOLVED constant from evidence_authority.rs rather than redefining it). Each has a parity test under tests/parity_lib_rules_*.rs; all compile and pass under -p membrane-blueprint. Verdict stays insufficient per NCL-02 (caller cutover for the blueprint/ tree is still not observed; see UPDATE above from LANE LIB1). See lib-inventory.json for per-file nativeEquivalent records and portLocations."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/lib/update",
    "verdict": "insufficient",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either. UPDATE (LANE LIB1, 2026-09-10): lane produced a file-level inventory for this subfolder at audit/qualification/windows-r5/ncl-02/lib-inventory.json (exports, best-effort native-equivalent check, shipped-caller notes). No modules in this subfolder were ported within the lane time budget; all files here remain unported (not started). See lib-inventory.json for per-file detail and honest confidence notes (basename grep on generic names such as errors/index/service was too noisy to report caller mappings reliably for most files in this batch). UPDATE (LANE LIB3, 2026-09-10): all 4 code files in this cluster were ported to engine/crates/membrane-blueprint/src/lib_update_*.rs (channel.mjs fully; manifest.mjs's non-crypto surface fully plus a fail-closed structural-only signature check -- see lib-inventory.json for the explicit Ed25519-crate-unavailable scope limitation; apply.mjs and rollback.mjs scoped to their deterministic digest/durable-write cores, with rollback.mjs reusing apply.mjs's copy_recursive rather than duplicating it). trusted-update-keys.json is a data fixture with no code to port. Each ported file has a parity test under tests/parity_lib_update_*.rs; all compile and pass under -p membrane-blueprint. Verdict stays insufficient per NCL-02 (caller cutover for the blueprint/ tree is still not observed; see UPDATE above from LANE LIB1). See lib-inventory.json for per-file nativeEquivalent records and portLocations."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/mcp",
    "verdict": "insufficient",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers (misc)",
    "verdict": "insufficient",
    "fileCount": 5,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/bridges",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/compilers",
    "verdict": "insufficient",
    "fileCount": 2,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/frameworks",
    "verdict": "insufficient",
    "fileCount": 2,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/iac",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/modules",
    "verdict": "parity-proven-partial",
    "fileCount": 2,
    "nativeDestination": "engine/crates/membrane-blueprint/src/module_resolution.rs (wired via graph.rs's import_specifiers/resolve_import aliases, lane P4 2026-09-10)",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_module_resolution_wired.rs",
    "gap": "Lane P4 (2026-09-10): graph.rs's build pass now calls module_resolution::{import_specifiers_in_files, resolve_import_in_files} instead of its own private duplicates -- no longer dead code. These are new disk-free adapters (not resolve_js_module/resolve_python_module directly) because graph.rs's build pass works over an in-memory FileRecord map, not the filesystem; the pre-existing filesystem-based functions' typed-Ambiguous / stdlib / pyproject.toml-root features remain unreachable from the native build pass. See providers-parity.json rows for javascript.mjs/python-resolver.mjs for full detail."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/ranking",
    "verdict": "parity-proven",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint/src/providers/ranking.rs (lane P4 2026-09-10)",
    "nativeTests": "engine/crates/membrane-blueprint/src/providers/ranking.rs (in-module tests)",
    "gap": "Fully ported: both RANKING_TYPES entries plus a typed parse() that returns None for unknown roles rather than a silent default."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/providers/schemas",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/service",
    "verdict": "insufficient",
    "fileCount": 9,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/src/sources",
    "verdict": "insufficient",
    "fileCount": 9,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "blueprint-repair",
    "cluster": "blueprint/watchman",
    "verdict": "insufficient",
    "fileCount": 4,
    "nativeDestination": "engine/crates/membrane-blueprint (partial/uncertain — no file-level mapping confirmed for this cluster; native crate has 20 src files vs 234 legacy files across 32 clusters, so most legacy surface, including all 37 language-tables/*.mjs grammars, the 15 release scripts, and the 3 scripts/ci helpers, has no identified native counterpart at all)",
    "nativeTests": null,
    "gap": "Caller cutover is not observed for this cluster (or for the blueprint/ tree generally): scripts/run-m0-baseline-current.mjs still executes \"pnpm --dir blueprint test:all\" via blueprint/scripts/release/npm-cli.mjs, scripts/qualification/cases/bpt-windows.mjs explicitly states DELIVERED status is void for the native path per REC-02 until engine/crates/membrane-blueprint lands equivalent behavior and still loads files from this legacy tree directly, mcp/capability-inventory.mjs lists blueprint/src/lib/findings/detect.mjs, blueprint/src/graph/module-surface.mjs, and blueprint/src/graph/resolution/index.mjs as the live artifacts backing shipped capabilities, and released-build SBOM/runtime-language-manifest evidence under docs/evidence/releases/ still enumerates blueprint/ as shipped runtime. Per NCL-02, a cluster with no caller cutover is rejected regardless of native module existence or test evidence, so no parity-proven verdict is claimed for any file in this cluster. No passing I1 test name is cited as proving replacement because doing so would not establish parity while a live caller still reaches the legacy path — the negative control in NCL-02 (vacuous/incomplete assertion does not count) is read here to mean incomplete caller-cutover evidence does not count either."
  },
  {
    "owner": "public-runtime",
    "cluster": "continuity-transcript-core",
    "verdict": "insufficient",
    "fileCount": 5,
    "nativeDestination": "engine/crates/membrane-transcript (adapters.rs, canonical.rs, classify.rs, error.rs, event.rs, evidence.rs, parser.rs, redact.rs, source.rs; 3472 total lines)",
    "nativeTests": null,
    "callerCutover": {
      "checkedAgainst": "adapt/src/adapt/*.py (live source; __pycache__/*.pyc excluded as stale)",
      "liveImporters": [
        "adapt/src/adapt/mine_snapshot_manifest.py:26 -- from continuity.transcript import parse_source_events",
        "adapt/src/adapt/transcript_snapshots.py:20 -- from continuity.transcript import compact_text, parse_source_events"
      ],
      "importChain": "mine_snapshot_manifest.py:23 imports transcript_snapshots, so the two live importers form one call chain, not two independent ones",
      "staleClaimCorrected": "adapt/src/adapt/insights.py and adapt/src/adapt/taste_v2_pipeline.py no longer exist as source files (only stale __pycache__/*.pyc remain, predating a rewrite onto engine/crates/membrane-adapt/src/insights/mod.rs and taste.rs); they are not live importers. The original 'six live Adapt production modules' claim is not supported by current source -- only 2 files (one import chain) remain",
      "verdict": "still no FFI/subprocess bridge from these 2 files to membrane-transcript; caller cutover not done"
    },
    "gap": "Two live Adapt production modules (mine_snapshot_manifest.py, and transcript_snapshots.py which it imports) still import continuity.transcript directly; deleting driver.py/host_adapters.py/__init__.py would break them at import time. No Rust-backed replacement is wired into Adapt's Python call sites (no FFI/subprocess bridge to membrane-transcript found from adapt/src/adapt/*.py). Needed before parity: (a) Adapt's Python callers switched to call the compiled membrane-transcript binary/FFI instead of importing continuity.transcript, (b) a passing test that exercises that switched call path, (c) then this file group becomes a caller-cutover candidate."
  },
  {
    "owner": "public-runtime",
    "cluster": "continuity-transcript-tests",
    "verdict": "insufficient",
    "fileCount": 3,
    "nativeDestination": "n/a (these are tests of continuity-transcript-core, not production code)",
    "nativeTests": null,
    "gap": "Cannot be deleted before continuity-transcript-core is cut over; deleting them first would strip coverage from code six live Adapt modules still depend on."
  },
  {
    "owner": "public-runtime",
    "cluster": "mcp-server-entrypoint-and-runtime",
    "verdict": "insufficient",
    "fileCount": 37,
    "nativeDestination": "engine/crates/membrane-mcp (discovery.rs, http_security.rs, jsonrpc.rs, prompts.rs, resources.rs, tools.rs, lib.rs; 1860 total lines) launched via mcp/membrane-mcp-native.mjs -> engine/target/.../membrane stdio-mcp",
    "nativeTests": null,
    "callerCutover": {
      "referencesEnumerated": [
        {
          "ref": "mcp.json (repo root) and installed-product mcp.json (versions/0.1.24/mcp.json under C:/Users/adrds/AppData/Local/Orthic Labs/Membrane)",
          "callsLegacy": false,
          "servedByNative": true,
          "note": "mcpServers.membrane already points to command 'membrane' args ['stdio-mcp'] -- the native binary, not mcp/server.mjs"
        },
        {
          "ref": ".claude-plugin/plugin.json, .codex-plugin/plugin.json, .antigravity-plugin/plugin.json (repo root and installed versions/0.1.24)",
          "callsLegacy": false,
          "servedByNative": "n/a",
          "note": "no mcp/server.mjs or mcp/host reference found in any plugin.json variant, installed or in-tree"
        },
        {
          "ref": "package.json scripts.test, scripts.\"test:mcp\", scripts.\"test:fable\"",
          "callsLegacy": true,
          "servedByNative": false,
          "note": "all three run `node --test mcp/*.test.mjs mcp/host/*.test.mjs` -- the legacy test harness is still an active, non-optional dependency of the shipped test/CI gate"
        },
        {
          "ref": ".github/workflows/ledger-completion.yml:41-42",
          "callsLegacy": true,
          "servedByNative": false,
          "note": "CI step runs `node --check mcp/server.mjs` and `node --check mcp/toolsets.mjs` -- a real build-time syntax-check dependency on the legacy file, contradicting interpreter-dispositions.json's 'dev-only' label"
        },
        {
          "ref": "apps/membrane-hub/scripts/runtime-inventory.mjs",
          "callsLegacy": false,
          "servedByNative": "n/a",
          "note": "no mcp/server.mjs or mcp/host reference found in this script"
        },
        {
          "ref": "apps/membrane-hub/tests/runtime-inventory.test.mjs:124",
          "callsLegacy": false,
          "servedByNative": true,
          "note": "asserts the runtime manifest contains NO entry whose source is .py or includes 'mcp/host' -- positive evidence the shipped inventory is native-only"
        },
        {
          "ref": "mcp/capability-inventory.mjs (self) and its consumers .agent/graph/parse-cache/records.json, .audit/2026-09-02-wiring-full/blueprint/graph/parse-cache/records.json, migration/native-rust/executable-ledger.json",
          "callsLegacy": "indirect",
          "servedByNative": "n/a",
          "note": "these are Blueprint's own parsed-file graph/ledger records that list mcp/server.mjs as a parsed source, not a functional caller invoking its behavior at runtime"
        },
        {
          "ref": "scripts/tools/productization/generate-product-truth.mjs:6, scripts/tools/productization/render-docs.mjs:8",
          "callsLegacy": false,
          "servedByNative": "n/a",
          "note": "only stale comments mention mcp/server.mjs / capability-matrix.v1.json; grep for actual import/require of mcp/server.mjs in these files found none -- the doc-generation claim in the prior gap text is not supported by current source, only by comment text"
        },
        {
          "ref": "scripts/qualification/cases/psh-windows.mjs:565",
          "callsLegacy": false,
          "servedByNative": "n/a",
          "note": "treats legacy mcp/host/push-tool-egress.mjs evidence as already void (PSH-I027, REC-02); it discounts the legacy path rather than calling it"
        },
        {
          "ref": "release SBOM: scripts/release/evidence/sbom.mjs and generated docs/evidence/releases/{92d72b80,ee9cdfd9}-windows-x86_64/sbom.json",
          "callsLegacy": false,
          "servedByNative": true,
          "note": "grep for mcp/server.mjs or mcp/host in both generated release SBOMs returns zero matches -- the shipped artifact inventory does not list the legacy tree"
        }
      ],
      "summary": "Shipping/runtime surfaces (mcp.json, plugin.json variants, release SBOM, runtime-inventory manifest) are cut over to the native binary. Two non-shipping harness surfaces are NOT cut over: package.json's test/test:mcp/test:fable scripts still execute the full legacy mcp/*.test.mjs + mcp/host/*.test.mjs suite, and CI (ledger-completion.yml) still syntax-checks mcp/server.mjs and mcp/toolsets.mjs. Caller cutover is therefore proven for the product's runtime path but not yet for its own test/CI harness."
    },
    "gap": "interpreter-dispositions.json labels mcp/server.mjs currentPolicy as 'dev-only'; the caller-cutover enumeration above shows the shipped runtime path IS cut over (mcp.json/plugin.json/SBOM/runtime-inventory all point to or confirm the native binary), but package.json's own test scripts and .github/workflows/ledger-completion.yml still actively invoke/check the legacy file, so the harness itself is not yet cut over. Independently, the native crate (1860 lines across 6 modules: discovery, http_security, jsonrpc, prompts, resources, tools) has no counterpart for decision-points, delivery-receipt/serialization, rollout, workspace-routing, feedback-loop, push-client/push-limits, adapters/provenance, authorization, or the diagnostics/verification-command libs — behavior in those legacy files has no plausible native destination file to check assertions against, so no 'exists' claim can be made for named native destination modules covering them. Needed before parity: name the specific native module per legacy file (most don't have one yet), port the missing behavior, cut package.json's test scripts and the CI workflow over to the native path, then supply passing tests naming the ported assertions."
  },
  {
    "owner": "public-runtime",
    "cluster": "mcp-host-adapter-chain",
    "verdict": "parity-proven",
    "fileCount": 12,
    "nativeDestination": "engine/crates/membrane-mcp/src/host_capability_matrix.rs (capability-matrix.mjs; embedded fixture engine/crates/membrane-mcp/fixtures/capability-matrix.v1.json copied from capability-matrix.v1.json), host_candidate_set.rs (candidate-set.mjs), host_continuity.rs (continuity.mjs), host_evidence_interceptor.rs (evidence-interceptor.mjs), host_observable_event.rs (observable-event.cjs), host_observable_ingress.rs (observable-ingress.cjs), host_delivery_receipt.rs (delivery-receipt.cjs), host_delivery_ledger_store.rs (delivery-ledger-store.cjs, including the P2 8.9 mutation outbox), host_push_tool_egress.rs (push-tool-egress.mjs), host_context_adapter.rs (context-adapter.cjs -- partial: ports the pure client-identity/task-envelope logic only; the legacy file's subprocess/HTTP transport and its delegation to the out-of-scope mcp/context-renderer-lib.cjs render/finalize/ContextSessionV1 are Node-hook process orchestration with no MCP-surface counterpart and are intentionally not ported). index.mjs has no destination file: it is a pure re-export barrel, documented as such in a comment at the end of engine/crates/membrane-mcp/src/lib.rs next to the module registrations.",
    "nativeTests": "engine/crates/membrane-mcp/tests/parity_host_boundary.rs, parity_host_context_adapter.rs, parity_host_delivery_ledger.rs, parity_host_delivery_receipt.rs, parity_host_observable_event.rs, parity_host_observable_ingress.rs, parity_host_observable_ingress_default_path.rs, parity_host_push_tool_egress.rs (all new; plus unit tests embedded in each host_*.rs module). All pass under `rightkit.cmd cargo test -p membrane-mcp --target x86_64-pc-windows-msvc`.",
    "gap": "Closed by lane MCPHOST (2026-09-10): every module now has a named native destination with passing parity test evidence reproducing the legacy assertions/fixtures. Caller-cutover status (investigated, not changed by this lane): mcp/host/* is referenced only by mcp/server.mjs (the legacy JS dev-mode MCP server) and by dev-only scripts (scripts/qualification/cases/psh-windows.mjs, scripts/generate-host-contracts.mjs, scripts/ci/check-network-boundary.mjs, scripts/ci/test-random.mjs) plus its own test files -- none of apps/membrane-hub's shipped runtime-inventory manifest, installer, or Tauri binary reference mcp/host or mcp/server.mjs (grep of apps/ finds zero hits; apps/membrane-hub/tests/runtime-inventory.test.mjs explicitly asserts no shipped manifest entry contains \"mcp/host\"). mcp/host is therefore dead code on every shipped/production path today; the still-live references are the legacy Node dev-mode server and its own tooling, which this lane's addendum explicitly scoped out of caller rewiring (no legacy-tree edits permitted). Cutting those dev-mode callers over to the native membrane-mcp binary is unstarted and out of this lane's scope."
  },
  {
    "owner": "public-runtime",
    "cluster": "mcp-membrane-mcp-native-shim",
    "verdict": "insufficient",
    "fileCount": 1,
    "nativeDestination": "engine/target/.../membrane (stdio-mcp binary) — this file is already just an 8-line spawn wrapper around the native binary, not legacy logic to port.",
    "nativeTests": null,
    "gap": "Even though this file has essentially no legacy behavior left to port (it already just spawns the native binary), NCL-02 requires 'integration checkpoint I1 test evidence hash recorded' and no test in i1-passing-tests.txt exercises membrane-mcp-native.mjs (no mcp/membrane-mcp-native*.test.mjs exists in the 100-file list or the repo). Absent any passing test naming this file's behavior, and per the negative control ('vacuous test... counts as no assertion'), there is no assertion to cite. It is functionally near-parity but not evidenced — insufficient, not invented as parity-proven."
  },
  {
    "owner": "public-runtime",
    "cluster": "mcp-test-suite-for-still-live-modules",
    "verdict": "insufficient",
    "fileCount": 42,
    "nativeDestination": "n/a — these are the test files for mcp-server-entrypoint-and-runtime and mcp-host-adapter-chain, not independent production code.",
    "nativeTests": "engine/crates/membrane-mcp/tests/parity_mcp_blueprint_capability.rs (new)",
    "portedTestNames": {
      "coveredByExistingNativeTests": [
        {
          "legacy": "mcp/server.test.mjs",
          "native": "engine/crates/membrane-mcp/tests/discovery_roundtrip.rs (discovery_matches_initialize_contract, negotiated_toolsets_advertise_only_registered_native_tools, tool_calls_are_typed_and_never_use_legacy_fallback)"
        },
        {
          "legacy": "mcp/product-truth.test.mjs",
          "native": "engine/crates/membrane-mcp/tests/discovery_roundtrip.rs (tool-count and schema assertions on discovery_response())"
        },
        {
          "legacy": "mcp/authorization.test.mjs, mcp/authorization.conformance.test.mjs, mcp/scope-grant-v1.test.mjs",
          "native": "engine/crates/membrane-runtime/tests/authorization_conformance.rs, native_authorization.rs"
        },
        {
          "legacy": "mcp/install.test.mjs, mcp/installation-binding.test.mjs",
          "native": "engine/crates/membrane-runtime/tests/installation_manifest.rs (active_manifest_publishes_once_and_persists_for_subsequent_calls, handshake_rejects_wrong_installation, handshake_rejects_incompatible_schema, handshake_rejects_unexpected_data_root, handshake_accepts_matching_manifest, parsed_manifest_distinguishes_three_states)"
        },
        {
          "legacy": "mcp/client.test.mjs",
          "native": "engine/crates/membrane-client/tests/explicit.rs, compat.rs, installed_lifecycle.rs, installed_transport.rs"
        },
        {
          "legacy": "mcp/repository-catalog.test.mjs, mcp/project-registry.test.mjs",
          "native": "engine/crates/membrane-runtime/tests/pull_catalog.rs, ledger_owner_acceptance.rs"
        },
        {
          "legacy": "mcp/proposal-store.test.mjs",
          "native": "engine/crates/membrane-adapt/tests/proposal_state_machine.rs, proposal_target_exclusion.rs (already parity-proven under the adapt-completion/proposal-lifecycle-and-conflict cluster)"
        },
        {
          "legacy": "mcp/retrieval-contracts.test.mjs",
          "native": "engine/crates/membrane-runtime/tests/pull_catalog.rs, pull_freshness.rs, pull_residual_qualification.rs"
        },
        {
          "legacy": "mcp/diagnostics-tools.test.mjs",
          "native": "engine/crates/membrane-runtime/tests/diagnostics_authorization.rs, live_diagnostics_audit_persistence.rs"
        }
      ],
      "coveredByNewParityTest": [
        {
          "legacy": "mcp/blueprint-capability.test.mjs",
          "native": "engine/crates/membrane-mcp/tests/parity_mcp_blueprint_capability.rs (new: asserts membrane_blueprint discovery schema required/enum fields, search/recall query validation, expand node validation, and the typed blueprint_envelope_invalid error path via McpServer.dispatch)"
        }
      ],
      "coveredByHostAdapterChainParityTests_ported2026-09-10": [
        "mcp/host/host-boundary.test.mjs",
        "mcp/host/host-context-adapter.test.mjs",
        "mcp/host/host-delivery-ledger.test.mjs",
        "mcp/host/host-delivery-receipt.test.mjs",
        "mcp/host/host-observable-event.test.mjs",
        "mcp/host/host-observable-ingress-default-path.test.mjs",
        "mcp/host/host-observable-ingress.test.mjs",
        "mcp/host/push-tool-egress.test.mjs"
      ],
      "reclassified2026-09-10-lane-MCP3": {
        "note": "The 18-file 'no native module' bucket below was re-audited by lane MCP3 with file:line evidence across the whole engine/ tree. Full detail (exact native destinations and grep evidence) lives in audit/qualification/windows-r5/ncl-02/mcp-parity.json under mcpTopLevelTests_remainsToPort_2026-09-10-lane-MCP3-reclassification. Result: 10 files are (a) implemented natively under a different structure/crate than membrane-mcp (deadline, working-context, feedback-loop, push-limits, calibration-harness/retrieval-contracts, delivery-receipt top-level, delivery-serialization, adapters/provenance/index, blueprint-readiness, workspace-routing); 1 file (working-context, the MBR-305 activeContextPage cursor-pagination slice only) is (b) a genuine gap, with a new native regression test added this session (engine/crates/membrane-runtime/src/mcp_executor.rs, working_context_save_load_close_and_scope_isolation_match_legacy_store_contract) covering the save/load/close/scope-isolation contract that IS ported, while the cursor-pagination/custom-expiresAt slice remains explicitly un-ported and documented as such; 8 files are (c) dead code with zero live importers outside their own test or the already-dead mcp/server.mjs (decision-points, capability-inventory, rollout, e2e-benchmark, lifecycle, adapters, adapter-manifest, server-durable).",
        "remainsToPort_true_gap": [
          "mcp/working-context.test.mjs (MBR-305 activeContextPage cursor-paged history + caller-supplied expiresAt only -- the save/load/close/scope-isolation contract is now covered by a native test)"
        ]
      },
      "noNativeImplementationFound_remainsToPort": [
        "mcp/blueprint-readiness.test.mjs",
        "mcp/calibration-harness.test.mjs",
        "mcp/capability-inventory.test.mjs",
        "mcp/deadline.test.mjs",
        "mcp/decision-points.test.mjs",
        "mcp/delivery-receipt.test.mjs",
        "mcp/delivery-serialization.test.mjs",
        "mcp/e2e-benchmark.test.mjs",
        "mcp/feedback-loop.test.mjs",
        "mcp/lifecycle.test.mjs",
        "mcp/push-limits.test.mjs",
        "mcp/rollout.test.mjs",
        "mcp/workspace-routing.test.mjs",
        "mcp/adapters.test.mjs",
        "mcp/adapter-manifest.test.mjs",
        "mcp/adapters/provenance/index.test.mjs",
        "mcp/server-durable.test.mjs",
        "mcp/working-context.test.mjs"
      ]
    },
    "gap": "SUPERSEDED 2026-09-10 by lane MCP3's reclassification (see reclassified2026-09-10-lane-MCP3 above and mcp-parity.json): of the 18 files below, only mcp/working-context.test.mjs names a genuine remaining gap (cursor pagination), now partially closed by a new native test. The other 17 either have a real native destination under a different module/crate (10, evidenced with file:line citations) or are dead code unreachable from any shipped or native-serving path (8, evidenced by zero live importers). This list is retained verbatim below for history; treat mcp-parity.json's a/b/c breakdown as authoritative going forward, not this flat list."
  },
  {
    "owner": "WIRE4",
    "cluster": "providers-plugin-ecosystem",
    "verdict": "gap-open-partial",
    "fileCount": 11,
    "nativeDestination": "engine/crates/membrane-blueprint/src/providers/{mod.rs, ranking.rs, source_disposition.rs} (lane P4 2026-09-10); bridges/scip/frameworks/iac still none, tracked as lanes P1/P2/P3",
    "nativeTests": null,
    "gap": "Lane P4 (2026-09-10) ported and wired: ranking/types.mjs (full), source-disposition.mjs (ported, not yet called from build_generation -- provider id \"ingestion\" reserved), plugin-loader.mjs (registry scaffold + PROVIDER_ORDER slot only, admission gate itself NOT ported -- still a real gap if native plugins are in scope), and rewired graph.rs's ad-hoc import resolution onto module_resolution.rs (see cluster 51 / row 38). Still MISSING with zero native equivalent: bridge/FFI seam scanning, SCIP-index compiler-tier ingestion, cross-stack framework fact extraction, Terraform/K8s IaC facts -- these remain lanes P1 (bridges), P2 (scip), P3 (frameworks/iac) per the providers/mod.rs registry append-points."
  },
  {
    "owner": "WIRE4",
    "cluster": "resolution-owner-and-module-resolution",
    "verdict": "parity-proven-partial",
    "fileCount": 3,
    "nativeDestination": "engine/crates/membrane-blueprint/src/lib_findings_specifier.rs (wired via findings.rs:131); engine/crates/membrane-blueprint/src/module_resolution.rs (wired into graph.rs build pass via two new in-memory adapters, lane P4 2026-09-10)",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_module_resolution_wired.rs",
    "gap": "graph/resolution/index.mjs remains correctly ported+wired (unchanged from prior audit). module_resolution.rs is NO LONGER dead code: graph.rs's former private import_specifiers()/resolve_import() are now aliases onto module_resolution::{import_specifiers_in_files, resolve_import_in_files} (lane P4, 2026-09-10), a verbatim relocation of the exact same first-match-wins logic (equivalence proved by parity_module_resolution_wired.rs, existing tests/graph.rs goldens unchanged). resolve_js_module/resolve_python_module's richer disk-based semantics (typed Ambiguous, stdlib short-circuit, pyproject.toml roots) remain unreached by the build pass -- an in-memory-vs-filesystem model gap, not a missing-caller gap."
  },
  {
    "owner": "wire2",
    "cluster": "git-source-observation-build-time-wiring",
    "verdict": "parity-proven-partial",
    "fileCount": 2,
    "nativeDestination": "engine/crates/membrane-blueprint/src/engine.rs build_and_publish() now calls git_source_observation::git_source_observation() (engine/crates/membrane-blueprint/src/git_source_observation.rs, already a compiled pub mod with zero prior callers) instead of the independently drifted lib_application_snapshots::current_git_identity (different porcelain exclusion set, no statusDigest)",
    "nativeTests": "engine/crates/membrane-blueprint/tests/parity_wire_freshness.rs (2 tests: clean-tree and dirty-tree real git fixture repos, asserting persisted sourceObservation.head/dirty/statusDigest equal an independent git_source_observation() call against the same fixture)",
    "gap": "Only the build-time capture half of NCL-02's git-source-observation.mjs row is wired and proven. freshness_receipt::build_freshness_receipt (the query-time freshness-receipt assembly that consumes this observation as CurrentSourceState) has zero production callers anywhere in engine.rs/query.rs/service.rs/cli.rs -- native status() instead uses a different recompute-and-compare-source_hash freshness model, a pre-existing design divergence too large to safely replace in this lane's scope given concurrent editors on engine.rs/query.rs/graph.rs. Separately, blueprint/src/sources/freshness-observation.mjs's observeRepositoryFreshness (per-file overlay enumeration, content hashes, stability re-check, commit distance) has NO native module at all -- MISSING, not unwired. Both are named as open follow-up in receipt lane-WIRE2-2026-09-10.json."
  }
]
