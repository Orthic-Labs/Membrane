# Packaging & daemon inventory

Competitor comparison uses source only; local-gap rows additionally record read-only host diagnostics captured 2026-08-28. Scope: package/install/update/uninstall plus resident, daemon, startup, health, & recovery behavior. Competitor root: `\\192.168.1.7\d\claude\repos\membrane`. Selected repositories expose strongest packaging mechanisms across both competitor collections.

| Section | Concrete function / mechanism | Membrane | GitNexus | Serena | Claude Context | CodeGraph | MemOS local plugin | OpenViking |
|---|---|---|---|---|---|---|---|---|
| Packaging | Primary distributable | Tauri desktop bundle with Windows NSIS template; private root package has no public `bin`. `package.json:2-3`; `apps/membrane-hub/src-tauri/tauri.conf.json:5-21`; `apps/membrane-hub/src-tauri/tauri.windows.conf.json:2-10` | Public npm CLI `gitnexus`, version 1.6.9, with executable mapping. `blueprint/abhigyanpatwari__GitNexus/gitnexus/package.json:2-3,30-33` | Public PyPI package `serena-agent`; console scripts `serena`, `serena-agent`, `serena-hooks`. `blueprint/oraios__serena/pyproject.toml:5-11,63-66` | Public npm MCP package `@zilliz/claude-context-mcp` with `dist/index.js` binary; separate VS Code extension. `blueprint/zilliztech__claude-context/packages/mcp/package.json:2-8`; `packages/vscode-extension/package.json:2-8` | Public npm package `@astudioplus/codegraph-mcp` with MCP, daemon, hook, & engine-fetch executables. `blueprint/codegraph-ai__CodeGraph/mcp-package/package.json:2-3,22-28` | Public npm plugin `@memtensor/memos-local-plugin`; dedicated PowerShell/shell installers perform host integration. `membrane/MemTensor__MemOS/apps/memos-local-plugin/package.json:2-3,21-27` | Public PyPI package `openviking` with `ov`, `openviking`, & `openviking-server`; Docker image plus Helm chart. `membrane/volcengine__OpenViking/pyproject.toml:11-16,201-204`; `deploy/helm/openviking/Chart.yaml` |
| Packaging | Runtime payload composition | Bundles native `cortex`, `membrane`, `membrane-tray`, `membrane-daemon` sidecars plus runtime resources & complete Blueprint Node tree. Inventory validates component names, hashes, paths, version, file count. `apps/membrane-hub/scripts/runtime-inventory.mjs:55-83,129-206,252-264` | npm `files` includes built CLI/MCP, hooks, skills, scripts, vendor, web assets; postinstall builds tree-sitter grammars. `gitnexus/package.json:33-52` | Wheel/sdist supplies Python runtime; Python range `>=3.11,<3.15`. `pyproject.toml:11` | npm `files` publishes `dist`; external Milvus & embedding services remain runtime dependencies. `packages/mcp/package.json:31-37`; `packages/mcp/src/index.ts:258-262` | npm carries JS launchers; postinstall fetches platform-specific native engine instead of shipping all platform binaries. `mcp-package/bin/postinstall.js:19-66` | Installer stages packed npm payload plus dependencies; preserves data, logs, skills, daemon, migrations, config, auth, & runtime-home. `apps/memos-local-plugin/install.ps1:186-241` | Image builds Python, Rust CLI, native extension, & web studio; runtime image contains entrypoint plus pending-health helper. `membrane/volcengine__OpenViking/Dockerfile:3-13,46-58,108-110` |
| Packaging | Platform-native asset acquisition | All sidecars pre-staged into installer; no install-time network asset acquisition. `runtime-inventory.mjs:71-83` | Native dependencies arrive through npm install/postinstall; no separate platform installer found. | No separate platform asset downloader found. | No native daemon asset path found. | Detects OS/arch, downloads required release assets, verifies SHA-256, stages all assets before promotion, writes engine-version marker. `mcp-package/bin/fetch-engine.js:63-75,283-323,370-426` | Rebuilds/verifies `better-sqlite3` during staged install; optional local model downloads on first use. `install.ps1:201-241` | Native artifacts built during package/image build; no runtime platform downloader found. |
| Packaging | Install-time side effects | NSIS copies binaries/resources, writes protocol handler, shortcuts, uninstall metadata, canonical Run entry, & deletes legacy Run value. `apps/membrane-hub/src-tauri/windows/installer.nsi:716-760,996-1025` | `postinstall` builds grammars; editor mutation stays explicit under `gitnexus setup`. `gitnexus/package.json:52`; `gitnexus/src/cli/setup.ts:1198+` | Package install itself does not register clients; `serena setup <client>` is explicit. `src/serena/cli.py:208-225` | No install-time host configuration found. | Postinstall fetches/verifies engine but only prints opt-in hook command; it does not install hooks automatically. `mcp-package/bin/postinstall.js:56-66,134-136` | npm postinstall is deliberately side-effect-free; explicit installer performs host wiring. `apps/memos-local-plugin/scripts/postinstall.cjs:1-35`; `install.ps1` | Package/image install builds artifacts; no desktop-host mutation found. |
| Packaging | Client/editor registration | MCP manifest expects command `membrane stdio-mcp`, but desktop installer does not register clients or make `membrane` discoverable. `mcp.json:2-9`; `package.json:2-3` | `setup` detects supported clients & writes MCP config; prefers directly spawnable global binary, otherwise version-pinned `npx -y gitnexus@<version> mcp`, using `cmd /c` on Windows. `gitnexus/src/cli/setup.ts:136-206,962-1002` | `setup` shells to native client registration commands such as `claude mcp add` & `codex mcp add`. `src/serena/config/client_setup.py:56-88` | Not found in in-scope source; package exposes stdio entrypoint only. | Hook registration is explicit & optional; MCP binary remains client-spawnable. `mcp-package/package.json:22-28`; `mcp-package/bin/postinstall.js:134-136` | Explicit installer detects OpenClaw/Hermes/DSH & patches host configuration. `apps/memos-local-plugin/install.ps1` | Not found in in-scope source. |
| Packaging | Artifact integrity / admission | Release staging validates sidecar paths & Blueprint tree hash; updater module implements fail-closed dual-signature admission, but live updater artifacts are disabled. `runtime-inventory.mjs:129-206`; `apps/membrane-hub/src-tauri/src/update_admission.rs:220-254`; `tauri.windows.conf.json:3` | npm integrity only; no extra artifact admission layer found. | Package-manager integrity only; no extra artifact admission layer found. | npm/VSIX integrity only; no extra artifact admission layer found. | Downloads `.sha256`, hashes staged file, refuses mismatch, promotes only after every required asset verifies. `mcp-package/bin/fetch-engine.js:283-323,370-426` | Staged payload is structurally checked, swapped, & rolled back on failure; no independent signed admission found. `install.ps1:186-241` | Image/package build integrity; Helm config checksum triggers rollout. `deploy/helm/openviking/templates/deployment.yaml:20` |
| Ownership & activation | Default process owner | Desktop tray owns resident daemon; dashboard is on-demand & explicitly rejects startup ownership. `apps/membrane-tray-windows/src/main.rs:58-67,88-119`; `apps/membrane-hub/src-tauri/src/main.rs:139-149` | MCP client owns default stdio process. Optional HTTP server is foreground CLI. `gitnexus/src/cli/index.ts:229-232`; `src/mcp/http-transport.ts:8-19` | MCP client owns stdio lifespan; non-stdio singleton remains active inside foreground server. `src/serena/mcp.py:398-414` | MCP client owns stdio process; VS Code owns extension process. `packages/mcp/src/index.ts:258-262`; `packages/vscode-extension/package.json:28-29` | MCP client owns MCP wrapper; standalone watcher is separate foreground process expected to be launched externally. `mcp-package/bin/codegraph-daemon.js:4-14,135-143` | Host plugin ensures viewer daemon; explicit installer also starts it immediately for Hermes. `adapters/hermes/memos_provider/daemon_manager.py:359-485`; `install.ps1:751-772` | Container/orchestrator owns server process. `docker/openviking-entrypoint.sh:118-156` |
| Ownership & activation | Durable resident owner | Visible per-user tray is sole resident owner; daemon is its headless child. `apps/membrane-tray-windows/src/main.rs:88-119`; macOS `TrayUI.swift:114-129` | N/A; no durable resident required for default mode. | N/A; no durable resident required for stdio mode. | N/A; work lives inside client or VS Code host. | External OS manager is claimed in wrapper comments, but LaunchAgent/systemd/Windows-task installer was not found in repository source. `mcp-package/bin/codegraph-daemon.js:9-10` | Hermes host/provider owns singleton viewer daemon through ensure-on-start. `daemon_manager.py:359-485` | Docker/Kubernetes is durable owner; Deployment controls replicas & probes. `deploy/helm/openviking/templates/deployment.yaml:11,60-68` |
| Ownership & activation | OS login / boot registration | Windows HKCU Run launches tray with `--login-launch`; macOS uses `SMAppService.mainApp`. No Windows Service exists by design. `apps/membrane-tray-windows/src/startup.rs:8-43`; `apps/membrane-tray-macos/Sources/MembraneTrayMacOS/TrayUI.swift:129` | Not found; client launch is activation. | Not found; client launch is activation. | VS Code extension activates at `onStartupFinished`; no OS startup entry. `packages/vscode-extension/package.json:28-29` | Claimed external launch mechanisms are not implemented in inspected source. | Not found; host/plugin startup is activation. | Container restart/boot policy belongs to Docker/Kubernetes deployment, not app code. |
| Ownership & activation | Current-session activation after install | Interactive NSIS finish page can run installed tray; silent/passive mode only runs it when `/R` is present. Run entry otherwise waits until next login. `installer.nsi:455-462,805-815` | `setup` config takes effect when client next spawns MCP; direct CLI runs immediately. | `setup` registration takes effect when client next spawns MCP; direct server runs immediately. | Package launch is immediate when MCP client or extension starts. | Postinstall prepares engine; client or explicit daemon invocation starts runtime. | Installer immediately launches hidden viewer daemon after deployment, then checks status. `install.ps1:751-777` | Entrypoint starts server immediately & waits for health. `docker/openviking-entrypoint.sh:118-156` |
| Ownership & activation | Daemon launch target | Tray resolves sibling `membrane-daemon.exe`; macOS resolves bundle `Contents/MacOS/membrane-daemon`. `apps/membrane-tray-windows/src/main.rs:749`; `apps/membrane-tray-macos/Sources/MembraneTrayMacOS/DaemonSupervisor.swift:212` | N/A. | N/A for stdio; server command runs Serena directly. | N/A. | JS launcher resolves native engine & passes `--watch` plus CLI args. `mcp-package/bin/codegraph-daemon.js:135-143` | Provider builds Node/tsx bridge command with `--daemon` & shared runtime home. `daemon_manager.py:167-184,441-448` | Entrypoint launches `openviking-server` as child. `docker/openviking-entrypoint.sh:118-156` |
| Ownership & activation | Workspace / data-root resolution | Login launch requires durable `%USERPROFILE%\.config\membrane\workspace.json` schema 3; runtime rejects missing/invalid root. `apps/membrane-hub/src-tauri/src/workspace.rs:56-68,197-209` | Per-repo `.gitnexus` plus user registry/config; client command can run independent of desktop install. | Project passed explicitly or auto-detected from nearest `.serena/project.yml` / `.git`. `src/serena/cli.py:76,233-316` | Workspace supplied by MCP/VS Code host; no resident-global root found. | Daemon is explicitly workspace-scoped; heartbeat namespace derives from workspace slug. `crates/codegraph-server/src/daemon.rs:12-16,164-195` | Installer passes normalized host-data home; daemon command shares same runtime home. `install.ps1:117`; `daemon_manager.py:167-184` | Config/data paths live in mounted container state; server port/config resolved by entrypoint. `docker/openviking-entrypoint.sh:7-28,39-77` |
| Ownership & activation | Multiple-instance exclusion | Tray supervision uses one owner topology; dashboard has Tauri single-instance plugin. Daemon ownership relies on tray lifecycle. `apps/membrane-hub/src-tauri/src/main.rs:180`; `apps/membrane-tray-windows/src/process.rs:182-257` | Client-spawned stdio permits per-client processes; HTTP reports port conflicts. `src/mcp/http-transport.ts:605-616` | Per-client stdio instances are supported; no global singleton lock found. | Per-client stdio process; no global singleton lock found. | Live heartbeat refuses second writer for same workspace; stale heartbeats are removed. `crates/codegraph-server/src/daemon.rs:116-126,192-195` | Cross-process startup directory lock plus identity-bearing port health prevents competing viewer daemon. `daemon_manager.py:67-105,282-298,403-485` | Orchestrator replica count controls multiplicity; no local singleton lock. `deploy/helm/openviking/templates/deployment.yaml:11` |
| Reliability | Readiness / identity proof | Tray waits up to 35 seconds for daemon handshake; dashboard starts only after ready endpoint/token. `apps/membrane-tray-windows/src/supervisor.rs:30-34,281-297`; `apps/membrane-tray-windows/src/main.rs:752-784` | HTTP exposes unauthenticated `/health`; non-loopback binding requires bearer token. `src/mcp/http-transport.ts:458-480,533` | CLI provides project health check, but no separate daemon identity probe. `src/serena/cli.py:911-949` | MCP connection completion is readiness; no HTTP identity probe found. | Heartbeat includes PID, workspace, slug, timestamps; freshness decides ownership. `crates/codegraph-server/src/daemon.rs:37-126` | `/api/v1/health` must report service `memos-local-plugin` plus agent `hermes`; startup waits up to 45 seconds. `daemon_manager.py:282-298,461-485` | Entrypoint serves 503 pending state, probes `/health`, fails if child exits or timeout occurs; Helm adds liveness/readiness probes. `docker/openviking-entrypoint.sh:39-77,118-156`; `deploy/helm/openviking/templates/deployment.yaml:63-68` |
| Reliability | Restart / crash-loop policy | Tray restarts after 1 second; enters crash loop at 3 exits within 60 seconds; drains for 7 seconds. `apps/membrane-tray-windows/src/supervisor.rs:30-34,338-356` | No restart loop; client or orchestrator restarts process. | No restart loop; client or orchestrator restarts process. | No restart loop; client/VS Code host restarts process. | Wrapper reports abnormal exit but contains no restart loop; absent external service package leaves restart ownership unresolved. `mcp-package/bin/codegraph-daemon.js:170-202` | Ensure-on-start launches missing daemon; no continuous supervisor restart loop found. `daemon_manager.py:359-485` | Entrypoint propagates child exit; Docker/Kubernetes restart policy owns recovery. `docker/openviking-entrypoint.sh:142-156` |
| Reliability | Child containment / signal propagation | Windows Job Object with kill-on-close plus `CREATE_NO_WINDOW`; tray drain/stop owns child lifetime. `apps/membrane-tray-windows/src/process.rs:182-257`; `supervisor.rs:281-297` | Foreground HTTP shares signal wiring; stdio closes with client. `src/mcp/http-transport.ts:635-636` | stdio lifespan teardown shuts server down. `src/serena/mcp.py:398-414` | SIGINT/SIGTERM exit cleanly. `packages/mcp/src/index.ts:292-300` | JS wrapper forwards SIGINT/SIGTERM/SIGHUP so Rust persists state & removes heartbeat. `mcp-package/bin/codegraph-daemon.js:198-202`; `crates/codegraph-server/src/daemon.rs:260-298` | Detached child uses `start_new_session=True`; stop path escalates SIGTERM to SIGKILL. `daemon_manager.py:441-448,629-636` | Entrypoint forwards INT/TERM, waits child, & propagates exit status. `docker/openviking-entrypoint.sh:127-156` |
| Reliability | Stale-owner recovery | Startup code removes legacy Run value, but installer/runtime has no full repair pass for missing ARP entry, wrong executable owner, or current-session nonactivation. `startup.rs:42-43`; `installer.nsi:725-760` | Setup rewrites owned config; uninstall precisely removes owned keys/dirs. `gitnexus/src/cli/setup.ts`; `gitnexus/src/cli/uninstall.ts:109-209` | Client setup can be rerun; no resident stale state exists. | N/A; client-owned process. | Stale heartbeat is removed opportunistically; consumers fall back to local work. `crates/codegraph-server/src/daemon.rs:116-126` | Stale startup lock removal, conservative port ownership checks, & zombie cleanup. `daemon_manager.py:67-105,282-298,518-589` | Orchestrator replaces failed container; config checksum rolls pods on config change. `deploy/helm/openviking/templates/deployment.yaml:20,63-68` |
| Reliability | Foreground / container lifecycle | Desktop resident is tray-owned; not a foreground service or Windows Service. | Foreground CLI & client-spawned stdio; optional Docker CLI image can use process as PID 1. | Foreground CLI/client process; compose variant runs server. | Client/extension-host process only. | Foreground wrapper; external persistence mechanism missing from inspected source. | Host-owned detached local daemon. | Explicit container entrypoint provides pending health, readiness wait, signal forwarding, & child status propagation. `docker/openviking-entrypoint.sh:39-77,118-156` |
| Upgrade & removal | Upgrade atomicity / state preservation | NSIS supports in-place update & preserves startup value intent, but no runtime-level transactional swap/rollback was found. `installer.nsi:241-353,725-739` | npm package manager owns upgrade; setup can be rerun to restamp version-pinned fallback. `gitnexus/src/cli/setup.ts:32-37,164-188` | Package manager owns upgrade; no local transactional swap found. | npm/VSIX package manager owns upgrade. | Every required engine asset is verified before promotion, limiting partial multi-asset installs. `mcp-package/bin/fetch-engine.js:370-426` | Stages tarball, moves live tree to backup, swaps staged tree, restores preserved state, & rolls back on any error. `install.ps1:186-241` | Image tag/Helm rollout owns upgrade; config checksum triggers new rollout. `deploy/helm/openviking/templates/deployment.yaml:20` |
| Upgrade & removal | Uninstall reversal | NSIS removes binaries, shortcuts, protocol/uninstall keys, canonical & legacy Run values; optional app-data removal is offered. `installer.nsi:838-930` | Explicit `uninstall` defaults to dry-run, removes only owned MCP/hooks/skills, preserves indexes/global package unless user requests separate commands. `gitnexus/src/cli/uninstall.ts:4-28,434-576` | No dedicated reversal command found in in-scope source; package/client removal is external. | No dedicated reversal command found. | Hook installer supports opt-out/uninstall; package removal is npm-owned. `mcp-package/bin/postinstall.js:134-136` | Installer-specific host cleanup exists, but exact full reversal was not found in inspected PowerShell slice. | Helm/package manager owns uninstall; no desktop residue exists. |
| Upgrade & removal | Update channel / application | Tauri updater public key & admission gate exist, but Windows config sets `createUpdaterArtifacts: false`; source states no live end-to-end update install flow. `tauri.conf.json:21-25`; `tauri.windows.conf.json:3`; `update_admission.rs:39-43` | npm release/update. | PyPI/uv/pip update. | npm/VSIX update. | npm client plus independently versioned fetched engine. `mcp-package/bin/fetch-engine.js:63-75` | npm tarball updater through explicit installer; local state preserved transactionally. | PyPI/image/Helm update. |
| Upgrade & removal | CLI discoverability after desktop install | Installer places `membrane.exe` beside app but does not add install directory to PATH or write MCP config with absolute path. Live `Get-Command membrane` returns nothing. `runtime-inventory.mjs:55-73`; `mcp.json:6-9` | Setup tests direct spawnability; uses absolute/global executable or version-pinned `npx` fallback. `gitnexus/src/cli/setup.ts:136-206` | Client setup writes explicit `serena start-mcp-server` launch command through client CLI. `src/serena/config/client_setup.py:22-35,56-88` | npm executable is discoverable through package runner/global install; no setup fallback found. | npm executables expose MCP/daemon/fetch commands. `mcp-package/package.json:22-28` | Host integration records concrete plugin path/runtime command. `install.ps1`; `daemon_manager.py:167-184` | Python console scripts expose server/CLI. `pyproject.toml:201-204` |
| Local gap verdict | Installed resident activation observed on 2026-08-28 | Installed tray/daemon were written at 06:25 after Windows boot/Explorer login on 2026-08-25. Only running tray/daemon/dashboard come from `%TEMP%\membrane-dev-e2e-review1`; installed copy is not running. Installed tray `--self-test` passes; workspace config/root/runtime config all exist. Root cause is activation, not binary or workspace failure. | N/A | N/A | N/A | N/A | MemOS avoids this gap by launching immediately after deploy then probing identity health. `install.ps1:751-777`; `daemon_manager.py:461-485` | OpenViking avoids this gap by making start+health wait part of entrypoint contract. `docker/openviking-entrypoint.sh:118-156` |
| Local gap verdict | Service-model diagnosis | No `Win32_Service` entry exists because product architecture defines visible per-user tray as supervisor. “Service not running” therefore means installed tray was never activated; it is not failed Service Control Manager state. `startup.rs:8-43`; `main.rs:88-119` | No OS service; default client-owned stdio is sufficient. | No OS service; default client-owned stdio is sufficient. | No OS service; host-owned lifetime. | Attempts external daemon persistence, but installer for it is missing. | No OS service; host-owned ensure-on-start. | Uses container orchestrator, not desktop service manager. |
| Local gap verdict | Installer-state reconciliation observed | HKCU legacy `Membrane Tray` Run value holds installed executable command; canonical `Membrane` value exists but is blank/invalid. No StartupApproved disable record exists. Install directory includes `uninstall.exe`, but no matching HKCU uninstall registration exists. This contradicts NSIS install section, strongly indicating raw/partial/deviant deployment path bypassed installer completion. `installer.nsi:725-760` | Setup/uninstall explicitly reconcile owned editor state. | Setup explicitly registers client state. | Not found. | Engine installer reconciles version marker/assets, not OS install state. | Transactional installer reconciles host payload & preserved state. | Orchestrator reconciles declared deployment. |
| Local gap verdict | Wheel-reuse decision | Keep tray-owned daemon; do not add Windows Service. Missing layer is standard installer activation + reconciliation: launch installed tray now, prove daemon identity/health, repair canonical Run & uninstall registration, & register clients with an absolute executable or reliable package-runner fallback. | Reuse direct-spawn probe plus version-pinned fallback for client registration. `setup.ts:136-206` | Reuse explicit client setup workflow. `client_setup.py:56-88` | Reuse host-owned process lifetime where resident service is unnecessary. | Reuse verified staged native-asset promotion; do not copy its missing OS persistence layer. `fetch-engine.js:283-323,370-426` | Reuse transactional swap, immediate hidden launch, identity probe, cross-process startup lock, & stale cleanup. `install.ps1:186-241,751-777`; `daemon_manager.py:67-105,282-298,359-485` | Reuse start/readiness/signal contract only for container distribution. `docker/openviking-entrypoint.sh:118-156` |
| Local gap verdict | Required packaging contract | Installer success must mean: payload registered; canonical login entry present; installed tray launched in current session for interactive, silent, passive, & update paths; daemon identity becomes healthy within bounded timeout; client launch command resolves; repair/uninstall metadata exists. Failed activation must surface as install failure or repairable degraded state. | `setup` provides client resolution pattern. | `setup` provides native client registration pattern. | Client ownership shows resident daemon is optional, not default. | Verified staging provides native payload pattern. | Immediate launch + identity health + rollback provides closest reusable local lifecycle pattern. | Entrypoint health gate provides production start contract. |
