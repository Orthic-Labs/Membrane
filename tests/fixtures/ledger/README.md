# Ledger fixtures

Keep deterministic Markdown sources, expected outlines, hash-mismatch cases, &
projection rebuild receipts here. Fixtures must remain source-bound & must not
contain durable Cortex-memory payloads.
