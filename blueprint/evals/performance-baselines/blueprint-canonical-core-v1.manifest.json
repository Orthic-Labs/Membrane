{
  "schema": "blueprint-canonical-core-benchmark-manifest-v1",
  "harness": "scripts/benchmark-canonical-core.mjs",
  "rawReportSchema": "evals/performance-baselines/blueprint-canonical-core-v1.report.schema.json",
  "selection": "git ls-files --cached -z, excluding .agent, vendor-research, fixture snapshots, performance fixtures/baselines, and generated caches/output",
  "requiredMetrics": ["smoke", "coldBuild", "noChangeBuild", "stageTimings"],
  "stableStages": ["source_scan", "parse_cache_extraction", "indexed_global_resolution", "identity", "parse_cache_persistence", "treesitter_augmentation", "graph_persistence"],
  "stageCoverage": "graph_persistence is explicitly marked unobserved when CLI-owned persistence is outside static-provider reach; no timing is inferred",
  "samples": 1,
  "command": "node scripts/benchmark-canonical-core.mjs --samples 1 --report <path>",
  "sourceIdentity": "HEAD, complete porcelain worktree identity, tracked selection/content digests, explicit measurement-overlay paths, and measured source-content digest are captured in every raw report",
  "isolation": "all Blueprint writes target an OS temporary snapshot; live .agent is never written"
}
