# Cortex overlay — SEAM-CONTRACT dependency

Before sealing any contract touching hub, watcher lifecycle, the cortex↔membrane API, or peer-service discovery, read `docs/plans/orthic/SEAM-CONTRACT.md` and declare it a dependency.

This overlay carries SEAM-CONTRACT §8 last row — anti-drift mechanism. See `tools/rhook`, `docs/plans/orthic/SEAM-CONTRACT.md` §8 for conformance checks.
