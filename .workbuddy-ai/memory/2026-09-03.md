# 2026-09-03

## Repo feature-union enrichment (best-in-class map)
- Extended `D:\Claude\membrane\repos-feature-union.md` with a "Best-in-class per feature (winner map)" section: for every one of the ~150 feature atoms across 22 domains, marked the single repo that implements it best, with concrete implementation evidence and a one-line rationale. Tags reference the 26-repo legend.
- Follow-up user requests: (1) rated all 26 repos 1–10 and proposed a 10-repo prune shortlist; (2) asked to annotate each feature's best implementation in the union doc — done inline.
- Key best-in-class winners: infigraph (#15) for code-intel breadth/offline; GitNexus (#5) for code-analysis + multi-repo; rag-rat (#8) for code+repo-memory hybrid + provenance; MemOS (#2) for memory architecture (parametric/activation); Hindsight (#13) for conversational memory SOTA + integrations; graphiti (#10) for temporal graph; SLM (#22) for retrieval breadth + governance; serena (#20) for IDE editing/refactor; joern (#25)/codeql (#12) for static taint; OpenViking (#24) for filesystem-as-context design.

## Gap & redundancy analysis (companion deliverable)
- Created `D:\Claude\membrane\repos-gap-analysis.md` as the mirror view of the union doc. Counts were parsed programmatically from `repos-feature-union.md` (winner map + coverage matrix), not re-read from repos.
- Headline findings: union has **236 feature atoms** across 22 domains. **Only 2 of 26 repos are fully subsumable** (0 sole-provider atoms): #7 codegraph, #21 potpie. The other 24 each uniquely provide ≥1 capability → pruning always loses real features.
- Top-10 by value index (#13,#15,#5,#22,#10,#8,#2,#6,#23,#3) covers 193/236 (82%); 29 of 43 dropped atoms are sole-provider → forced losses cluster in: deep code-analysis/taint (#12/#25/#26), IDE debug (#20 serena), multi-agent (#24 OpenViking), evolutionary memory (#17/#18), peer memory (#14 honcho).
- Provided 3 prune strategies: A breadth (top-10), B capability-complete (keep all 24 sole-provider repos), C goal-scoped keep-sets (memory-only / code-only / security / SaaS / air-gapped).

## Semantic bucketing + CSVs + skill extension + matrix reconciliation
- Added a "Semantic buckets" orientation table to `repos-feature-union.md`: 22 domains → 5 buckets (Ingestion & Representation / Retrieval & Search / Code Intelligence / Agent Memory / Platform & Ops).
- Generated `repos-scorecard.csv` and `repos-feature-atoms.csv` (236-atom machine-readable union) via a reusable script.
- Extended the existing `repo-feature-union` skill (`C:\Users\adrds\.workbuddy-ai\skills\repo-feature-union\`) to cover the FULL pipeline: union → best-in-class winner map → gap/redundancy → prune → CSV. Added `scripts/analyze_union.py` (parses the union doc, computes coverage/sole/value-index, top-10 prune-loss, writes CSVs).
- **Reconciliation fix:** running the script exposed that the hand-written coverage matrix in the union doc over-credited repos (claimed infigraph covered all 22 domains; per-atom `[tags]` show 17). Rewrote the matrix to be `[tags]`-derived, and rewrote `repos-gap-analysis.md` Section 1 scorecard + headline + Section 4 top-10 (keep-set = 13,15,5,22,2,10,8,6,3,4; 196/236 = 83%; 29 of 40 dropped atoms are sole-provider). Lesson: the `[tags]` on atoms are the source of truth; any coverage summary must be derived from them, not hand-estimated.

## Skill hardening: README strictly off-bounds (code-only rule)
- User corrected the original README-based capture: "the readme is strictly off bounds, I want it semantically captured through code." This is now the skill's HARD RULE.
- Edited `C:\Users\adrds\.workbuddy-ai\skills\repo-feature-union\SKILL.md`:
  - Added a "HARD RULE — code is the only evidence source" block to Overview: README/docs/`*.md`/wiki/marketing are off-bounds; real only when implemented & reachable in source (MCP `@server.tool`/fastmcp, CLI click/argparse/cobra/clap, REST/gRPC routes, SDK exports, typed config schemas, tests, dependency manifests).
  - Phase 2 subagent instructions now say NEVER read README/docs; find the executable surface in source; evidence in `file:symbol` form; empty clones skipped, never backfilled from prose.
  - Phase 5 winner map requires code `file:symbol` citation; candidates with no code symbol are disqualified.
  - Pitfalls rewritten to ban README entirely, derive MCP tool counts by counting `@server.tool`/`tool()` registrations in code, and run the verifier.
  - Added Phase 8 "Verify every atom is code-backed" + a deliverables checklist item.
- Created `C:\Users\adrds\.workbuddy-ai\skills\repo-feature-union\scripts\verify_union.py`: parses the union doc, checks each atom's `file:symbol` evidence actually exists in the source tree (file present + symbol substring in file), flags NO_EVIDENCE/UNBACKED, optional per-domain `--probe` code-pattern cross-check. Smoke-tested: 3 atoms → 2 BACKED, 1 UNBACKED, exit 1. Enforces reproducibility/code-grounding of the HARD RULE.
- NOTE for next run: the existing `repos-feature-union.md` / `repos-gap-analysis.md` were produced under the OLD README-based rule and are now non-compliant. To be fully consistent they should be re-derived from code (start with a sample repo to prove the new rule).

## Stashed README-based artifacts for comparison
- User confirmed code-only is the correct rule and asked to remove the README-based outputs. Chose STASH (not delete) so the README version remains comparable. Moved all 4 README-derived artifacts into `D:\Claude\membrane\_archive_readme-based\`: `repos-feature-union.md`, `repos-gap-analysis.md`, `repos-scorecard.csv`, `repos-feature-atoms.csv`. Workspace root now clean.
- User will start a NEW chat and rerun the skill code-only. Provided a self-contained copy-paste prompt invoking `repo-feature-union` (user-level skill at `C:\Users\adrds\.workbuddy-ai\skills\repo-feature-union\`), pointing at `\\192.168.1.7\d\claude\repos\membrane`, re-stating the HARD RULE (README off-bounds, code-only, file:symbol evidence, verify_union.py must pass with 0 unbacked), and same output filenames so the new code-only set diffs cleanly against `_archive_readme-based/`.
- User's key insight (validated): docs cannot cover every capability in detail; their own repos guarantee docs miss internal surface (hidden MCP tools, CLI flags, routes, config-gated behavior, test-only features). Code-only + verify_union.py is the more complete and precise capture; expect the code version to be LARGER and more accurate, especially tool counts.

## Removed hardcoded tag→repo mapping from analyze_union.py (skill portability fix)
- User flagged: analyze_union.py had a hardcoded `NAMES` dict mapping tags 1–26 to specific repo names (joern=25, opengrok=26, not directory order). This broke reusability — the script only worked for that one folder, and any rerun had to reproduce the exact tag order.
- FIX: rewrote `C:\Users\adrds\.workbuddy-ai\skills\repo-feature-union\scripts\analyze_union.py` to (a) `parse_legend()` extract {tag: repo} from the union doc's `## Legend` markdown table (two-column `| # | Repo | # | Repo |` form), (b) derive domain names from section headers instead of a hardcoded DOMS list, (c) error clearly if no Legend present. The skill is now portable to ANY folder/any repo set. verify_union.py's per-domain PROBES dict is an intentional optional cross-check heuristic (not repo-specific) and stays.
- Validated against `_archive_readme-based/repos-feature-union.md`: parses 26 repos, 237 atoms (real count; earlier "236" was a hand estimate), ranking + top-10 prune (196/237=83%, 29 sole losses) identical to prior corrected numbers. No regression.
- CONSTRAINT for rerun: the new code-only union must re-use the SAME tag→repo order (1=Understand-Anything … 25=joern, 26=opengrok) so the Legend-derived mapping matches the archived baseline for a clean diff. The mapping is whatever the new Legend says — must be identical for comparison.
- Network share `\\192.168.1.7\d\claude\repos\membrane` is NOT reachable from this sandbox (No such file or directory). Rerun must happen in the user's env. Remote URLs reconstructed from the Legend owner/repo as https://github.com/<owner>/<repo>.git for all 26 (covers any of the 7 that lacked a local remote during enumeration).

## User principle: NO hardcoding — repos path must be a runtime arg
- User corrected: the repos directory must NOT be hardcoded anywhere (skill, scripts, or the copy-paste prompt). They supply it at runtime when invoking the agent. The earlier prompt baked in `\\192.168.1.7\d\claude\repos\membrane`; fixed to `<REPOS_DIR>` placeholder.
- This extends the no-hardcoding rule already applied to the tag→repo mapping: paths, repo names, remote URLs, and tag mappings are ALL runtime-provided or derived from the union doc's Legend — never baked in. SKILL.md already uses a generic `//host/share/path` placeholder (clean); scripts take paths as argv. Re-issued the prompt with a `<REPOS_DIR>` parameter the user fills in.

## Membrane 0.1.24 dogfood checklist (docs/DOGFOOD.md)
- Read-only audit of installed product (`C:\Users\adrds\AppData\Local\Orthic Labs\Membrane\current`, 0.1.24) + repo docs. Wrote `D:\Claude\membrane\docs\DOGFOOD.md`: 6 sections (claims, first-run checklist, daily scenarios, failure triage, known-broken table, UNVERIFIED list) + appendix.
- Verified live: Hub `/health` ok (port 47851, serviceId membrane-hub, 6 subsystems, nativeOnly true); hooks in `~/.claude/settings.json` (PreCompact/PostToolUse/PostToolUseFailure/PostCompact); MCP in `~/.claude.json` mcpServers.membrane and `~/.codex/config.toml` [mcp_servers.membrane]; data roots + activation-receipt present; tray (membrane-tray.exe) running.
- Defects observed 2026-09-03 on 0.1.24 unsigned build: `releaseGeneration:"sha256:unknown"` (known-broken seed); `membrane` NOT on PATH (`which membrane` fails; product mcp.json uses bare `membrane` but registered entries use full path); `dailyAnalysis` alert/unavailable/missing_output; Cortex store empty (memoryCount 0).
- Gotchas: `membrane doctor` is invalid → use `membrane cli doctor` (needs CORTEX_DB). `activate skipped (silent install)` in install log means install does not auto-activate.
- UNVERIFIED (not exercised live): Push reduction observability, hook user-visible execution, membrane_context/blueprint/source_read/adapt live behavior, cross-session memory recall (store empty), whether PATH entry is expected.
