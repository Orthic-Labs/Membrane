# 2026-09-02

## Repo feature-union master list (membrane)
- Analyzed all 26 repos under `\\192.168.1.7\d\claude\repos\membrane` and produced a set-union capability inventory.
- Deliverable: `D:\Claude\membrane\repos-feature-union.md` — 22 feature domains, each listing capabilities + contributing repo tags, plus a repo→domain coverage matrix and a 26-repo legend.
- Method: 9 parallel Explore subagents (batches A–E) for first ~14 repos; completed remaining 12 repos (hindsight, honcho, infigraph, sense, mengram, mnemon, mnemosyne, serena, potpie, superlocalmemory, supermemory, OpenViking) by direct README reads after subagent pool hit a 429 rate limit (~3h reset).
- Interpretation note: the user's "add missing features into the next repo" was treated as building a running *union* list, not modifying the external checkouts.
- Data source for repo URLs: `git config --get remote.origin.url` per checkout.
