{
  "schemaVersion": 1,
  "toolCount": 2,
  "toolsets": {
    "default": ["pull", "push"],
    "memory": ["pull", "push"],
    "blueprint": ["pull", "push"],
    "push": ["pull", "push"],
    "diagnostic": ["pull", "push"],
    "operator": ["pull", "push"]
  },
  "invariants": [
    "advertised_tools_have_native_executor_entries",
    "unknown_tool_is_rejected",
    "native_unavailable_is_typed_and_never_falls_back_to_node",
    "negotiated_toolsets_cannot_expand_public_registry",
    "proposer_cannot_self_authorize_review",
    "native_memory_envelopes_are_strictly_validated"
  ]
}
