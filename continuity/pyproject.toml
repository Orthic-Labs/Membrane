[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "membrane-continuity"
version = "0.1.0"
description = "Membrane-owned transcript normalization & semantic continuity primitives"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
