# Platform evidence

Store one immutable `membrane.platform-acceptance.v1` receipt per locally verified signed artifact. Receipt must bind exact commit, release generation, version, platform, artifact name, SHA-256, trust outcomes, no-bypass result, & full lifecycle outcomes. Current qualification is Windows x86_64 on one local laptop; no clean-VM gate applies.

Synthetic tests are not Windows artifact acceptance.
