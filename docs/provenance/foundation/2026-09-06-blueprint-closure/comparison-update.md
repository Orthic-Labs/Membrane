# Blueprint competitive disposition update (canon closure wave)

Baseline comparison revision: `026387f15a401697728752a0006d3406b8987d22`.
Freshness: `2026-09-06`.

This receipt updates only the atoms promoted in the same wave. Each was
recorded CURRENT_INCOMPLETE against comparison revision
`30b3c211ae874f369bed3fe92eb94b2fc5acbb16` on the strength of a named
implementation gap. Every gap named below is now closed in production code with
a real consumer and executable evidence, so the disposition can no longer
honestly assert incompleteness. None of these claims release qualification;
competitive closure stays UNRESOLVED until the RELEASED boundary is reachable.

Two of these were not stale bookkeeping but genuinely broken, and are recorded
as fixed rather than reinterpreted. BPT-057 had no plugin acceptance path at
all — the tests that appeared to prove refusal each defined their own loader
inside the test file. BPT-044's typed taxonomy was dropped on the Hub IPC wire,
so a caller reached through the daemon saw `retryable: undefined` where every
in-process caller saw the real value.

| Atom | Scope | Competitive disposition | Best mechanism | Current evidence | Donor evidence | Gap / action |
|---|---|---|---|---|---|---|
| BPT-003 | COMMITTED | UNRESOLVED | Every git-tracked path receives exactly one terminal ingestion disposition, sealed into the generation manifest so nothing disappears silently. | `DELIVERED / FOCUSED_PASS`; `src/providers/source-disposition.mjs:55` via `build.mjs` into `static-provider.mjs:1457`. | Prior comparison named no donor mechanism accounting for every considered source. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-010 | COMMITTED | UNRESOLVED | Capability/permission provider registry validating identity, checksum, licence and isolation on the production registration path. | `DELIVERED / FOCUSED_PASS`; `src/providers/index.mjs:82-161` exercised via `semantic-orchestrator.mjs` registering with a manifest and licence allowlist. | Prior comparison named no donor registry enforcing licence and integrity before execution. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-011 | COMMITTED | UNRESOLVED | Rules and documents admitted only as evidence-bound declarations carrying categorical provenance and no authority. | `DELIVERED / FOCUSED_PASS`; `doc-truth-projection.mjs` plus `src/lib/rules/evaluate.mjs`. | Prior comparison named no donor keeping declarations from becoming observed facts. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-012 | COMMITTED | UNRESOLVED | Trusted providers run repository-read-only, network-free, process-bounded, cancellable, with typed crash and hang. | `DELIVERED / FOCUSED_PASS`; `src/providers/index.mjs:169+` on the live semantic path. | Prior comparison named no donor enforcing the same isolation envelope. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-013 | COMMITTED | UNRESOLVED | Stable portable identities plus deterministic entity-level rename/move reconciliation that refuses to guess an ambiguous rename. | `DELIVERED / FOCUSED_PASS`; `portable-identity.mjs` and `reanchor.mjs` wired into `watchman/reconcile.mjs`. | Prior comparison named no donor reconciling renames at entity rather than path level. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-014 | COMMITTED | UNRESOLVED | Evidence bound to source address, span/hash, provider/version, generation and truth class, with categorical provenance carrying `confidence: null` and numeric confidence reserved for heuristic evidence. | `DELIVERED / FOCUSED_PASS`; `provenance.mjs:27-70` with the nullable-confidence migration. | Prior comparison named no donor separating categorical from numeric confidence. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-017 | COMMITTED | UNRESOLVED | Conservative re-anchoring by exact entity, fingerprint or unique normalized text, failing closed to stale/ambiguous. | `DELIVERED / FOCUSED_PASS`; `reanchor.mjs:31-64` reached from `service.mjs:414`. | Prior comparison named no donor refusing a fuzzy re-anchor. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-020 | COMMITTED | UNRESOLVED | Explicit dependency DAG over source/provider/config/schema/generation parents, with every declared parent actually populated. | `DELIVERED / FOCUSED_PASS`; `dependency-dag.mjs` with `build.mjs` now publishing the previously-null config parent. | Prior comparison named no donor invalidating through a declared parent set. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-021 | COMMITTED | UNRESOLVED | Incremental build semantically equivalent to a clean build across all nine canon cases. | `DELIVERED / FOCUSED_PASS`; equivalence coverage for add/delete/rename/move/dirty/no-op/config/provider/crash. | Donor previously better on equivalence breadth; the three uncovered cases now have real cold-vs-incremental proof. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-026 | COMMITTED | UNRESOLVED | Strictly lexicographic, non-compensatory Recall ordering where no numeric score can outrank a lower-tier categorical signal. | `DELIVERED / FOCUSED_PASS`; `compareVectors` and `comparePaths`, plus a contract test proving summing the presentational components yields a different order. | Prior comparison named no donor with a non-compensatory ordering. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-027 | COMMITTED | UNRESOLVED | Traversal ceilings enforced by the traversal itself, including the no-generation fallback, with cursors failing closed on a stale generation. | `DELIVERED / FOCUSED_PASS`; `traverse-store.mjs` fallback clamps plus `traversal-cursor.mjs`. | Prior comparison named no donor bounding the internal fallback path. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-033 | COMMITTED | UNRESOLVED | Liveness reported only as LIVE/UNREACHED/UNKNOWN with evidence; zero inbound edges never proves dead. | `DELIVERED / FOCUSED_PASS`; `liveness.mjs:15-88` via `service.mjs:598-605`. | Prior comparison named no donor refusing the zero-inbound-implies-dead inference. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-034 | COMMITTED | UNRESOLVED | Test recommendations with evidence, reason, uncovered impact and omissions, never claiming unproved minimality. | `DELIVERED / FOCUSED_PASS`; `test-recommendation.mjs:19-64` inside `impact`. | Prior comparison named no donor declining to claim minimality. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-038 | COMMITTED | UNRESOLVED | Claims bound to facts with direct/indirect/unsupported/contradicted/ambiguous/stale grounding. | `DELIVERED / FOCUSED_PASS`; `doc-truth-projection.mjs:41-90` via `documentTruth`. | Prior comparison named no donor exposing the full grounding vocabulary. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-039 | COMMITTED | UNRESOLVED | Declared intent and deterministic evidence both preserved with mismatch, citation, generation, confidence and invalidation. | `DELIVERED / FOCUSED_PASS`; same projection and consumer as BPT-038. | Prior comparison named no donor preserving both sides of the comparison. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-042 | COMMITTED | UNRESOLVED | One application service served identically through Hub IPC, bounded one-shot, CLI, JS SDK and native MCP, with executable cross-adapter parity. | `DELIVERED / FOCUSED_PASS`; `service.mjs:168` shared by all five, proven by `tests/adapter-parity.test.mjs`. | Prior comparison named no donor serving one semantics across five adapters. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-043 | COMMITTED | UNRESOLVED | Watcher resident only under an active Hub-owned parent, terminating with it, with no OS service registration and no hidden background process. | `DELIVERED / FOCUSED_PASS`; `authorizeHubWatcher` gating the only detached spawn. | Prior comparison named no donor coupling watcher lifetime to Hub ownership. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-044 | COMMITTED | UNRESOLVED | One canonical envelope and one typed error/retry/partial taxonomy surfaced identically by every adapter. | `DELIVERED / FOCUSED_PASS`; `errors.mjs` metadata now preserved across the IPC wire, the SDK and the CLI printer, proven by the parity test that found the loss. | Donor irrelevant: the gap was internal and is fixed. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-051 | COMMITTED | UNRESOLVED | One finding explained through source-bound evidence and rule reasoning, reachable from a real host command. | `DELIVERED / FOCUSED_PASS`; `findingsExplain` now routed by `blueprint findings explain` and proven end to end through the real CLI. | Prior comparison named no donor binding explanation to source evidence. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-052 | COMMITTED | UNRESOLVED | Source-bound evidence pack for an explicitly selected set of findings, produced through the governed daemon path. | `DELIVERED / FOCUSED_PASS`; `findingsEvidencePack` now routed by `blueprint findings evidence-pack`, refusing an empty selection. | Prior comparison named no donor producing a generation-bound evidence pack. | Keep UNRESOLVED until RELEASED-bound qualification. |
| BPT-057 | COMMITTED | UNRESOLVED | Poisoned plugin manifests refused before acceptance by path confinement, shape, type, permission ceiling, checksum and publisher trust, deciding from bytes without importing or executing anything the manifest names. | `DELIVERED / FOCUSED_PASS`; `src/providers/plugin-loader.mjs` running on every build with refusals sealed into the generation. | Donor irrelevant: there was previously no acceptance path to compare. | Keep UNRESOLVED until RELEASED-bound qualification. |
