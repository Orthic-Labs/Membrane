# Cortex governed lifecycle comparison receipt

Baseline revision: `ba7db9a86996c793a90ee60fe049d43c402ebedc`. This receipt carries forward the completed Cortex audit/design decisions without rerunning donor research. It compares the new canon contracts with the actual pre-branch Membrane baseline and records only source-level implementation state; release qualification remains separate.

| Atom | Scope | Competitive disposition | Best mechanism | Current evidence | Donor evidence | Gap / action |
|---|---|---|---|---|---|---|
| CTX-040 | COMMITTED | UNRESOLVED | Named/versioned retrieval-only recipe over existing permitted Cortex hybrid recall, with explicit bounds, projection identity, requested/actual execution and typed fallback. | `PARTIAL / PENDING`; `engine/crates/membrane-runtime/src/cortex_lifecycle.rs` implements `recall_recipe`; `membrane_memory` exposes it; source tests cover supported, unsupported and explicit fallback identities. | Earlier Cortex audit/supplement review found configurable-recipe mechanisms useful but rejected donor graph/answer-synthesis authority expansion. No new donor bake-off was required. | Qualify deterministic execution and receipt identity through managed Rust CI and released consumer evidence; recipe must never enable CTX-039 or widen policy. |
| CTX-041 | COMMITTED | UNRESOLVED | Canonical signed recall-suppression control state, applied by every ordinary recall/resolver gate, persisted across reopen/reindex and included in digest-sealed backup/restore, with signed CAS-fenced reversal. | `PARTIAL / PENDING`; `cortex_recall_suppression_v1`, lifecycle review, store eligibility filters, resolver check, reindex filter and backup/restore source test are present on this branch. | Earlier audit/supplement review preferred reversible canonical suppression over index deletion or destructive forgetting. No donor supplied stronger authority/replay semantics. | Qualify all ordinary recall lanes, restart/reindex/restore and reversal in managed Rust CI; keep hard erase separate. |
| CTX-042 | EXPLORATORY | NOT_COMMITTED | Scoped, evidenced, reversible alias resolution only if separately promoted. | `MISSING / PENDING`; no production alias store/resolver is introduced by this branch. | Donor alias mechanisms were deliberately not adopted because automatic identity merge can widen semantics and authority. | HOLD. Do not implement or promote without explicit future authority and false-merge/retraction qualification. |
