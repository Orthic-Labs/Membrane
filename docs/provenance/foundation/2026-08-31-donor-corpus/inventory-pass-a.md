# Foundation donor inventory — pass A

Frozen corpus: 26 manifests at commits recorded in `corpus.json`, frozen 2026-08-31. Commit summary: `abhigyanpatwari__GitNexus` 94f67d79, `caura-ai__caura` a750d35e, `colbymchenry__codegraph` 6a056ec5, `cq27-dev__rag-rat` c8b82755, `Egonex-AI__Understand-Anything` ba450c43, `emulo` 7f80fd8d, `getzep__graphiti` 8b61fce9, `github__codeql` fa8a2870, `graph-memory-starter` 496a6e9d, `hindsight` d2e8fa72, `honcho` 82a92429, `intuit__infigraph` 1c3622f1, `joernio__joern` 9b1de27e, `luuuc__sense` a4317d50, `MemTensor__MemOS` 185ebdb9, `mengram` 5449c1cd, `mnemon` e04c3b50, `mnemosyne-oss__mnemosyne` f6916b48, `Muvon__octocode` f89a24d0, `oracle__opengrok` 238a41f6, `oraios__serena` 43ae0211, `Phoenixrr2113__codebase-graph` f6f55f99, `potpie-ai__potpie` 217e51cd, `qualixar__superlocalmemory` 85483816, `supermemoryai__supermemory` 9ccd1b64, `volcengine__OpenViking` 2c88269d.

Scope: operative production source only. Evaluated repos: 26. Unresolved repos: 0. Excluded repos: 0. Excluded non-operative mechanisms: governed relation aliases & search/reasoning displacement evaluation in `graph-memory-starter`; benchmark, generated, build, cache, test-only, docs, README, marketing, issue, & report surfaces corpus-wide.

| Platform | Domain | Atom | Definition / boundary | Source evidence |
|---|---|---|---|---|
| Windows, macOS, Linux, local service/runtime | Pull | Recall eligibility gate | Decide if prompt may trigger recall from explicit mode, keywords, session state, noise rules, & minimum length; boundary ends before retrieval. | Observed — caura-ai__caura: `plugin/src/context-engine.ts#shouldRecall`; live consumer `CauraContextEngine.assemble`; mechanism evaluates always, never, keywords, & auto policy before search; session deny state & rate-limit state remain in engine, denied input returns no recall; focused safeguards reject slash commands, noise, trivial prompts, & short input. |
| Windows, macOS, Linux, local service/runtime | Pull | Hybrid lexical-semantic fusion | Retrieve independent lexical & vector legs, normalize/fuse ranks, & remain useful if vector leg is unavailable; boundary excludes graph expansion. | Observed — graph-memory-starter: `rag/search.py#keyword_leg`, `#meaning_leg`, `#fuse`; live consumer `rag/search.py#main` & `rag/recall_hook.py#hits`; mechanism fuses FTS5 & embedding ranks with reciprocal-rank fusion; SQLite index persists rows/vectors, missing embeddings produce keyword-only results; grounding rows without valid source are excluded by index build.<br>Observed — hindsight: `hindsight-api-slim/hindsight_api/engine/search/retrieval.py#retrieve`; live consumer `hindsight-api-slim/hindsight_api/engine/memory_engine.py#MemoryEngine.recall`; mechanism runs semantic, BM25, graph, & temporal strategies then fusion; database retains memories/links, disabled or failed optional legs contribute no candidates; per-strategy limits bound input.<br>Observed — luuuc__sense: `internal/search/search.go#Search`; live consumer `internal/mcpserver/tools.go#handleSearch`; mechanism combines vector search with text fallback then enrichment; SQLite index persists chunks/graph, unavailable vector path falls back to text; result limits & repository scope guard output. |
| Windows, macOS, Linux, local service/runtime | Pull | Grounded future-question projection | Convert source text into likely future questions, then index those questions as query-facing text only when cited quote exists in source; boundary excludes runtime query rewriting. | Observed — graph-memory-starter: `rag/distil.py#distil_one`, `#run` & `rag/build_index.py#parse_entry`; live consumer `rag/build_index.py#main`, then `rag/search.py#keyword_leg`; mechanism derives questions plus quote, verifies normalized quote containment, & places question/summary/rule/quote in FTS body; per-note SHA-256 state persists changed-note work, invalid or failed records are dropped while source chunks remain searchable; quote containment is focused grounding safeguard. |
| Windows, macOS, Linux, local service/runtime | Pull | Entity-alias query seeding | Resolve stored entity aliases as equivalent seed mentions before graph traversal; boundary excludes predicate or relation aliases. | Observed — graph-memory-starter: `src/recall.py#_seeds`; live consumer `src/recall.py#recall`; mechanism applies exact word-boundary matching over entity names plus stored aliases, then starts recursive relation walk; aliases persist in SQLite, unmatched query yields empty `Facts`; build inserts alias only when normalized entity resolves. |
| Windows, macOS, Linux, local service/runtime | Pull | Bounded graph-neighborhood recall | Expand matched seeds through relation edges for bounded hops/top-k, returning evidence-bearing facts; boundary excludes embedding retrieval. | Observed — graph-memory-starter: `src/recall.py#recall`, `#Facts.as_text`; live consumer module CLI; mechanism recursively walks relations in both directions & ranks bounded facts; SQLite entities/relations/source documents persist, no seeds return empty facts; hop & top-k caps prevent unbounded expansion.<br>Observed — getzep__graphiti: `graphiti_core/graphiti.py#Graphiti.search_`; live consumer `server/graph_service/routers/retrieve.py#search`; mechanism searches entity/community/edge graph with configured recipe & optional reranker; graph driver persists nodes/edges, empty candidates return empty results; recipe limits & filters bound retrieval. |
| Windows, macOS, Linux, local service/runtime | Pull | Whole-evidence budget trimming | Admit complete evidence hits until per-hit & aggregate character/token budgets are exhausted; never cut protected evidence into misleading fragments. | Observed — graph-memory-starter: `rag/recall_hook.py#block`; live consumer `rag/recall_hook.py#main`; mechanism appends whole retrieved hits within per-hit & total character ceilings to hook `additionalContext`; no persistent state beyond index, parse/index/search failure emits nothing; exception boundary never blocks prompt.<br>Observed — caura-ai__caura: `plugin/src/context-engine.ts#trimToTokenBudget`; live consumer `CauraContextEngine.assemble`; mechanism trims retrieved memory context against configured token budget; session LRU/cache state is bounded, timeout/search failure yields unaugmented prompt; AbortController timeout caps wait. |
| Windows, macOS, Linux, local service/runtime | Pull | Provider-chain rerank failover | Try ordered rerank providers & preserve retrieval order/results when provider fails; boundary excludes candidate retrieval. | Observed — hindsight: `hindsight-api-slim/hindsight_api/config.py#HindsightConfig.reranker_chain` & `hindsight-api-slim/hindsight_api/engine/search/reranking.py#rerank_results`; live consumer `hindsight-api-slim/hindsight_api/engine/memory_engine.py#MemoryEngine.recall`; mechanism invokes configured primary then indexed failover members; provider configuration persists, total failure retains fused candidates; per-provider exception handling & chain order are safeguards. |
| Windows, macOS, Linux, local service/runtime | Pull | Scope-bound conversational search | Search messages only inside selected session, workspace, or peer scope; boundary excludes authoring new memories. | Observed — honcho: `src/utils/search.py#search`; live consumers `src/routers/sessions.py#search_session`, `src/routers/workspaces.py#search_workspace`, & `src/routers/peers.py#search_peer`; mechanism resolves scope to message IDs/vector namespace then queries storage; PostgreSQL messages & vector namespaces persist, empty scope returns empty matches; authorization dependency & scope filters prevent cross-scope admission. |
| Windows, macOS, Linux, local service/runtime | Pull | Evidence-bound repository memory retrieval | Return memory only through current path/symbol/chunk/edge anchors with anchor health & source evidence; boundary excludes free-form unanchored notes. | Observed — cq27-dev__rag-rat: `crates/rag-rat-query/src/memory/api.rs#memory_evidence_for_symbol_and_edges`; live consumer `crates/rag-rat-cli/src/commands/memory.rs#execute`; mechanism resolves symbol plus edge bindings & hydrates evidence rows; SQLite persists memories/anchors, unresolved/stale anchors are surfaced by validation/doctor state rather than silently trusted; `crates/rag-rat-query/src/memory/validate.rs#validate_memories` checks hashes & binding status. |
| Windows, macOS, Linux, local service/runtime | Pull | Repository context assembly | Extract query symbols, retrieve candidate files/symbols, expand graph neighbors, & pack within budget; boundary excludes index construction. | Observed — colbymchenry__codegraph: `src/context/index.ts#ContextBuilder.buildContext`; live consumer `src/mcp/tools.ts#contextTool`; mechanism runs symbol search, type-hierarchy/relationship expansion, entry-point weighting, file diversity, & token packing; database persists graph, missing symbols yield partial/empty context; visited sets, depth, file, & token limits bound assembly.<br>Observed — abhigyanpatwari__GitNexus: `gitnexus/src/mcp/tools.ts#GITNEXUS_TOOLS`; live consumer `gitnexus/src/mcp/server.ts#startMCPServer`; mechanism exposes context/query tools over indexed symbol graph & hybrid search; Kuzu/index storage persists graph, absent index returns typed tool error; schema validation & result limits guard calls. |
| Windows, macOS, Linux, local service/runtime | Pull | Dual-retriever consensus | Run independent lexical & semantic retrieval, identify overlap, & return consensus metadata without treating disagreement as proof; boundary excludes downstream graph enrichment. | Observed — Phoenixrr2113__codebase-graph: `packages/core/src/consensus.ts#dualRetrieve`, `#computeConsensus`; live consumer `packages/core/src/service.ts#CodeGraphServiceImpl.search`; mechanism compares keyword & vector candidate identities and annotates agreement; graph/vector stores persist index state, unavailable semantic leg leaves lexical results with reduced consensus; bounded top-k & deduplication safeguard output. |
| Windows, macOS, Linux, local service/runtime | Pull | Fielded source search | Search indexed source by text, definition, symbol, path, or history field under project scope; boundary excludes structural graph reasoning. | Observed — oracle__opengrok: `opengrok-indexer/src/main/java/org/opengrok/indexer/search/SearchEngine.java#search`; live consumer `opengrok-web/src/main/java/org/opengrok/web/api/v1/controller/SearchController.java#search`; mechanism builds Lucene query from fielded request & project filter; Lucene indexes persist, zero matches return empty result; project authorization/filtering & result pagination constrain visibility. |
| Windows, macOS, Linux, local service/runtime | Pull | Declarative interprocedural flow query | Resolve value flow through language semantic model & configurable sources/sinks; boundary excludes runtime execution traces. | Observed — github__codeql: `javascript/ql/lib/semmle/javascript/dataflow/DataFlow.qll#module DataFlow`; live consumer production queries under `javascript/ql/src/Security/CWE-079/ReflectedXss.ql#query`; mechanism evaluates dataflow predicates over extracted CodeQL database; database persists facts, absent paths yield no result; source/sink barriers & query metadata constrain reported flows. |
| Windows, macOS, Linux, local service/runtime | Pull | Code property graph traversal | Traverse typed call/dataflow edges from methods or expressions; boundary excludes text similarity. | Observed — joernio__joern: `semanticcpg/src/main/scala/io/shiftleft/semanticcpg/language/callgraphextension/MethodTraversal.scala#MethodTraversal`; live consumer query layers in `querydb/src/main/scala/io/joern/scanners/`; mechanism follows CPG caller/callee edges exposed as traversal steps; CPG storage persists graph, missing edges yield empty traversal; typed node/edge predicates constrain expansion. |
| Windows, macOS, Linux, local service/runtime | Pull | Language-server symbol lookup | Resolve definitions, references, callers, & containing symbols through language-server index; boundary excludes semantic embeddings. | Observed — oraios__serena: `src/serena/tools/symbol_tools.py#FindSymbolTool`; live consumer `src/serena/mcp.py#SerenaMCPServer`; mechanism routes MCP request through active project language server & symbol manager; project cache/memories persist separately, unavailable language server returns tool error; project activation & relative-path filters scope results. |
| Windows, macOS, Linux, local service/runtime | Pull | Semantic code-chunk search | Embed query & indexed code chunks, rank nearest matches, & surface source locations; boundary excludes graph traversal. | Observed — Egonex-AI__Understand-Anything: `understand-anything-plugin/packages/core/src/embedding-search.ts#searchEmbeddings`; live consumer `understand-anything-plugin/packages/core/src/search.ts#searchKnowledgeBase`; mechanism compares query embedding with persisted knowledge embeddings; local knowledge/index files retain vectors, unavailable embedding path falls back to text search; score threshold & result cap guard output.<br>Observed — Muvon__octocode: `src/indexer/search.rs#search`; live consumer `src/commands/search.rs#execute` & `src/mcp/semantic_code.rs#semantic_code`; mechanism embeds query then searches local vector store with filters; branch/index state persists, absent index returns actionable error; threshold, path filter, & maximum result count bound output. |
| Windows, macOS, Linux, local service/runtime | Pull | Hierarchical URI recall | Resolve memory/resource paths plus query into concise recalled context while preserving addressable origin; boundary excludes session mutation. | Observed — volcengine__OpenViking: `openviking/server/routers/search.py#recall`; live consumer `openviking/server/mcp_endpoint.py#recall`; mechanism searches hierarchical Viking URIs then returns recalled content/context; local vector/index & filesystem state persist, no hit yields empty items; client identity, limit, & score parameters constrain results. |
| Windows, macOS, Linux, local service/runtime | Blueprint | Multi-language symbol/reference graph build | Parse supported files into symbols & typed references, then persist repository graph; boundary excludes retrieval ranking. | Observed — abhigyanpatwari__GitNexus: `gitnexus/src/core/ingestion/pipeline.ts#runPipeline`; live consumer `gitnexus/src/cli/analyze.ts#analyze`; mechanism applies language parsers, resolves symbols/relationships, & writes Kuzu graph; repository index persists, unsupported files are skipped; ignore rules & parse diagnostics bound ingestion.<br>Observed — colbymchenry__codegraph: `src/extraction/index.ts#extractFile`; live consumer `src/sync/index.ts#syncProject`; mechanism selects language extractor then upserts nodes/edges; database persists graph, parse failure records no file graph; supported-extension & ignore filters safeguard scope. |
| Windows, macOS, Linux, local service/runtime | Blueprint | Normalized knowledge-graph build | Canonicalize analyzed files, concepts, & relations before persistence; boundary excludes query-time search. | Observed — Egonex-AI__Understand-Anything: `understand-anything-plugin/packages/core/src/analyzer/graph-builder.ts#buildKnowledgeGraph` & `understand-anything-plugin/packages/core/src/analyzer/normalize-graph.ts#normalizeGraph`; live consumer analyzer orchestration in `understand-anything-plugin/packages/core/src/index.ts#analyzeRepository`; mechanism creates then normalizes graph identities/edges; persistence module stores graph, invalid/duplicate edges are normalized or omitted; schema/type validation guards output. |
| Windows, macOS, Linux, local service/runtime | Blueprint | Incremental file reindex | Replace graph facts for changed files & remove deleted-file facts without full rebuild; boundary excludes branch overlay semantics. | Observed — intuit__infigraph: `crates/infigraph-core/src/lib.rs#Infigraph.index_file`, `#remove_file`; live consumer CLI watch/index commands in `crates/infigraph-cli/src/main.rs#main`; mechanism parses changed path, upserts graph, or removes prior file facts; graph backend & file hashes persist, parse failure leaves prior/typed failure state; write lock prevents concurrent mutation.<br>Observed — qualixar__superlocalmemory: `src/superlocalmemory/code_graph/incremental.py#IncrementalUpdater.update`; live consumer `src/superlocalmemory/code_graph/watcher.py#CodeGraphWatcher`; mechanism batches filesystem changes into graph-store build commit; SQLite/vector graph persists, deleted paths remove associated nodes; batch transaction & ignored-path filters guard updates. |
| Windows, macOS, Linux, local service/runtime | Blueprint | Branch-overlay index | Preserve base index while storing changed branch paths as overlay with explicit precedence; boundary excludes Git history analysis. | Observed — Muvon__octocode: `src/indexer/branch.rs#BranchIndex.overridden_paths`; live consumer `src/commands/branch.rs#execute`; mechanism records branch metadata & path overrides above main index; local store persists branch index, missing branch falls back to base index; sanitized branch names & override set prevent cross-branch collision. |
| Windows, macOS, Linux, local service/runtime | Blueprint | Live repository watcher | Observe filesystem changes, debounce bursts, & trigger per-file reindex/delete; boundary excludes initial full index. | Observed — Phoenixrr2113__codebase-graph: `packages/core/src/watchService.ts#WatchService.start`; live consumer service startup in `packages/core/src/service.ts#CodeGraphServiceImpl.watch`; mechanism applies gitignore-aware watcher events to `indexSingleFile` or deletion; graph/vector stores persist state, stopped watcher makes no mutations; debounce, ignore rules, & explicit stop lifecycle bound work. |
| Windows, macOS, Linux, local service/runtime | Blueprint | Cross-language bridge detection | Recognize explicit FFI/JNI/gRPC/PInvoke/COM seams as typed graph bridges; boundary excludes inferred semantic similarity. | Observed — intuit__infigraph: `crates/infigraph-core/src/bridges/mod.rs#detect_bridges`; live consumer `crates/infigraph-core/src/lib.rs#Infigraph.detect_bridges`; mechanism scans indexed symbols/calls & emits typed bridge records; graph backend persists source facts, absent matching pair yields none; bridge-kind matchers & path evidence constrain links. |
| Windows, macOS, Linux, local service/runtime | Cortex | Temporal episodic graph ingestion | Add episode with event/reference time, extract entities/facts, & persist temporal links; boundary excludes later recall. | Observed — getzep__graphiti: `graphiti_core/graphiti.py#Graphiti.add_episode`; live consumer `server/graph_service/routers/ingest.py#add_episode`; mechanism extracts/deduplicates nodes & edges, links episode sequence, & stores valid/invalid timestamps; graph driver persists episodes/entities/edges, extraction failure aborts episode write; source episode, group, & reference time remain attached. |
| Windows, macOS, Linux, local service/runtime | Cortex | Fact validity interval | Preserve when relation became valid, invalid, or expired separately from ingestion time; boundary excludes user-facing temporal query parsing. | Observed — getzep__graphiti: `graphiti_core/graph_queries.py#add_entity_edge`; live consumer `graphiti_core/graphiti.py#Graphiti.add_episode`; mechanism writes `valid_at`, `invalid_at`, & `expired_at` on entity edges; graph database persists intervals, unknown end remains null; edge invalidation/deduplication logic prevents simultaneous contradictory active facts. |
| Windows, macOS, Linux, local service/runtime | Cortex | Incremental episode summary watermark | Summarize only episodes newer than persisted temporal watermark & advance watermark after successful update; boundary excludes raw episode storage. | Observed — getzep__graphiti: `graphiti_core/graphiti.py#Graphiti._update_saga_summary`; live consumer `Graphiti.add_episode`; mechanism selects post-watermark episodes, caps batch, writes summary plus maximum `valid_at`; saga node persists summary/watermark, no new episodes produces no write; bounded batch & success-before-watermark update prevent skipped history. |
| Windows, macOS, Linux, local service/runtime | Cortex | Structured fact retention | Extract discrete facts/entities/links from incoming content, then store durable records with source context; boundary excludes recall. | Observed — hindsight: `hindsight-api-slim/hindsight_api/engine/retain/orchestrator.py#retain`; live consumer `hindsight-api-slim/hindsight_api/engine/memory_engine.py#MemoryEngine.retain`; mechanism orchestrates fact extraction, entity resolution, link creation, & storage; database persists memories/entities/links, failed extraction leaves request error/no partial commit; transaction & input-size limits guard retention. |
| Windows, macOS, Linux, local service/runtime | Cortex | Reflective derived-memory update | Derive higher-level observations from retained memories while recording additions/retractions as explicit deltas; boundary excludes direct overwrite of raw memories. | Observed — hindsight: `hindsight-api-slim/hindsight_api/engine/reflect/agent.py#ReflectAgent.run` & `hindsight-api-slim/hindsight_api/engine/reflect/delta_ops.py#apply_delta_operations`; live consumer `hindsight-api-slim/hindsight_api/engine/memory_engine.py#MemoryEngine.reflect`; mechanism runs constrained reflection tools then applies add/retract delta operations; observation state persists separately, failed tool run leaves prior observations; operation validation & bounded iterations guard mutation. |
| Windows, macOS, Linux, local service/runtime | Cortex | Graph memory node/edge CRUD | Create, update, delete, & embedding-search memory graph nodes/edges through storage abstraction; boundary excludes memory extraction. | Observed — MemTensor__MemOS: `src/memos/graph_dbs/base.py#BaseGraphDB` & `src/memos/graph_dbs/neo4j.py#Neo4jGraphDB.search_by_embedding`; live consumer `src/memos/memories/textual/tree_text_memory/retrieve/searcher.py#GraphSearcher`; mechanism dispatches typed graph CRUD/search to Neo4j; graph database persists nodes/edges/vectors, no match returns empty list; parameterized queries & dimension checks guard access. |
| Windows, macOS, Linux, local service/runtime | Cortex | Separate episodic/procedural recall | Store & query lived episodes independently from reusable procedures, then compose when requested; boundary excludes ontology graph search. | Observed — mengram: `engine/brain.py#Brain.recall`, `#search_procedures`; live consumer `api/rest_server.py#recall`; mechanism queries vault episode/procedure collections through hybrid retrieval; vault/vector state persists, missing collection yields empty matches; user/brain scope & result limits constrain recall. |
| Windows, macOS, Linux, local service/runtime | Cortex | Durable mutation receipt log | Append immutable operation record for remember/link/forget mutations & expose receipt lookup; boundary excludes mutable materialized node state. | Observed — mnemon: `internal/memory/store/oplog.go#Store.AppendOp`; live consumers `cmd/memory/remember.go#runRemember`, `cmd/memory/link.go#runLink`, `cmd/memory/forget.go#runForget`, & `cmd/memory/receipt.go#runReceipt`; mechanism commits operation plus receipt identity alongside graph mutation; SQLite oplog persists history, failed transaction emits no receipt; transaction & content hash bind record to mutation. |
| Windows, macOS, Linux, local service/runtime | Cortex | Typed memory lifecycle | Store memory with explicit type & allow working-memory update/forget without conflating durable classes; boundary excludes retrieval ranking. | Observed — mnemosyne-oss__mnemosyne: `mnemosyne/core/typed_memory.py#TypedMemory`; live consumers `mnemosyne/mcp_tools.py#remember`, `#update_working`, `#forget_working`; mechanism validates type then routes create/update/delete through memory core; configured backend persists durable types while working state is replaceable, missing working item returns typed miss; type schema & ownership scope guard mutation. |
| Windows, macOS, Linux, local service/runtime | Cortex | Temporal/veracity consolidation | Parse temporal claims & reconcile supporting, contradicting, or superseding memories into explicit consolidated state; boundary excludes query fusion. | Observed — mnemosyne-oss__mnemosyne: `mnemosyne/core/temporal_parser.py#TemporalParser` & `veracity_consolidation.py#VeracityConsolidator`; live consumer `mnemosyne/core/memory.py#Memory.add`; mechanism extracts time bounds then evaluates existing claims before persistence; memory backend retains consolidated links/status, ambiguous time remains unspecified; confidence/type checks prevent silent fact replacement. |
| Windows, macOS, Linux, local service/runtime | Cortex | Windowed durable-context ingestion | Accept raw episodes, batch by window, process into durable context, & recover stale in-flight batches; boundary excludes downstream readers. | Observed — potpie-ai__potpie: `potpie/context-engine/src/potpie_context_engine/application/use_cases/submit_raw_episode.py#SubmitRawEpisode`, `process_batch.py#ProcessBatch`, & `reap_stale_batches.py#ReapStaleBatches`; live consumer context API in `potpie/context-engine/src/potpie_context_engine/api.py#create_app`; mechanism queues episode, leases batch, records durable context, & requeues stale lease; repository/database state persists episodes/batches, failed batch retains retryable state; idempotency key, lease timeout, & status transitions guard replay. |
| Windows, macOS, Linux, local service/runtime | Cortex | Policy-driven retention & erasure | Apply lifecycle/retention rule & delete profile/entity facts plus derived projections; boundary excludes ordinary record edit. | Observed — qualixar__superlocalmemory: `src/superlocalmemory/compliance/retention.py#RetentionManager.add_rule`, `src/superlocalmemory/compliance/gdpr.py#GDPRManager.forget_profile`, `#forget_entity`; live consumer package service/CLI compliance commands; mechanism records retention policy then deletes matching facts, temporal validity, graph edges, & projection outbox entries; SQLite persists rules/tombstones, absent subject is idempotent no-op; transaction & `src/superlocalmemory/storage/projection_outbox.py#forget_availability` verify projection erasure. |
| Windows, macOS, Linux, local service/runtime | Cortex | Agent-turn memory middleware | Retrieve user memories before model turn, inject formatted context, then optionally persist conversation after turn; boundary excludes backend memory semantics. | Observed — supermemoryai__supermemory: `packages/agent-framework-python/src/supermemory_agent_framework/middleware.py#SupermemoryChatMiddleware`; live consumer framework middleware hooks `process`/`process_response`; mechanism calls context provider/tools, deduplicates memories, injects context, & submits conversation; remote service owns persistence, timeout/network failure continues without memory; timeout handling, deduplication, & configurable user/container scope guard behavior. |
| Windows, macOS, Linux, local service/runtime | Cortex | Streaming memory-update outcomes | Apply streamed memory create/edit/delete decisions & report each item as recorded, edited, deleted, skipped, or error; boundary excludes session message capture. | Observed — volcengine__OpenViking: `openviking/session/memory/streaming_memory_updater.py#StreamingMemoryUpdater`; live consumer session commit route `openviking/server/routers/sessions.py#commit_session`; mechanism consumes extracted memory changes & writes hierarchical memory files/index entries while emitting per-item results; filesystem/vector state persists, failed item records error without hiding other outcomes; item validation & explicit outcome enum safeguard partial progress. |
| Windows, macOS, Linux, local service/runtime | Ledger | Source-addressed evidence rendering | Render retrieved fact with source document or file/section/line location so consumer can navigate origin; boundary excludes truth judgment. | Observed — graph-memory-starter: `src/recall.py#Facts.as_text` & `rag/search.py#main`; live consumers graph recall CLI & recall hook; mechanism carries `source_doc` for graph facts and file/section/line metadata for indexed hits; SQLite retains source identifiers, missing origin is omitted rather than invented; index grounding check rejects distilled quote without source containment. |
| Windows, macOS, Linux, local service/runtime | Ledger | Receipt ledger assembly | Convert mined source observations into stable receipt records that rules can cite; boundary excludes rule selection. | Observed — emulo: `emulo.py#build_receipt_ledger`; live consumer `emulo.py#main` adaptive pipeline; mechanism assigns evidence records to ledger before scoring/rule drafting; run directory persists receipt artifacts, empty mining yields empty ledger; `receipt_is_verbatim` & report validation prevent unsupported receipt text. |
| Windows, macOS, Linux, local service/runtime | Adapt | Evidence-bound rule admission | Admit learned rule only when schema, domain, & cited receipts validate; boundary excludes runtime profile activation. | Observed — emulo: `emulo.py#validate_rule`, `#validate_domain_draft`, `#validate_profile_pack`; live consumer adaptive pipeline before `activate_profile_pack`; mechanism checks rule structure, source receipt bindings, domain constraints, & pack integrity; candidate artifacts persist separately from active pack, invalid draft is rejected; verbatim receipt checks & report validation are focused safeguards. |
| Windows, macOS, Linux, local service/runtime | Adapt | Adaptive deepening plan | Select which domain needs another evidence pass from current coverage/findings, with bounded stages; boundary excludes executing selected work. | Observed — emulo: `emulo.py#build_adaptive_plan`, `#domain_needs_deepening`; live consumer `emulo.py#prepare_adaptive_run`; mechanism derives next stage & domain queue from current run findings; run state persists plan, no deficient domain stops deepening; stage ceiling & validation prevent unbounded loop. |
| Windows, macOS, Linux, local service/runtime | Push | Salience-bounded receipt packing | Score evidence receipts, select within budget, & pack records without mutating source; boundary excludes later model reasoning. | Observed — emulo: `emulo.py#score_receipts`, `#select_salience_stage`, `#pack_selected_receipts`; live consumer adaptive pipeline prompt preparation; mechanism ranks receipts then fits selected whole records into configured budget; run artifacts retain selection, no admissible receipts yields empty pack; deterministic ordering & budget accounting guard truncation. |
| Windows, macOS, Linux, local service/runtime | Push | Content-addressed context segments | Hash packed context segments, seal records, sync changed segments, & select bounded segment set; boundary excludes semantic retrieval. | Observed — emulo: `emulo.py#segment_hash`, `#seal_records`, `#sync_segments`, `#select_segments`; live consumer profile/adaptive run preparation; mechanism creates hash-addressed immutable segment records & updates segment registry; local run/profile state persists hashes, unchanged segment is reused; hash verification & record validation detect drift. |
| Windows, macOS, Linux, local service/runtime | Push | Transactional profile activation | Replace active learned profile only after candidate pack validation, preserving previous state on failure; boundary excludes candidate generation. | Observed — emulo: `emulo.py#activate_profile_pack`, `#replace_directory`, `#atomic_write_text`; live consumer adaptive pipeline completion; mechanism stages validated pack then atomically replaces active directory/files; prior profile remains on pre-replace failure, activation state persists locally; validation-before-replace & atomic filesystem writes guard partial activation. |

## Applicability & stop notes

- Pull/code retrieval saturation: `abhigyanpatwari__GitNexus`, `colbymchenry__codegraph`, `Egonex-AI__Understand-Anything`, `github__codeql`, `joernio__joern`, `luuuc__sense`, `Muvon__octocode`, `oracle__opengrok`, `oraios__serena`, & `Phoenixrr2113__codebase-graph`. Stopped after operative ingest plus retrieval contracts repeated: parse/index, scope, bounded query, incremental refresh, or graph traversal.
- Memory saturation: `caura-ai__caura`, `cq27-dev__rag-rat`, `getzep__graphiti`, `graph-memory-starter`, `hindsight`, `honcho`, `MemTensor__MemOS`, `mengram`, `mnemon`, `mnemosyne-oss__mnemosyne`, `potpie-ai__potpie`, `qualixar__superlocalmemory`, `supermemoryai__supermemory`, & `volcengine__OpenViking`. Stopped after operative admission, persistence/lifecycle, recall, provenance, & failure paths repeated.
- Adapt/reduction saturation: `emulo`. Stopped after receipt admission, bounded adaptive planning, segment packing, & transactional activation covered full operative loop.
- Cross-repository graph saturation: `intuit__infigraph`. Stopped after persistent index, incremental mutation, & typed bridge contracts; benchmark/check outputs excluded.
- No repo lacked applicable operative mechanism. `graph-memory-starter` relation strings are extraction outputs, not governed aliases; aliases govern entity seed resolution only. No production path evaluates search-versus-reasoning displacement, so no atom admitted for that concept.

## Adversarial self-review

Checked every manifest name appears in observed evidence or applicability notes, split retrieval from ingestion/persistence, merged only mechanism-level synonyms, preserved single-repo atoms, & removed claims supported only by docs/tests/benchmarks. Re-read special-attention paths: future-question projection & entity query aliases remain source-backed; governed relation aliases & displacement evaluation remain excluded. Citation scan targets production relative paths, symbols, live callers, state/fallback, & safeguards.

Inventory rows: 44. Validator: PASS.
