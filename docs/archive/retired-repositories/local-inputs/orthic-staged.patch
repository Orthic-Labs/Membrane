diff --git a/package.json b/package.json
index d04cb5f..b40ddab 100644
--- a/package.json
+++ b/package.json
@@ -20,7 +20,7 @@
     "@tauri-apps/plugin-os": "2.3.2"
   },
   "devDependencies": {
-    "@rightkit/release": "0.2.65",
+    "@rightkit/release": "0.2.66",
     "@tauri-apps/cli": "^2.0.0"
   }
 }
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 0d35ee6..78f360e 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -19,8 +19,8 @@ importers:
         version: 2.3.2
     devDependencies:
       '@rightkit/release':
-        specifier: 0.2.65
-        version: 0.2.65
+        specifier: 0.2.66
+        version: 0.2.66
       '@tauri-apps/cli':
         specifier: ^2.0.0
         version: 2.11.4
@@ -33,8 +33,8 @@ packages:
       '@tauri-apps/plugin-os': '>=2 <3'
       react: '>=18 <20'
 
-  '@rightkit/release@0.2.65':
-    resolution: {integrity: sha512-jtozfpStpF+bDCs/mvFF6DDQ7C2YNxk7VJcrMiUEo/dB4x6foZzXh6CNpmMCP3sGi2yz8osfcHjLYhx1EqqAYw==}
+  '@rightkit/release@0.2.66':
+    resolution: {integrity: sha512-DpAN7NGi930kZUvV5SWBsPVAKTeqCQtvXaWduJ2xzoi95liMHsGIurCK5BxiZWw/fMhkDV49KOz8+CjO1mUA/A==}
     hasBin: true
 
   '@tauri-apps/api@2.11.1':
@@ -130,7 +130,7 @@ snapshots:
       '@tauri-apps/plugin-os': 2.3.2
       react: 19.2.8
 
-  '@rightkit/release@0.2.65': {}
+  '@rightkit/release@0.2.66': {}
 
   '@tauri-apps/api@2.11.1': {}
 
diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
index 353fd6e..73b7a33 100644
--- a/pnpm-workspace.yaml
+++ b/pnpm-workspace.yaml
@@ -1,2 +1,2 @@
 minimumReleaseAgeExclude:
-  - '@rightkit/release@0.2.61 || 0.2.62 || 0.2.63 || 0.2.65'
+  - '@rightkit/release@0.2.61 || 0.2.62 || 0.2.63 || 0.2.65 || 0.2.66'
diff --git a/scripts/lib/target-root.mjs b/scripts/lib/target-root.mjs
index d110580..473e3b0 100644
--- a/scripts/lib/target-root.mjs
+++ b/scripts/lib/target-root.mjs
@@ -2,14 +2,23 @@ import { execFileSync } from "node:child_process";
 import path from "node:path";
 
 // A managed build owns the target root, so `src-tauri/target` is not where the
-// output lands. Honour the injected directory and otherwise ask Cargo — the
-// same resolution HeardRight's scripts/lib/target-root.mjs uses.
+// output lands. `cargo metadata`'s target_directory is the sole source of
+// truth for locating it — a broker-managed CARGO_TARGET_DIR can be set but
+// stale, so it must never be used to locate output. Fail closed if metadata
+// cannot be resolved, naming the manifest path.
 export function cargoTargetRoot(appRoot) {
-  if (process.env.CARGO_TARGET_DIR) return path.resolve(appRoot, process.env.CARGO_TARGET_DIR);
-  const raw = execFileSync("cargo", [
-    "metadata", "--manifest-path", "Cargo.toml", "--no-deps", "--format-version", "1",
-  ], { cwd: path.join(appRoot, "src-tauri"), encoding: "utf8", maxBuffer: 64 * 1024 * 1024 });
+  const manifestPath = path.join(appRoot, "src-tauri", "Cargo.toml");
+  let raw;
+  try {
+    raw = execFileSync("cargo", [
+      "metadata", "--manifest-path", "Cargo.toml", "--no-deps", "--offline", "--format-version", "1",
+    ], { cwd: path.join(appRoot, "src-tauri"), encoding: "utf8", maxBuffer: 64 * 1024 * 1024 });
+  } catch (err) {
+    throw new Error(`cargo metadata failed for ${manifestPath}: ${err.message}`);
+  }
   const directory = JSON.parse(raw).target_directory;
-  if (typeof directory !== "string" || !directory) throw new Error("cargo metadata did not report target_directory");
+  if (typeof directory !== "string" || !directory || !path.isAbsolute(directory)) {
+    throw new Error(`cargo metadata for ${manifestPath} did not report an absolute target_directory`);
+  }
   return path.resolve(directory);
 }
