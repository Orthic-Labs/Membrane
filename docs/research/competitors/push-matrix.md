# Push competitor input matrix

| Capability | Membrane Push | Reduction baseline | Qualification question |
|---|---|---|---|
| transform lineage | `runc` → `skel` → `compress` → `truncate` under one receipt contract | unrelated per-tool reducers | can every transform recover its source & receipt? |
| recoverability | content-addressed spill/restore | lossy truncation | can exact source be restored? |
| protected evidence | span verification | token-only pruning | are identifiers/errors retained? |
| telemetry ownership | host sink, no Cortex write | durable transform table | does reduction remain storage-independent? |

This is an input matrix, not a product claim; measured values live in
qualification receipts.
