# Doc Spine local end-to-end proof

## Product outcome

This authored Membrane workspace document proves registration, outline navigation, exact-section
fetch, and retrievable source pointers without turning document content into durable memory.
Retrieval probe: `amber729proof`.

## Exact fetch target

The exact section must return this sentence and no truncated document dump: **Doc Spine fetches
the requested anchor as source-bound content.**

## Safety boundary

This document is a machine-local source artifact. It is not durable memory and it carries no
authority beyond its repository scope.
