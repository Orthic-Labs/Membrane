# Native federation conformance fixtures

These JSON files are language-neutral frozen inputs/outputs consumed by Rust tests. They contain no executable federation implementation or Python runtime dependency.
