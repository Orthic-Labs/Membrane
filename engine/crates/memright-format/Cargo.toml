[package]
name = "memright-format"
version = "0.1.0"
edition.workspace = true
description = "MemRight compatibility facade for Crypt format codecs."

[dependencies]
crypt-format = { path = "../crypt-format" }
