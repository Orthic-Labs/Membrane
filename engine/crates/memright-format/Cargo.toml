[package]
name = "memright-format"
version = "0.1.0"
edition.workspace = true
description = "Open Knowledge Format (OKF) bundle support and deterministic prose compression."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3"
