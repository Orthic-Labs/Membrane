[package]
name = "crypt-store"
version = "0.1.0"
edition.workspace = true
description = "Crypt SQLite store, installation identity, scopes, and durable telemetry."

[dependencies]
rusqlite = { version = "0.40.1", features = ["bundled"] }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
getrandom = "0.2"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
