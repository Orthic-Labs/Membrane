//! Cortex durable-knowledge subsystem public API.

// Runtime implementation remains shared with Membrane process orchestration;
// Cortex is its canonical subsystem API, crate, CLI, service, & storage identity.
pub use membrane_runtime::*;
