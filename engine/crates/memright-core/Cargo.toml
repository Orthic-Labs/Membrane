[package]
name = "memright-core"
version = "0.1.0"
edition.workspace = true
description = "MemRight compatibility facade for Crypt core primitives."

[dependencies]
crypt-core = { path = "../crypt-core" }

[features]
fastembed = ["crypt-core/fastembed"]

[dev-dependencies]
